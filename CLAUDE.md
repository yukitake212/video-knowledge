# CLAUDE.md — video-knowledge

## このリポジトリの性質

**public リポジトリ。第三者の著作物を置かない。**

- 他人が書いたプロンプトの原文は、すべて **video-corpus（private）** に置く。ここにコピーしない
- `corpus/` は `.gitignore` 済み。作業用にローカルへ置くのは構わないが、コミットしない
- 過去に一度、他人のプロンプト原文6本をこのリポジトリに入れて public のまま push した。同じことを繰り返さない

## principles/ に何を書くか

`principles/` に書くのは、原文を分解して得た**一般的な作法**であって、他人の文章そのものではない。

- 引用は短い断片にとどめる。1文以上の長さの原文をそのまま貼らない
- 断片を引くときも、**出所を必ず書く**（`(corpus/xxx)` の形。ファイル名だけでよい）
- 英語の言い回しを丸ごと持ってくるより、何をしている指定なのかを日本語で書くほうが後から効く

## 各項目に必ず書くこと

**出所と時期。** その項目がどこから来たのかを、次の3つのいずれかで明示する。

- 自社で検証した（いつ、何で）
- 他人の公開プロンプトから読んだ（どの corpus か）
- 推測（検証していない、と分かるように書く）

## 書いてはいけないこと

- **プラットフォーム固有の制約を普遍則として書かない。** 過去に「ショット生成は4秒以上」を Hailuo H3 の制約と気づかないまま普遍則として記録し、全カットの設計を縛った。モデル名・確認日とセットで `platforms/` に書く
- **「これが正解で確定」と書かない。** 断定は、新しいものが見えてきたときの障害になる。覆る前提で書く

## 覆ったとき

`CHANGELOG.md` に**理由**を残す。git log は変更を記録するが、なぜ覆ったかは残らない。何を信じていて、何が起きて、今はどう考えているかを書く。

## 運用

### 知見の取り込み

制作の相談は別のチャットで進めている。そこで得た知見は**差分ファイル（.md）**の形で受け取り、ユーザーがこのリポジトリに持ち込む。渡されたら該当ファイルへ反映してコミットする。

- どのファイルのどこに入るかは、差分ファイルの内容から判断する
- 既存項目と衝突したら、上書きせずに**覆った理由を CHANGELOG に書く**
- 出所（自社検証／他人のプロンプト／推測）が差分ファイルに無ければ、書き込む前に確認する

### 月次の棚卸し

月に一度、`principles/` の各項目を見直す回を設ける。見るのは次の2点。

- **根拠が弱いもの** — 出所が「推測」のまま検証されていない項目、1つの corpus だけを根拠にしている項目
- **古くなったもの** — `platforms/` の確認日が古い項目、モデルの世代交代で前提が変わった項目

落とした項目・覆した項目は CHANGELOG に理由とともに残す。

## 関連リポジトリ

- `video-corpus`（private）— プロンプト原文。**public にしない**
- `video-projects`（private）— 案件の作業文書
