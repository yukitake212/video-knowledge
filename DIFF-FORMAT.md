# 差分ファイルの型

知見をこのリポジトリへ持ち込むための受け渡し形式。Claude以外のLLMで調べた結果も
この形で書き出させれば、そのまま取り込める。

反映の手順は `CLAUDE.md` の「知見の取り込み」にある。ここに書くのは**差分ファイルそのものの
書き方**。

---

## 原則

**差分ファイルは、それ単体で判断できる状態になっていること。**

受け取り側（リポジトリ側の作業者）は、その調査がどこで行われたかを見ていない。
「どこから来た主張か」「検証されているのか」「どこに置くべきか」が差分ファイルに
書かれていなければ、書き込む前に毎回確認が発生する。確認が面倒になると、
出所のない行がリポジトリに混ざる。それが一番まずい。

## ファイル名と単位

- 1セッション＝1ファイル。`diff-YYYY-MM-DD-<主題>.md`
- 例：`diff-2026-09-03-retention.md`、`diff-2026-09-10-veo-specs.md`
- 主題が複数にまたがったら分ける。1ファイルに混ぜない

### 2本以上を同時に出すなら、束の索引を付ける

**同時に出す差分は、同じファイルを取り合う。** 実際に5本同時のとき、
5本すべてが `skill/SKILL.md` と `principles/trailer-structure.md` を触る提案をしていて、
**順序と欄名の衝突を決めるために、もう1本「反映順序」の差分を書く必要が出た。**

2本以上をまとめて渡すときは、先頭に索引を1枚置く。

```
- 束の名前 / 本数
- 触るファイル一覧：どのファイルを何本が触るか（多い順）
- 「既存を覆す」項目の一覧：どの差分のどの項目か
- 新設を提案するファイルの一覧：**同じファイルへ別々の欄を提案していないか確認済みか**
- 推奨する反映順序と、その理由
```

**新設ファイル名の重複と、同じ新設ファイルへの欄名の衝突は、束の側でしか見えない。**

## ヘッダ

ファイル冒頭に置く。**使ったモデル名は出所の一部なので必ず書く。**

```
- 作成日：2026-09-03
- 使用したLLM：（例）ChatGPT / GPT-5.2、Gemini 3 Pro
- Web検索：使った／使っていない
- 主題：何を調べたか（1行）
- publicに出す想定か：public／private（video-projects側）／判断つかない
- リポジトリの現物を見せたか：見せた（どのファイル）／見せていない
```

最後の行が重要。見せていない場合、「既存を覆す」という判定はできていないので、
受け取り側が自分で衝突を確認する必要がある。

**「publicに出す想定か」は、差分の書き手にしか判断できない。** 受け取り側は案件の中身を
知らないので、案件固有の固有名詞や筋書きが混ざっていても気づけないことがある。
実際、この欄が無かった時期の差分では毎回ここだけが漏れた。判断がつかなければ
「判断つかない」と書く。空欄にしない。

**「〜は除いてある」と列挙形で書かない。列挙から漏れたものが素通りする。**
実際に「クライアント名・書名・著者名・キャラクター名は除いてある」と書かれた差分の
**プロンプト例のコードブロックに、原作の台詞が原文で入っていた。台詞が列挙に無かった。**
しかも**同じ差分のもう1本のプロンプト例は伏せてあった。片方だけ抜けるのが典型。**

代わりに、**どこを見たか**を書く。

- [ ] 地の文
- [ ] **コードブロック／プロンプト例／YAML の中**
- [ ] 引用と実例
- [ ] ファイル名・見出し

なおヘッダのうち**作成日・使用したLLM・Web検索・主題は調査側が埋める**が、
**publicに出す想定かとリポジトリの現物を見せたかは、案件の中身を知っている側が埋める**。
そのため後述の「他のLLMに渡す指示文」にはこの2つを含めていない。

## 項目の書き方

本体は項目の連なり。1項目＝リポジトリの1行〜1節に対応する粒度。

各項目に次の6つを書く。**空欄を作らない。分からなければ「不明」と書く。**

### 1. 主張

1〜3行。**断定形を避ける。** 覆る前提で書く。
「必ず〜すべき」「これが正解」ではなく、条件と帰結の形にする。

### 2. 出所

次の4分類のいずれか。分類名を明示したうえで、具体を書く。

| 分類 | 何を添えるか |
|---|---|
| 自社検証 | いつ／どのモデルで／何回試したか |
| 他人の公開プロンプト | corpus名（原文は貼らない） |
| 外部情報 | URL／発行日／書いたのは誰か（企業の公式・実務記事・個人ブログ・研究）／**そのURLを開いて確認したか、検索結果の要約から書いたか** |
| 推測 | 「検証していない」と明記 |

**出所を書けない主張は、書かない。** 調べて分からなかったなら「分からなかった」と
そう書く。埋めるために推測を混ぜない。

**URLを貼ることと、開いて確認することは別。** 項目ごとに区別して書く。

- **一次で確認**：そのURLを開き、**題名・著者・主張が一致することを見た**
- **要約から**：検索結果やスニペットから書いた。**題名も著者も未確認**

実際に、URLを貼った引用で**題名が実物と違っていた**ことがある。
別の引用では、書き手を「現役予告編編集者」としていたが、**開くとゲーム予告編の編集者**だった。
**書き手が自覚しているときは「原著未確認」と自己申告できていて機能した。
自覚していないときだけ素通りする。** だから欄で強制する。

### 3. 検証状態

未検証／自社で1回確認／複数案件で再現、のいずれか。

**同じ生成・同じ実験から複数の項目を書くなら、そう書く。** 別々の項目に見えると、
**独立した複数件の裏づけがあるように読まれる。**

実際に、**同じ3回の生成**から「最終フレームへジャンプする（版面差が原因）」
「長い禁止の列が効いていない（プロンプトの長さが原因）」「制御を足すほど悪くなる」の
3項目が、**3本の差分に別名で分散した。** 独立3件ではなく、**同じ観測に対する競合仮説**だった。

- 「この項目の観測は、〈他の項目名〉と同じ生成／同じ実験」
- **競合する仮説なら、そう書く。片方を根拠に片方を補強しない**

### 4. 適用範囲

- 全ジャンル共通
- ジャンル固有（どのジャンルか）
- プラットフォーム固有（**どのモデル名か、確認日はいつか**）

**プラットフォーム固有のものを普遍則として書かない。** 過去に「ショット生成は4秒以上」を
Hailuo H3の制約と気づかないまま普遍則として記録し、全カットの設計を縛った。
モデル依存かどうかが分からないときは「不明」と書く。断定より有用。

### 5. 提案する配置先

ファイルパスと、どの見出しの下か。**判断がつかなければ「不明」と書く。勝手に決めない。**

- `principles/` — モデル非依存の原則
- `platforms/` — プラットフォーム別の仕様（確認日が要る）
- `templates/` — 埋めて使う型
- `genres/` — ジャンル固有（実体はprivate側）
- `OPERATIONS.md` — 制作事業の運用
- ジャンル固有の内容を `principles/` に置かない

**現物を見せている場合は、`skill/SKILL.md` のどの工程から辿り着けるかも書く。**
辿り着けないなら「ルーティングの追加が要る」と書く。
**本文にファイルや節を足しても、`build-skill.sh` の参照切れ検査には映らない**——
あれは「名指しされたファイルが存在するか」しか見ない。
**「参照されるべきものが参照されていない」状態は構造上見えない。**

### 6. 既存項目との関係

新規／追記／**既存を覆す**、のいずれか。

覆す場合は、**何を覆すのか・なぜか**を書く。これがCHANGELOGの材料になる。
理由が書かれていない「覆す」提案は反映しない。

**現物を見せている場合は、覆される文言を grep して、何ファイル何箇所かを書く。**
同じ主張は複数のファイルに書いてある。**検索語は手元にある——いま覆そうとしている文**がそれ。

実際に、覆す対象として `platforms/model-quirks.md` だけが挙がっていたが、
**同じ記述が `principles/still-to-motion.md` にもあった。**
別の件では、原則ファイルを覆したのに**テンプレート側が古いまま残った。**

**書き手のほうが安い。** clone している側は grep できる。
受け取り側は「そこにも書いてある」ことを知らない。

---

## 数字を扱うときの追加規則

外部調査は数字を持ち帰ってくる。ここが一番汚染されやすい。

- **誰が測ったかを必ず書く。** 「業界平均」「一般に」は出所ではない
- **サンプル数と条件を書く。** 1件の制作記録なら「1件」と書く。複数事例の平均なら何件か
- **数字を丸めない、盛らない、範囲を代表値にしない。** 出典が幅で書いていれば幅のまま
- **自社に当てはまるかの判断は書かない。** それは受け取り側の仕事。調査側は数字と条件だけ渡す
- 複数の出典が食い違ったら、**片方に寄せずに両方書く**。食い違っていること自体が情報

## 書いてはいけないもの

このリポジトリは**public**。以下が混ざると取り返しがつかない。

- **第三者が書いたプロンプトの原文。** 作法の抽出と要約だけを書く
- **1文以上の長さの原文引用**（英語含む）。短い断片にとどめる
- クライアント名、担当者名、未発売作品の筋書き、案件固有の固有名詞
- 素材そのもの（画像・動画・PDF）。リンクだけ
- リポジトリの既存内容についての推測。現物を見ていないなら参照しない

---

## 記入例

```markdown
### 縦型の参照画像の向き

**主張**：9:16で生成するとき、参照画像も縦で渡すほうが構図が安定する傾向がある。
横向きの参照だと被写体の配置が横の習慣を引きずることがある。

**出所**：外部情報 — https://example.com/vertical-test（2026-08-14、
個人の検証記事。撮影スタジオ勤務と自称、所属の裏は取れていない）

**検証状態**：未検証（自社では試していない）

**適用範囲**：不明。記事はSeedanceでの検証。他モデルで同じかは書かれていない

**提案する配置先**：principles/vertical.md の「処方」節

**既存項目との関係**：追記。既存の「9:16と書くだけでは構図の癖は直らない」と
方向は一致していて、衝突しない
```

---

## 他のLLMに渡す指示文

以下をそのまま貼る。**リポジトリの現物を見せない前提**で書いてある。
見せる場合は、下の「現物を見せた場合の追加」も一緒に貼る。

見せる範囲は、`principles/` と `platforms/` が目安。この2つが覆る対象のほとんどで、
かつpublicなので外に出しても失うものがない。`templates/` は自社資産。

**ただし実運用では public 全体を clone させている。** そのほうが
「既存を覆す」判定と配置先の精度が上がり、**受け取り側の確認が減る。**
`templates/` まで見せる不利益より、衝突を見落とす不利益のほうが大きいと判断した
（2026-08-31。**この判断は未検証**）。

```
あなたが調べた結果を、私のナレッジリポジトリに取り込める「差分ファイル」の形で
出力してください。リポジトリはAI映像制作の知見を溜めたもので、publicです。

## 出力形式

冒頭にヘッダを置いてください。

- 作成日
- 使用したLLM（あなたのモデル名）
- Web検索：使った／使っていない
- 主題（1行）

そのあと、項目ごとに次の6つを必ず埋めてください。空欄を作らず、
分からないものは「不明」と書いてください。

1. 主張 — 1〜3行。断定形を避け、条件と帰結の形で書く
2. 出所 — 次の4分類のいずれかを明示：
   - 自社検証（いつ／どのモデル／何回）
   - 他人の公開プロンプト（名前のみ、原文は貼らない）
   - 外部情報（URL／発行日／書いたのは誰か。企業公式か、実務記事か、
     個人ブログか、研究か）／**そのURLを開いて題名・著者・主張を確認したか、
     検索結果の要約から書いたか。この区別を項目ごとに書く**
   - 推測（検証していないと明記）
3. 検証状態 — 未検証／1回確認／複数回再現。
   **同じ生成・同じ実験から複数の項目を書くなら、どの項目と同じかを書く。**
   別々に見えると独立した複数件の裏づけと読まれる。
   **競合する仮説なら、そう書く**
4. 適用範囲 — 全ジャンル共通／ジャンル固有／プラットフォーム固有
   （固有ならモデル名と確認日）
5. 提案する配置先 — 分からなければ「不明」
6. 既存項目との関係 — 新規／追記／既存を覆す（覆すなら何をなぜ）

## 守ってほしいこと

- 出所を書けない主張は書かないでください。調べて分からなかったなら
  「分からなかった」と書いてください。埋めるために推測を混ぜないでください
- 数字には誰が測ったか、サンプル数、条件を必ず添えてください。
  「業界平均」「一般に」は出所として認めません。1件の事例なら1件と書いてください
- 数字を丸めたり、幅を代表値にしたりしないでください
- 複数の出典が食い違ったら、片方に寄せず両方書いてください
- 「これが正解」「必ず〜すべき」と書かないでください。覆る前提で書いてください
- 他人が書いたプロンプトの原文を貼らないでください。作法の抽出だけにしてください
- 1文以上の長さの原文引用をしないでください（英語含む）
- プラットフォーム固有の制約を、普遍的な法則のように書かないでください。
  モデル依存かどうか分からなければ「不明」と書いてください
- 私のリポジトリの中身は見せていません。既存内容を推測で参照しないでください
- **コードブロック、プロンプト例、YAML の中も見てください。**
  地の文から固有名詞を消しても、実例の中に台詞や作品名が残ることがあります。
  「〜は除いてある」と列挙形で書かず、**どこを見たかを書いてください**
- **あなたが他のLLMの回答や要約を出発点にしている場合、そう書いてください。**
  出典が付いていても、**実在と記述の一致は別問題**です。
  一次情報に当たれたものと当たれなかったものを分けてください。
  **当たれなかった数値は書かないでください**
- **2本以上を同時に出すときは、先頭に索引を1枚付けてください。**
  どのファイルを何本が触るか／「既存を覆す」項目の一覧／新設を提案するファイルの一覧
  （**同じ新設ファイルへ別々の欄を提案していないか**）／推奨する反映順序と理由
```

### 現物を見せた場合の追加

**リポジトリを clone させた場合は、上に続けてこれも貼る。**

```
リポジトリの現物を渡しました。見た場合は次も守ってください。

- 監査したコミットのハッシュを書いてください
- **「既存を覆す」項目があるなら、覆される文言を grep して、
  何ファイル何箇所に同じ主張が書かれているかを列挙してください。**
  検索語は手元にあります——いま覆そうとしている文がそれです。
  原則ファイルだけ挙げてテンプレートを見落とす、が典型です
- **本文に足す内容へ、skill/SKILL.md のどの工程から辿り着けるかを書いてください。**
  辿り着けないなら「ルーティングの追加が要る」と書いてください。
  ビルドの参照切れ検査は「名指しされたファイルが存在するか」しか見ないので、
  この向きの抜けは機械では見つかりません
- **配置先は実在するパスで書いてください。** 新設を提案するなら「新規」と明示してください
```

---

## 受け取り側のチェック

反映する前に見る。

- [ ] 全項目に出所があるか。無ければ書き込まず、確認する
- [ ] ヘッダの「publicに出す想定か」が埋まっているか。**private なら video-projects 側へ回す**
- [ ] publicに出せない情報が混ざっていないか。**本文だけでなく、CHANGELOG に書く理由の説明文も見る**
- [ ] **コードブロック・プロンプト例・YAML の中も見たか。** 地の文が綺麗でも実例に残る。
      **ヘッダの「〜は除いてある」という列挙を信用しない。列挙から漏れたものが素通りする**
- [ ] **同じ生成・同じ実験から出た項目が、別々の裏づけとして並んでいないか。**
      束で受け取ったときは特に。**独立した複数件に見えて、実は同じ観測への競合仮説**のことがある
- [ ] 他人のプロンプト原文が混ざっていないか
- [ ] **`.md` を経由したテキストに、エスケープ記号・強調記号が混入していないか。**
      受け渡しの過程で付くことがある（`\~` `\*\*` や `**@Image 1**` のような二重化）。同じ原文の版が複数あるなら加工の少ない側を採る。
      **入ってくる原文（corpus 行き）だけでなく、こちらから生成モデルへ出ていくプロンプトでも起きる**（→ `OPERATIONS.md`）
- [ ] 普遍則として書かれているものが、実はプラットフォーム固有ではないか
- [ ] ジャンル固有の内容が `principles/` 行きになっていないか
- [ ] **外部調査由来のツール候補が入っていないか。** 入っている場合、**こちらの工程上の必要から探したものか**を確認する。検索結果に並んだだけのものを候補として記録しない
- [ ] 「覆す」提案に理由が書かれているか。→ CHANGELOGへ
- [ ] 数字に「誰が測ったか」が付いているか。付いていなければ落とす
- [ ] **他のLLMの調査結果が混ざっていないか。** 出典が付いていても、**実在と記述の一致は別問題**。
      一次情報に当たれたものと当たれなかったものを分ける。**当たれなかった数値は書かない**
- [ ] **本文に足した内容へ、`skill/SKILL.md` のどの工程から辿り着けるか。**
      辿り着けないならルーティングを直すか、直さない理由を書く。
      `build-skill.sh` の参照切れ検査では**この向きの抜けは見えない**
- [ ] **「既存を覆す」項目があるなら、覆される文言を grep したか。**
      同じ主張が別のファイルにも書いてある。**原則を直して、テンプレートを直し忘れる**のが典型
- [ ] **差分の「提案する配置先」を全部消し込んだか。** 触らなかった場所には理由を書く
- [ ] 外部情報が主体の差分なら、反映先の見出しに「自社未検証」と明示したか
