# 運用メモ

制作の外側で決めたこと。忘れると同じ失敗をする。

## 知見の維持

- **月に一度、棚卸しをする。** 放っておくと検証は発生しない工程なので、予定として置く。principles/ の各項目を見て、次の2点を洗い出す
  - **根拠が弱いもの** — 出所が「推測」のまま検証されていない項目、1つの corpus だけを根拠にしている項目
  - **古くなったもの** — platforms/ の確認日が古い項目、モデルの世代交代で前提が変わった項目
- 棚卸しのときにモデルの新バージョンも確認し、platforms/ の確認日を更新する
- 落とした項目・覆した項目は CHANGELOG に理由とともに残す
- **差分を反映してコミットしたら `./build-skill.sh` を走らせて上げ直す。**
  これが抜けるとリポジトリだけ進んで Skill が置いていかれる。
  配布版（zip / ChatGPT）はコピーなので、ビルドしない限り古いまま
- 検証されていない項目が混ざっていること自体は許容する。**出所が各行に書いてあれば、使うときに割り引いて読める**
- 制作の中で「効かなかった指定」に気づいたら、作業文書に1行残す。案件終了時にCHANGELOGへ拾い上げる

## リポジトリを private にするか

**現在は public。月次の棚卸しで見直す。**

判断の目安は3つ。**どれか1つでも当てはまったら private を検討する。**

- **自社検証の分量が外部情報に近づいたとき。** 現在は `platforms/model-quirks.md` が36行、
  外部情報（`models/` 配下）が1034行。ただし**自社検証は model-quirks.md だけではない。**
  `principles/` にも「（自社検証）」タグの項目が散在しており、`shot-structure.md` だけで12件、
  リポジトリ全体で22件ある。**数えるときは行数ではなくタグで数える。**
  **他人の調査で辿り着けるものが大半のうちは、公開しても失うものが少ない。**
  検証項目が埋まり、工数の実測が3案件たまると、そこが逆転する
- **クライアントから問われたとき。** 中身に作品情報は入っていないが、
  「制作の内部資料を公開しているのか」と聞かれたら説明のコストが発生する。
  一度でも問われたら、その時点で private にする理由になる
- **事業の資産として扱い始めたとき。** これはリポジトリの判断ではなく事業の判断

### private にする前に用意すること

**`git clone` が認証なしで通らなくなる。** 別のチャットや別のLLMからリポジトリを直接
読む経路が消えるので、**代替の受け渡し手段を先に決める。**

- 認証つきの clone がその環境で通るかを先に試す
- 通らないなら、ファイルを都度貼るか、Claude Code 側で完結させるかを決める。
  **後者にする場合、「差分を外で書いて中で反映する」という現在の分業が変わる**

`build-skill.sh` はローカルで動くので影響を受けない。
ただし `models/README.md` と `CLAUDE.md` の「public なので」という記述は直す。

## 保留中の検討事項

- **知見のSkill化**（2026-08-26に検討、保留中）
  工程ごとに book-analysis / teaser-structure / video-prompt の3つに切り、
  必要なときだけ読み込ませる構想。Higgsfield公式が Claude Skill を配布しているのが
  先例（内容は生成API操作。Seedanceのプロンプト作法は公式ブログ記事で、
  Skill形式のものは有志製。2026-08確認）。
  分け方の基準は**知識の分類ではなく、いつ使うか**。
  Skillは手順とチェックリスト、リポジトリは根拠と出所、と役割を分ける。
  二重管理になるので、Skillには「詳細はリポジトリの該当ファイル」と書く。
  → 差分3の反映後に判断する

- **30秒ティザーのフォーマット確定**（未着手）
  取引先2社が同じ価格帯を選んでいるため、形として固める価値がある。
  ただし価格に対する利益率は工数だけで決まるので、**フォーマットを固める本当の目的は
  工数の上限を定義すること**。現時点で工数データがないため、作業文書の工数欄が
  3案件たまるまで判断できない。
  固めるべきは4つ：骨格（ジャンル別）／必須要素／納品仕様／工数の上限

## リスク管理

- **単一モデル・単一プラットフォームへの依存が最大の事故要因。** 納品当日にサーバー障害で制作が止まった実績がある。本番の生成手段は常に2系統持つ
- 生成プラットフォームの規約より、**クライアント契約側の第三者ツール制限のほうが上位**。案件ごとに確認する
- クライアント案件は必ずプライベート領域に置く。コミュニティへの公開・コンテスト応募はしない

## クライアント原稿・第三者著作物の入力

出版社等から預かった原稿（漫画ページ、コマ、ネーム、校了PDF）は、
**AI生成用の一般的な参照素材とは分けて扱う。** 第三者の著作物であり、
未公開であり、こちらに所有権がない。

**以下は公開されているプライバシーポリシー・料金ページ・ヘルプの読み取りであって、
法的な助言ではない。** 各社の条件は変わる。案件ごとに確認する
（確認日は `platforms/` 各ファイル）。

### 入れる前に4つ確認する

順番に効く。上で止まったら下は見ない。

1. **契約で外部AI処理が許可されているか。** クライアント契約・発注条件・素材利用条件を見る。
   禁止されているなら、プラットフォーム側が学習除外でも入れない
2. **原稿画像の学習除外が確認できるか**
3. **第三者プロバイダによる処理・国外移転が案件条件に適合するか。**
   学習に使わないことと、外部事業者が一切処理しないことは**別の条件**
4. **プロンプトと出力まで契約上の保護が必要か。** 必要ならエンタープライズ契約を検討する

**プラットフォームの規約より、クライアント契約側の制限のほうが上位**（既出）。
原稿を扱う案件では特に効く。

### 運用

- **入力先を増やさない。** 承認済みの1つで前処理から生成まで進める。
  外部へ移す必要が出たら、その時点で条件を確認し直す。**処理先を増やすほど、
  契約確認・アクセス管理・削除管理の対象が増える**
- **加工しても素材の性質は変わらない。** コマの切り出し、文字消し、余白拡張、
  clean plate、動画化した中間素材は、いずれも原著作物を基礎にしている。
  「原稿そのものではない」を理由に自由に移動できる扱いにしない
- **プロンプトに固有名詞を持ち込まない。** 作品名・担当者名・未公開のあらすじ・
  不要な原作台詞を書かず、ショットに必要な行動・カメラ・音だけにする。
  必要ならアセットIDへ置き換える。**画像の保護条件とテキストの保護条件は
  同じとは限らない**（→ platforms/magnific.md）
- **クライアント素材をコミュニティ・公開フィード・コンテストへ出さない**（既出）
- **入力経路を記録する。** 元アセット → プラットフォーム → 使用モデル → 主要な中間生成物
  → 最終採用クリップ。作業文書の工数欄と同じ場所に残す。プラットフォーム側に
  系譜を出力する機能があればそれを使う（→ platforms/magnific.md）

### 暫定の判断表（2026-08-28時点。各社の条件は変わる）

| 条件 | 暫定ルール |
|---|---|
| 契約が外部AI処理を禁止 | 入れない。個別許可か契約変更が先 |
| AI処理は許可、学習利用を避けたい | Magnific を第一候補。ポリシーの条項を案件条件と照合する |
| Higgsfield Team / Scale | 原稿の直接入力をデフォルトで行わない |
| Higgsfield Enterprise | 契約・保持期間・再委託先を確認してから候補に戻す |
| Krea 通常プラン | 同等の保証を確認できるまでデフォルトで行わない |
| Krea Enterprise | 同上、確認してから候補に戻す |
| 入力も出力も契約上の学習除外が必要 | エンタープライズ契約を検討する |
| 別プラットフォームへ移したい | 派生素材でも同じ確認を通す |
| 公開フィード・コンテスト | 出さない |

### 未確認

- 各社のエンタープライズ契約書（MSA）を確認していない。上の表は公開資料の読み取り
- 法務レビューを受けていない
- Krea の通常プランについては「学習される」とも「されない」とも言えない。
  公開資料で確認できなかっただけ

## 生成プロンプトと素材の受け渡し

### `.md` で渡すとアンダースコアがエスケープされる

生成プロンプトを `.md` ファイルで受け渡すと、経路のどこかでMarkdownとして処理され、
**フィールド名のアンダースコアの前にバックスラッシュが挿入されることがある。**
`integrated_multimodal_description` が `integrated\_multimodal\_description` になる。
**日本語環境ではバックスラッシュが円記号として描画されるため、原因が分かりにくい。**

- 生成元のファイルには挿入されていなかった（`grep` とバイト列で確認）。**受け渡しの過程で付く**
- 回避は `.txt` にするか、**表計算のセルに入れて渡す。** セルはMarkdown処理を通らない
- （2026-08-28 / 自社検証。**どの経路で挿入されるかは特定できていない**）

**同じ現象が corpus への取り込みでも起きる**（→ `DIFF-FORMAT.md` の受け取り側チェック）。
**入ってくる原文と、出ていくプロンプトの両方が対象。**

### zipの日本語ファイル名はWindowsで構造ごと壊れる

Linuxの `zip` はファイル名をUTF-8で書くが**UTF-8フラグを立てない**ため、Windowsの標準の
展開機能がCP932として解釈して文字化けする。単に読めなくなるだけでなく、**化けた結果が
使えない文字を含むとフォルダとして成立せず、中身がファイル名の一部に潰れて構造が壊れる**
（2026-08-28 / 自社検証。日本語名のzipをWindowsで展開し、4フォルダのうち1つが0KBのファイルと
化け、その中身がファイル名にプレフィックスとして結合された状態を確認）。

- **素材一式を納品するなら、フォルダ名とファイル名は半角英数にする**
- 日本語の対応表は同梱するREADMEに書く
- **READMEはBOM付きUTF-8にする。** Windowsのメモ帳で開いても化けない

### 素材を依頼する段階で、帯なしの版があるかを訊く

書籍の帯には**数値を伴う体験談や断定的な効果の表現**が入ることが多い。表現ルールでそれらを
「使用しない素材」としている案件では、**高解像度の書影を入手した時点で、低解像度では
読めなかった文言が読めるようになり、ルール違反が可視化される**（2026-08 / 自社検証・1案件。
初稿で使われた低解像度の書影の帯部分を拡大し、該当する文言が含まれていることを確認）。

- **書影データを依頼する段階で、帯なしの版があるかを併せて確認する。**
  出版社は通常、帯あり・帯なしの両方を持っている
- 一般化すると、**表現ルールの運用は本編の文言だけでなく、素材そのものに含まれる文言にも及ぶ**
  （音についても同じ。→ `principles/sound.md`）

### 画質の判断は、指標を作る前に比較画像を作る

ディスクリーンの強度選定で、数値指標を組もうとして2回失敗した。**局所標準偏差は線画と
トーンを分離できず、勾配ベースのエッジ指標は1bit入力に対して意味のある値を返さなかった。**
比較画像を並べて目視した時点で即座に判断がついた（2026-08 / 自社検証）。

**ただしこれは「どちらが良く見えるか」を決める判断に限る。同じ案件で逆の事例が出ている。**
「処理が実際に適用されたか」の事実確認は目視では決まらず、数値でしか決まらなかった
（σ1.2 のプリブラーが納品素材に入っているかの検証。中間値の画素数と標準偏差が、独立に
再現した値と完全一致したことで確定した。**しかも最初に選んだ指標——FFTのピーク/中央値比——は
現象に対して不適切で、誤った結論を出していた**）。

| 何を決めるか | 手段 |
|---|---|
| どちらが良く見えるか | **比較画像を並べて目視。** 指標を組む前にこれをやる |
| 処理が適用されたか | **数値。** ただし、その指標が現象に対して妥当かを先に確かめる |

## 制作プラットフォームの選択

「本番の生成手段は常に2系統持つ」としているが、2系統目は未定
（→ platforms/magnific.md）。**決め方を先に定義しておく。**

### モデル数で比べない

3社とも同じ基礎モデルを載せている。**差が出るのはモデルではなく、その上の層。**

| | 主に見るところ |
|---|---|
| Magnific（現行） | 複数モデルのワークフロー、アップスケール、編集、系譜の記録 |
| Higgsfield | 撮影設計の上位層（Cinema Studio / Shots / Popcorn） |
| Krea | Nodes / Apps / LoRA による工程の再利用と自動化 |

**2026-08-28時点の仮説で、実案件で確かめていない。**

同じ基礎モデル名でも、ホスト側の参照上限・UI・失敗時の返金・並列数・生成設定が違う。
**モデル名だけで品質も原価も決まらない**（→ platforms/higgsfield.md「ホストによって違う」）。

### 比較は同一ショットで測る

単発の画質を見比べても、制作基盤としての差は出ない。**同じ素材・同じ完成条件で、
次を記録する。**

- 完成までの生成回数
- 所要時間
- 採用率
- **プラットフォーム間で素材を移した回数**
- 生成コスト

4つ目が効く。外部へ移すたびに、契約確認・アクセス管理・削除管理の対象が増える
（→ 上の「クライアント原稿・第三者著作物の入力」）。**工程が閉じているかどうかは、
品質とは別の価値がある。**

記録先は案件の作業文書の工数欄。既存の生成回数・所要時間・採用テイクに、
移動回数を足す形でよい。

### クレジット単価では比べられない

3社のクレジット単位に互換性がない。モデルごとの消費量も違う。
**同一ショットの実測コストでしか比べられない。**

### 2系統目の決め方

Krea と Higgsfield は強みが違うので、**用途別に比較してから決める。**
Krea はワークフローとアセットのパイプライン、Higgsfield は撮影設計。

実案件か、同一のテストショットで測る。**現時点では候補を確定しない。**

### 未確認

- 3社とも自社で契約・実測していない
- Higgsfield Enterprise の最低契約額は非公開
- Krea の通常プランのデータ利用条件
- Magnific の Spaces を実案件で使った実績なし

## 制作の相場と歩留まり（他社の実績値・2026-08収集／自社未検証）

**以下はすべて他社の公開された制作記録からの数字。自分たちでは測っていない。**
工数記録が3案件たまった時点で、自社の値と突き合わせること。

### 歩留まり

ある3分アニメの制作記録（2人チーム）：Seedance 2.0で164クリップ生成し、41本が採用。
**約25%。** 15秒生成のうち実際に使えた部分は平均5秒。

- **平均3生成で1カット**が採用ライン
- 最終カットの17本は複数生成のつぎはぎ（「フランケンシュタインショット」）
- 品質基準で歩留まりが変わる：SNS向け60〜85%／プロの物語作品40〜60%／
  放送・CM品質4〜5%

### 見積もりの掛け算

- 総生成数 ＝ 目標ショット数 × **約4**
- 使えるショットを得るための生成 ＝ 目標ショット数 × **約3**
- 最終カットの**約40%はつぎはぎ**になると想定する

15カットなら総生成60回前後。**捨てる分は無駄ではなく計画された経費。**

### 上流固定の効果

同じ制作記録で、キャラ1体につき約5生成（約$9.78）をかけて外見を確定させてから
ショット生成に入っている。

**「シートと環境参照を先に固定することが、1ショットあたりの生成回数を3回に保つ。
途中で一貫性が崩れると8〜10回に膨れ上がる」。**

→ 工数記録の生成回数が1カットあたり8回を超えていたら、上流のアセット固定が
足りていないと診断できる

### 完成尺あたりのコスト

4つの制作記録から**1分あたり$315〜$750**（捨てた75%の生成も含む）。
30秒なら$160〜$375、およそ2.5万〜5.5万円相当。

**ただしこれは映画的な品質を狙った制作の数字で、クレジット単価もプラットフォームに
よって大きく違う。自社に当てはまるかは測るまで分からない。**

### 人間側の時間

- プロンプトの執筆と反復：1〜4時間／本
- レビューと選定：30〜60分／本
- **編集は含まれていない**

### 価格帯の位置

参考値：テンプレート化された15秒のSNS広告が$300前後から、
30〜60秒のオーダーメイドの宣材が$2,000〜$8,000。

**現行の売価はテンプレート化された短尺の価格帯にある。** オーダーメイドで作れば赤字。
テンプレートで回して初めて成立する。逆に言えば、テンプレート化が進めば利益が出る構造。

## クライアント承認の設計（業界標準・2026-08収集／自社未検証）

### 業界標準

承認は3〜4回、各段階1〜2ラウンド。契約に含める修正回数は**2〜3ラウンドが標準**。

| 段階 | 何を承認するか | 修正ラウンド |
|---|---|---|
| 脚本／構成 | 文言と構成 | 2 |
| 絵コンテ＋スタイルフレーム | 構図・配色・キャラデザ・場所の見た目 | 1〜2 |
| ラフカット | **構造に関する指摘のみ。** 音は仮、色補正なし | 1 |
| ファイナルカット | **見た目の細部のみ。** 綴り、位置、わずかな尺調整 | 1 |

**段階ごとに「何を言う場か」を決めているのが要点。** 絵コンテを急いで承認して、後の
段階で構図の懸念を出すと手戻りになる。

### 数字

- 構造化されたレビュー工程がない場合、**平均5.7ラウンド**。フィードバックが
  メール・Slack・口頭・PDF・音声メモの4系統以上から同時に来る
- 1ラウンドあたり**3〜7日**追加され、そのどれも作品を良くしない
- 承認のボトルネックは全体の工期を**25〜40%**延ばす
- 「前工程に20%多く時間をかけると、制作と後工程で50%の時間が節約できる」

推奨：**承認者を1名指名、並行レビュー、48〜72時間の期限、タイムスタンプ付きの指摘を
1箇所に集約、2ラウンドで上限。**

### 現行の進め方との差分

| 項目 | 業界標準 | 現行 |
|---|---|---|
| 構成の承認 | 承認を求める文書として出し、以降の変更は修正ラウンドに数える | 選択肢の提示にとどまり、合意の性質を持たせていない |
| 絵コンテ／スタイルフレーム | キャラ・配色・場所の見た目を初稿の前に承認 | **工程が存在しない** |
| 指摘の粒度 | 段階ごとに「何を言う場か」を明示 | 明示していない。初稿で構成・文字サイズ・考証が同時に来た |
| 修正回数 | 契約に明記、2〜3ラウンド | 決めていない |
| 指摘の窓口 | 1箇所に集約と明示 | 成り行きで決まっている |
| 承認者 | 1名指名 | 指名していない |

### 30秒案件で採る案（未検証）

承認を3回に絞る。

1. **構成の承認** — カット表とセリフ。ここで確定
2. **スタイルフレームの承認** — キャラクターシートと場所のアセット数枚
3. **初稿の承認** — 修正2ラウンドまで

**2番はアセットを先に作る方針の副産物として、追加工数ゼロで出せる。**
各段階で「今回はここまでの指摘をお願いします」と粒度を明示する。

取引先から「どの粒度で修正希望を伝えるのが適切か分からない」という声が実際に出ている。
**これはこちらが目安を示していないことが原因。**

### 承認された資料が、その段階の要件を満たしているとは限らない（自社検証）

**上の表と手順は業界標準で自社未検証。ここだけは自社で起きたこと。**

クライアント承認を得た資料が「カット表」として扱われていても、**中身がシーン候補の一覧で
あり、採用する構成パターンも秒割りも確定していない**場合がある。資料自体に「候補として
ご提示します」「制作中に変動します」と書かれていても、**進行の過程で確定したものとして
扱われてしまう**（2026-08 / 自社検証。1案件で発生）。

- **作業に入る前に、資料の中身が確定事項なのか候補なのかを確認する**
- 確定していない場合、**作業を止めるのではなく「確定していない軸のすべてに耐える形で作る」
  という選択肢がある。** 実際にそうした案件では構成パターンが未確定だったため、
  全候補を仕分けの対象にした

## 構成を確定する前に数える

**カット数・想定秒数の合計・画面に出る文字の総量を、構成を承認に出す前に出す。**
数えずに進めると、超過に気づかないまま素材づくりとプロンプト作成まで進む。

実際に、20カット構成で**合計30.3〜34.8秒**（下限を全部採っても30秒を超える）という状態のまま、
素材の作成とプロンプトの作成まで進んでいた（2026-08 / 自社検証・1案件）。

**多く入れて物語を作るのは易しい。少ないカットで成立させるのが設計。**

上の承認設計では「構成の承認 — カット表とセリフ。ここで確定」としているが、
**確定の要件に尺の合計が入っていなかった。** 入れる。
文字量の数え方は `principles/still-to-motion.md`「素材に焼き込まれた文字は、読字予算になる」。

### 画面に出る文字だけを通しで読む

素材に文字が入っている案件では、**画面に出る文字だけを並べて通しで読む**検証を
カット確定前に行う。**絵の繋がりだけを見ていると、文字側の破綻を見落とす。**

実施して見つかった破綻（2026-08 / 自社検証・1案件）。

- 同じ意味の台詞が連続する（冒頭で疲労の描写が3回）
- 同じ台詞が2度出る
- 指示語の指す対象が、前のカットに存在しない
- 問いと答えの順序が逆になる
- 誰の発話か分からない台詞がある

**いずれも絵を見ているだけでは気づかなかった。**

**この検証は記憶で行わない。** 1回目は素材を見ずに書いたため、実際には含まれていない台詞を
含めてしまい、判断を誤らせた。**実物の画像を並べて読む。**

### 構成パターンを選んだら、そのパターンに属さないシーンは残さない

複数の構成パターンを候補として持つ場合、1つを選んだ後も**別のパターンに属するシーンを
惜しんで残してしまう**ことがある。結果として構成の芯がぼやける。

実際に、一人の変化を追う構成を選びながら、**別パターンに属する波及のシーンを複数残していた。**
全部落として一人の変化に揃えたほうが、選んだ構成に忠実になった（2026-08 / 自社検証・1案件）。

## 案件の選別

- 判断軸は「漫画かどうか」ではなく **編集工数がどれだけ乗るか**。編集比率が上がる案件はAIによる効率化が効かず、構造的に合わない
- ただし現時点で工程別の時間データがない。**作業文書の工数欄（生成回数・所要時間・採用テイク）を必ず埋める。** 3案件たまれば、切るべき案件の条件が数字で言える

## クライアントとのやりとり

- 探索していること自体は隠さない。問題は「約束と成果物がずれて見えること」であって、手探りであることではない
- 蓄積が見える会社のほうが強く見える。過去の検証結果を根拠として示せる状態を作る
- 構成候補群は「合意文書」ではなく「方向性のたたき台」として位置づける。確定は試し撮りの後

## このリポジトリ

リポジトリの方針（public/private の判断、置いてはいけないもの、書き方のルール）は
`CLAUDE.md` にある。ここには書かない。
