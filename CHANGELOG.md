# CHANGELOG

覆った知見はここに理由とともに残す。git logは変更を記録するが、**なぜ覆ったか**は残らない。

## 2026-09-02（起動はスキルの外にあった）

**スキルは自分自身を起動できない。**
SKILL.md をどれだけ整えても、それが読まれるかは**スキルの外**で決まる。
これまでの改善を全部**中身の層**に入れてきたが、効かなかった原因の一部は**起動の層**だった。

**ChatGPT のメモリに「AI動画制作の議題のときは video-knowledge を読む」と入れた途端、
読む習慣がついた。中身は何も変えていない。**（ユーザー実測）

**効いた理由は、引き金が「話題」で書かれていたこと**

| 書き方 | 発火するか |
|---|---|
| 「必要に応じてスキルを使う」 | **しない。**「必要か」は答えを組み立てたあとの判断で、間に合わない |
| 「AI動画制作の話題のときは開く」 | **する。** 話題は最初の1行で分かる |

**配置の穴が1つ見つかった**

`video-knowledge/CLAUDE.md` は**そのディレクトリで作業しているときしか読まれない。**
**制作は video-projects で起きる。** 知見が要る瞬間にちょうど読まれない配置だった。

- `~/.claude/CLAUDE.md`（**全ディレクトリで読まれる**）を新設 — 本命
- `video-projects/CLAUDE.md` を新設 — 「ここは作る場所で、知っている場所ではない」

どちらもリポジトリ外なので、**`ONBOARDING.md` に「0. 最初にやる設定（5分）」として
貼る文面ごと書いた。** 新しく入る人は自分の環境に同じものを置く。

**メモリ層に知見そのものを書かない**

**入口だけ書く。** 写した知見はリポジトリが更新されても古いまま残り、
**新しい記述を古いメモリが上書きする。**
リポジトリはビルドと検査（孤児・壊れ参照・READMEの網羅）を通るが、
**メモリには何の検査もかからない。検査の外に内容を置かない。**

`OPERATIONS.md`「知見の維持」に節を追加。

**`CHATGPT.md` を作った（106行）**

ChatGPT はこのワークフローで**2役**やっていて、**危険が違う**のに1つに混ざっていた。

| 役 | 主な危険 |
|---|---|
| **使う側** | 知見を開かずに一般論で答える |
| **書く側** | **引用が実物と違う** |

**書く側の危険は実測がある。** 受け取った差分で3件ずれた。

- **Oja (2019)** — 題名が実物と違った（年と誌名は合っていた）
- **Derek Lieu** — **ゲーム**予告編の編集者なのに「現役予告編編集者」、**三幕ではなく4区分**、
  「title 後の button」は**その記事に出てこない**
- **Dávila (2010)** — 群の人数まで一致していたのに、**分母が49→実際47前後**

**題名・年・誌名が合っていても中身がずれる。URL が貼ってあることは開いた証拠にならない。**

Claude 側との構造の差も書いた（**フラット／検索で引く／「読まない」が効かない**）。
**工程1を ChatGPT でやらない理由がこれ。** 禁止側の制御が効かないので型に寄る。

`README.md` の入口表と一覧、`ONBOARDING.md`「0.」、`DIFF-FORMAT.md` の末尾から参照。

**ついでに1件**

`OPERATIONS.md` の「クライアント承認の設計」見出しが**2行重複**していた。1つに直した。

## 2026-09-02（保留を解く基準を作り、3本を解放した）

**「検証が済むまで待つ」だけでは、効く知見が埋もれる。**

**基準を非対称性で書いた**

| | 誤っていたときの害が**小さい** | 害が**大きい** |
|---|---|---|
| **効果が大きい** | **解放する。未検証と明記して、埋まらない欄は消せるようにする** | 待つ |
| 効果が小さい | 待つ | 待つ |

**害が小さい**＝欄が余るだけ／読む対象が1つ増えるだけ／後から消せる。
**害が大きい**＝生成の内容が変わる／数値が引用されうる／版で腐って古い情報を信じる／
**埋まらない量で「埋めない習慣」を作る。**

**見送りの本来の理由は「使われたときに検証済みに見える」だった。**
**そこを潰せば理由が消える**ので、解放する型には
「**未検証。埋まらない欄は消してよい**」を冒頭に書くことにした。

**解放した3本**

| 作ったもの | 効果 | 誤っていたときの害 |
|---|---|---|
| `templates/trailer-brief.md`（64行） | **案件の最初に1枚。** 訴求・支配軸のずれ・開示・媒体条件・具体化の範囲・**配置の方針** | 欄が余るだけ |
| `templates/source-breakdown.md`（53行） | 原作を選択可能な単位へ。**「原作を引いたものは通り、引かなかったものは全部却下」は自社検証済み**なので、分解の価値は実証されている | 欄が余るだけ |
| `templates/style-reference-card.md`（63行） | 権利台帳。**許諾の根拠が空の素材を本番へ流さない** | **ゼロ。抜けているほうが危険** |

**どれも20欄前後に絞った。** 見送りの原因だった120フィールドの失敗を繰り返さない。

**`trailer-brief.md` に「配置の方針」を入れた。** 前段で見つけた
`off-centre` と「中央寄り」の衝突は、**案件ヘッダで先に決めるのが解**なので、
その欄をここに作った。

**2本は「不要」に確定した**

- `cut-pair` / `cut-interface` → **`templates/shot-ir.md` の「繋ぎ」6欄に入った**
- `non-plot-shots` → **`principles/trailer-structure.md` に節がある**

**索引が15本 → 10本になった。**

**待ち続けるものと、その理由を明示した**

`style-specification`(120欄) は**埋まらない量**、`style-compiler` は**版で腐る**
——**この2つは害が大きい側**なので、効果があっても待つ。
残り8本は案件条件か実験待ち。

**索引の抜けを1件埋めた**

**「他社の書籍ティザーを20〜30本見て分類する」が索引に無かった。**
数日前に「誰も思いついていなかった穴」として自分で挙げたのに、**保留索引へ足していなかった。**
corpus にあるのは他人のプロンプト9本で、**他人の完成した予告編は0本。**
Grøn の100本の類型を借りておいて**自分では1本も見ていない。**

**判断待ちを1件解放した**

**公開判断の4つ目の基準「商売としての露出」を足した。** 基準を足すだけなら害が無い。
既存の3基準は**知見としての価値しか見ていない**が、
**知見ではないものが public に増えている**——相場と歩留まり、案件の選別、
クライアントとのやりとり、`CHANGELOG.md` の自己批判。
**NDA の問題ではなく商談の問題**なので、既存の3つでは検出できない。
**公開範囲そのものの判断は未決のまま**（事業の判断）。

**検査が2つとも発火した**

新規3本を作った時点で、**孤児ファイル検査とディレクトリREADME検査が両方鳴った。**
ルーティング（案件条件×2、工程2×1）と README を足して解消。**機械が捕まえた。**

## 2026-09-02（第3段・工程間の「橋」3本と、既存の矛盾2件）

同じ公開プロジェクトを別のLLMに分析させた結果を受領。**私の第2段が個別の技に寄っていて、
工程間の中間表現を取り落としていた。**

> 最大の資産は「優れたプロンプト文」ではない。
> **物語意図→演技、文章→空間、生成→編集の三つの橋**である。

**既存の矛盾を1件見つけてもらった**

**「被写体は中央に置く」（`vertical.md`）と「off-centre, never centred」（`directing.md`）が
正面衝突していた。** `house-style-live-action.md` と `templates/shot-schema.md` にも
`off-centre` が入っており、**4箇所。しかも shot-schema はテンプレなので、貼ると必ず入る。**

**縦型の実写ティザーでは4つが同時に効く。** 直近の案件がまさにそれだった。

**解を書いた**——**off-centre を左右ではなく上下でやる。** 横は中央寄り、
**縦の三分割でずらす。** どちらを優先するかは案件ヘッダで決める。
`shot-schema.md` のテンプレ行からは `off-centre` を外した。

**私自身の過剰一般化を1件直した**

第2段で「**覆う要素が画面の9割以上を満たすフレームで切る**」と書いたが、
**9割は一作品の値。** 数値を規則にしない。
**採るのは抽象のほう——編集ハンドルを、偶然余ったフレームに頼らず生成前に設計する。**

**橋1：シーンの事件から人物ごとの行動へ降ろす（`principles/directing.md`）**

**このファイルには目線・表情・二人芝居・台詞の演技があったが、
「このシーンで何が起きているか」から「この人物に何をさせるか」へ降ろす層が無かった。**

```
原作 → シーンの事件 → 人物ごとの動機/目的/障害/戦術 → 一つの物理行為
→ 目と身体の仕事 → カメラ・光・音 → プロンプト
```

**カメラ・グレード・音はこの読みに従う。先導しない。**

**ここが危ない**と書いた。撮影言語やルックの知識が増えるほど、**そちらからシーンを
逆算する**方向に寄る。**既存の失敗（破綻対策から逆算）と同じ形で、
逆算の対象が「破綻」から「ルックの知識」に変わっただけ。**

**橋2：配置図という中間表現（`principles/shot-structure.md`）**

**文章のブロッキングと完成ファーストフレームの間が空いていた。**

| 手段 | 固定するもの | 残る自由度 |
|---|---|---|
| 文章だけ | 言葉の配置 | 構図は自由。**配置の精度は低い** |
| **配置図** | **位置・姿勢・向きだけ** | 見た目とカメラは自由 |
| 完成ファーストフレーム | 画素配置まで | **最も低い** |

**実案件で人物が塊化し、椅子が湧き、モブの造形が崩れたカットは、ここに該当する。**

構造だけを8点書いた（正面図・クロップは補完しない・輪郭で塗らない・図に文字を入れない・
色は画像側のID／文字はプロンプト側のID・支配範囲を肯定形で閉じる・最後に添付・
直すときは元フレームから作り直す）。**方法の考案者に権利があるので本文は写していない。**

**`world-assets.md` の「空間の見取り図」とは役割が違う**——
あちらは**世界の永続的な地理を文字で**、こちらは**一ショットの配置を画像で。**

**橋3：繋ぎの素材を生成段階で作る（`templates/shot-ir.md`）**

**「繋ぎ」6欄を追加。** 受ける／渡す／運動の方向／覆う要素／音の橋／切る条件。
**編集で余ったフレームを探すのではなく、生成の前に埋める。**

**そのほか4件**

- **座標系を4つに分ける**（画面／カメラ相対／人物の解剖／物体固有）。
  第2段の「幾何はフレームに縛る」を精密化。**混同の既定値は「画面」**
- **参照どうしの優先順位。** 支配範囲を `only` で閉じても、
  **2つの参照が同じ属性を主張したらどちらが勝つかは決まらない**
- **カバレッジを表で見る。「毎ショット必ず変える」を規則にしない**——
  意図的な反復と無意識の反復が区別できなくなる。第2段の書き方を差し替えた
- **プレートへ焼く判断基準**——**全編を通して変わらない属性**を最も強い画像参照へ。
  **迷ったら「これは全カットで同じか」を訊く**

**衝突を1件、明示して統合した**

外部の事例には「**全アセットがロックされるまで1カットも生成しない**」という規律があるが、
**我々の試し撮り工程と衝突する。** `lock-blocks.md` に統合形を書いた——
**最小限の仮アセットで3〜5カット試験 → 成立確認 → 本生成の前に全アセットをロック。**

**保留索引へ2件**

- **配置の3手段の比較実験**（文章のみ／配置図／完成フレーム。3人以上の画で各3回）。
  **見るのは配置精度だけでなく、顔の同一性・衣装への色移り・線画の混入・
  カメラ自由度・生成回数**
- **演技の完全版スキルが添付されていない。** 核は公開本文から抽出したが、照合したい

## 2026-09-02（第2段・外部プロジェクトから9件の構造）

第1段の残り。**全件が外部情報で自社未検証。本文は1行も写していない。**
原文は `video-corpus/research/the-trigger-2026-09/`。

**一番大きいのはこれ**

`principles/image-production.md`「**難しいものは静止画で勝つ。動画側で戦わない**」

| 難物 | 動画側で書くと | プレート側で解くと |
|---|---|---|
| レンズの性格 | ショットごとに光学が漂流する | **プレートに焼き込む。プレートがレンズになる** |
| 夜の暗さ | 「とても暗い」と書いても**夕暮れが返る** | **プレートを2〜3段落とす。動画の露出は最初のフレームの露出に従う。形容詞ではない** |
| 画面内の印刷文字 | 毎フレーム描き直される | プレート側で確定させる |

**帰結が2つ。**

- **プレート側で解いたものは、動画プロンプトに一語も書かない。禁止としてすら書かない**
- **夜は構図でも解ける。空がフレームに入ると必ず blue hour へ寄る。**
  向かいの壁で端まで埋めれば、画面で一番明るいものが実光源になる

**直近の案件で部屋が白飛びしたのは、暗さをプロンプトで1回書いただけだったから**
（→ `platforms/model-quirks.md`）。**書く回数の問題ではなく、書く場所の問題だった。**

**残り8件**

- `principles/shot-structure.md` 4件——**幾何はフレームに縛る**（「銃の左側」が frame-left と
  読まれた）／**スケールは手がかりで数値ではない**（**ただし物の寸法は数値。分かれ目は
  モデルが基準を持っているか**）／**物理はシーケンスに勝つ**（死重として倒れる：頭から、
  肩が非対称に、膝が内へ）／サイズと角度を毎ショット変える
- `principles/directing.md` 2件——**死んだ目は照明でもキャッチライトでも直らない。
  目に仕事を与えると直る**／**1つのビートに反応は1つ**（反応→目をそらす→また反応が既定値で、
  それが瞬間を殺す）
- `principles/guards.md`——**数えられる出来事には証拠を書く**（空になった発射管が
  見えていること。でないと4本の管から9発撃つ）
- `principles/trailer-structure.md`——**繋ぎ目は設計する。** のりしろを付けて生成し、
  **覆う要素が画面の9割以上を満たすフレームで切る。抜けた後では切らない**
- `principles/workflow.md`——**編集の段9を5つに分解。**
  とくに**03「再生成の集中パス」を独立させる**のが要点。
  再生成が散発的に発生すると、そのたびに編集がやり直しになる

**繋がりを2件直した**

- **`guards.md` に「否定の中に語を入れること自体が召喚」が無かった。**
  `image-production.md` と `templates/lock-blocks.md` の両方がそこを参照していたのに、
  **参照先に本体が無い状態だった。** 既存の「禁則は肯定形に言い換える」の下へ足した——
  **あれは言い方の問題ではなく、語を出さないという話**
- **「サイズと角度の反復」が2箇所に独立して立った。**
  `trailer-structure.md` の集合検査と相互参照で繋いだ。**あちらは選ぶとき、こちらは書くとき**

## 2026-09-02（「読むもの」と「貼るもの」を分ける）

**「毎回読ませる」構造そのものへの対処。** 公開されたAI短編プロジェクトの制作解説から
**構造を抽出**した（原文は `video-corpus/research/the-trigger-2026-09/`。
**方法の考案者に権利があるものを含むので、本文は写していない**）。

**問題の立て方が変わった**

前日に「3行ヘッダで読ませる」を提案したが、**あれは弱いほうの半分だった。**

**知識には2種類ある。**

| | 使うときに何をするか | 飛ばせるか |
|---|---|---|
| **読んで判断する** | 読んで、理解して、当てはめる | **飛ばせる。適用が毎回の判断だから** |
| **そのまま貼る** | 貼る | **飛ばすと出力が不完全になる** |

**`principles/` の7,396行はほぼ全部が前者。後者は `house-style-live-action.md` の1本だけだった。**

**そして実案件で一発で通ったものは、ほぼ全部が後者だった。**
物アセット（寸法をmmで）／テロップ2枚／書影カード／空白の禁止列挙3箇所。
**反復したものは全部、判断が要るものだった。**

`反省.md` は一発群の共通点を「根拠が原作か実測／対象が単純／空白の型を使った」と
分析していたが、**もう一段下に共通項がある——貼れる形になっていたか。**

**入れたもの**

- `templates/lock-blocks.md`（161行）——**一度書いて以後そのまま貼るブロックの型。**
  型1 ハウススタイル／型2 声のロック／型3 参照のコネクタ／型4 空白を作らせる／型5 禁止。
  加えて**共通の規律4つ**と**命名規約**
- `templates/README.md` を**使い方の3種別**（貼る／埋める／引く）へ再構成。
  **いま7本が同じ棚に並んでいて、貼るものと埋めるものが混ざっていた**
- `house-style-live-action.md` に「**これは貼るものの原型**」を明示
- Skill の案件条件に「**ロックブロックを先に作ったか**」、4D の冒頭に「**まず貼る**」

**これは新設テンプレートの見送り15本と矛盾しない**

見送った15本は**まだやっていないことのスキーマ**だった。今回のは
**既に書いて既に効いたブロックを型として抜き出したもの。**
`OPERATIONS.md` の着手条件「**1案件で手書きして、実際に埋まった欄だけを昇格させる**」を
満たしている。**満たしていない候補3つは「まだ型になっていないもの」として分離した。**

**外部から構造だけ取ったもの（自社未検証）**

| 取ったもの | 内容 |
|---|---|
| **命名規約** | **1要素1名前。新しい状態は新しいアセットで、上書きしない。** 同じ人物の別状態、同じ場所の別の面は別アセット |
| **参照の添付順** | 写真参照を先、図式を最後。**添付の順序が画風の votes を決める** |
| **否定に語を入れること自体が召喚** | 「旗を出すな」で旗が戻った実例。**無地の布と書くほうが消える** |
| 版で管理する | 作り直したら版を上げ、そのショットでは有効な版だけを参照する |

**まだ入れていない構造（第2段）**

**難しいものは静止画で解く**（光学・暗さ・印刷文字はプレート側で勝つ）／
**幾何はフレームに縛る**／**目にタスクを与える**／1ビート1リアクション／
数えられる出来事には証拠／スケールは手がかりでメートルではない／
物理はシーケンスに勝つ／トランジションは設計する（のりしろ8フレーム、90%で切る）／
**編集に「再生成の集中パス」を独立した段として置く**。

**12件のうち4件を第1段で入れ、8件を第2段へ回した。**

## 2026-09-02（実案件からの差分・約45項目を18ファイルへ）

**リポジトリが受け取った初めての、実案件の自社検証。**
ティザーの修正フェーズ（約10時間半、受領45件、納品済み）。

**これまでの差分はほぼ全部が外部調査だった。** 今回は n=1 だが**自社の実測**で、
出所の格付けが違う。

**案件固有の情報は伏せた。** クライアント名・作品名・登場人物名・地名・
台詞の文言・凶器のモデル名。**スキャン0件。** 原文は `video-corpus/diffs/` と
`video-projects/sessions/` にある。

**未確認が3件解けた**

| 項目 | 従来 | 今回 |
|---|---|---|
| GPT Image 2 が Magnific で使えるか | **未確認** | **使える。安い。編集器として強い**（3回以上） |
| Soul Cinema の参照 | 「プロンプトが使えないという記述があるが確認できなかった」 | **プロンプトは使える。制限されるのは参照の対象（キャラクターのみ）** |
| Soul ID の学習枚数 | 公式は「5枚以上」とも読める | **3枚では成立しなかった** |

**新設**

- `platforms/higgsfield.md`「**Cinematic Locations**」——項目が無かった。
  **実写のシネマティックな環境アセットでは GPT Image 2 と同列に比較できない。**
  同一プロンプトの3モデル比較で、**GPT Image 2 は「記録写真」、Soul 系は「映画のワンカット」**
- `platforms/magnific.md`「Seed Audio 1.0 のUIパラメータ」——採用値の実例つき
- `OPERATIONS.md`「**誰が何を決めるか**」——判断の切り分け表

**制作フローを改訂した（保留が1件解けた）**

`OPERATIONS.md` の「案件が来たら着手するもの」にあった**「制作フローの順序変更」の
着手条件が「現行フローで1案件通してから」**だった。**通した。**

**10回の手戻りを実測**——読んでいれば防げたもの6件、前提を確かめていれば防げたもの2件、
順番が逆だったもの2件。その逆算で `principles/workflow.md` の制作フローを
**5段から10段へ改訂**した。**手戻りから足したのは3つ**——音声を先に実測、
最初から縦で評価、モデルの文法と版を確認。

**歩留まりの初めての自社実測**

| 一発で通った | 3回以上 |
|---|---|
| 物アセット／テロップ2枚／書影カード／場所1件／音声／新規キャラのシート | 確定済みキャラのシート4／場所5・5・3／動画3 |

| 一発群の共通点 | 反復群の共通点 |
|---|---|
| **根拠が原作か実測にある** | **人物の同一性が絡む** |
| **対象が単純** | **複数人が動く** |
| **空白を作らせる型を使った** | **既存物を修正する／縦型の評価が後回し** |

**他社の「上流固定が崩れると8〜10回」と整合した。ただし n=1 なので自社の値としては使わない。**

**主な追記（18ファイル）**

- `principles/shot-structure.md`——**カメラは速度と終端状態の両方**（片方だと極端に振れる）／
  **「1ショットに動き1つ」は画面全体で1つではない**／**被写体にタスクを与える**
- `principles/directing.md`——**予告編で顔を隠すのは原則として損**（6カット中6カットで隠していた）／
  **抑制は強度の上限ではなく演じ方の質**（同じ節に `no mannequin stillness` がある）／
  肌の指定を全キャラに使い回すと人物差が消える
- `principles/image-repair.md`——**壊れた箇所の数だけ直す**（1箇所を4箇所同時に変えて退行）／
  **PRESERVE は要素ごとに名指し**（彩度・コントラスト・カメラ角度・消失点・グレインまで）
- `principles/typography.md`——**空白の禁止列挙が3箇所とも一発**／
  **文字の後出しは静止画2枚を編集で切り替える**（揃い版を先に作る）
- `principles/cross-media-style-translation.md`——**原作を引いたものは通り、引かなかったものは全部却下**
- `principles/world-assets.md`——**実在の土地は地名を書く。地名は IP ではない**／
  **縦型は最初から縦で評価**（9:16 は中央31.6%）
- `principles/guards.md`——**動くものを1つに限定すると暴走する**（花が花吹雪に）
- `principles/sound.md`——**「小声＝ゆっくり」は外れた**（1.2xが採用）／**発話行為で書く**
- `templates/character-sheet.md`——**通った要素7つ**／
  **確定済みキャラを型に合わせて作り直すのはリスク**（顔が3回別人化）
- `platforms/model-quirks.md`——局所編集3回で壊れる／**同一プロンプトでも同じ空間は出ない**／
  「廃」の語だけで荒廃が湧く／**遠近の逆転**
- `OPERATIONS.md`——**検品は指定していない領域も見る**（大きさ・数・主役以外の造形）／
  **縮小画像で細部を判定しない**（4回誤読した）／クライアント対応5件

**いちばん実害があった項目**

> **送信文に、決まっていない行動を書かない。**
> 「修正版のカット表と尺の調整案を先にお送りします」という**会話で決めていない約束**が
> 含まれ、そのまま送信され、**クライアントはそれを待っていた。**

**保留索引を更新**

- 実験に「**参照ありの再生成 vs 編集**」を追加。**前回の比較は設計ミスで測れていない**
  ——参照なしの再生成に「保持」を要求した。**両方の条件に存在する軸だけで測る**
- 一次情報待ちに4件（C2PA の納品先確認、Magnific 側の Seedance の版、
  テロップの適正サイズ、「小声＝ゆっくり」の他台詞での傾向）

**新設テンプレートは提案されなかった**

差分自身が「**埋めた欄がまだ1案件ぶんしかない**」として提案していない。
`OPERATIONS.md` の着手条件と整合している。

## 2026-08-31（参照画像のアスペクト比・「必要ない」より強い結果が出た）

**問い：参照で渡すアセットは、出力動画の規格と一致させる必要があるか。**
**リポジトリには記述が0件だった。** 公式仕様を一次で確認した。

**MiniMax H3（一次確認）**

| | 枚数 | 寸法 | アスペクト比 | 出力の比率 |
|---|---|---|---|---|
| **参照画像（Ref2VA）** | ≤9（混在で計12） | 256〜5760 px | **制約の記載なし** | **`ratio` で別に指定できる** |
| **first / last frame** | 0 / 1 / 2 | 256〜5760 px | **2:5 〜 5:2** | **入力画像が決める** |

**決定的なのはこれ。**

> for image-to-video mode, **the aspect ratio is determined by the input image
> and ratio is always "adaptive"**

**I2VA では出力比率を選べない。** テキストのみのモードでは `ratio` が必須で
`adaptive` にできないのに、**画像を渡した瞬間に `adaptive` へ固定される。**

**「一致させる必要はない」より強い。同じ1枚でも、渡し方でアスペクト比の自由度が変わる。**

**前日の議論と接続した**

「シネマティックさを含めると 9:16 用でも 16:9 のほうが生成しやすい」という観察に対し、
**クロップの前にモードで決まる**ことが分かった。

- **16:9 のアセットを縦の動画に使うなら、参照として渡す**
- **first frame として渡すと 16:9 の動画になる。9:16 は選べない**

**矛盾を1件直した**

`templates/shot-ir.md` の「UI・APIで固定する値 [尺・解像度・**アスペクト比**・カメラ設定]」。
**I2V ではアスペクト比は固定する値ではない。入力画像から決まる値。**
「アスペクト比」を外し、モード依存であることを別に書いた。

**入れたもの**

- `models/video/minimax-h3.md`「画像入力の要件」（一次確認・確認日つき）
- `principles/prompt-compilation.md` のモード選択表に**「出力の比率」列**を追加。
  判定手順4段の後に、アスペクト比の自由度もここで決まると明記
- `templates/shot-ir.md`——上記の修正と、**アセットを作る前にどちらで渡すか決める**

**限定して書いたこと**

- **参照画像にアスペクト比の制約が「書かれていない」ことは、「何でもよい」の証明ではない。**
  記載が無いという事実まで、と本文に書いた
- **Seedance** は公式でアスペクト比を**プロンプト本文で指定する例**（"16:9 landscape"）が
  並ぶだけで、参照素材の形式要件の記載は無い。**書けることが少ないので本文に足していない**
- **Runway は 403 で読めなかった。未確認**

## 2026-08-31（新しく入る人向けの文書を作る）

`ONBOARDING.md`（220行）を新設。**リポジトリが約9,800行になり、
「どこから読むか」の3行では足りなくなった。**

**6節。**

1. **学習の順序** — 最初の1時間で読むのは**4つ・合計200行くらい**。
   `principles/` 17ファイルと `CHANGELOG` は**読まない**。工程ごとに必要なものだけ。
   **出所の割引だけは先に覚える**——直近に足したファイルほど未検証が多い
2. **制作の流れ** — 5工程と3段階の承認。**工程1で評価しない**理由も
3. **判断の流れ** — 分岐点6つと、それぞれ見る場所。覆すときの手順。数字の読み方
4. **記録の流れ** — 3リポジトリの使い分け、差分が入ってくる経路、
   見送ったときと却下したときの置き場、**上げ直し**
5. **やってはいけないこと** — **全部、過去に一度やっている6件**
6. **詰まったら** — 症状索引、測る手段、保留索引

**5節を「過去に一度やっている」の形にしたのが要点。** 禁止事項の羅列だと読み飛ばされる。
public への原文流出、4秒の普遍則化、未検証テンプレート、覆したのに他ファイルを直さない、
工程1での評価、上げ直し忘れ——**全部このリポジトリの履歴にある。**

`README.md` の「どこから読むか」冒頭と、ルート直下ファイルの一覧に追加した。

**配布物には入らない。** 人がリポジトリを歩くための文書で、Skill には別の入口がある。

## 2026-08-31（Skill の最終点検・参照が22箇所切れていた）

制作へ入る前の点検。**配布物の参照が22箇所壊れていた。**

**見つけたもの**

`skill/SKILL.md` の参照に**2種類の書き方が混在**していた。

```
reference/principles/xxx.md  … 27箇所（正しい）
principles/xxx.md            … 22箇所（prefix が無い）
```

**両方の配布版で壊れる。**

- **zip版**——ファイルは `reference/` 下に置かれるので、`principles/xxx.md` は**存在しないパス**
- **ローカル版**——`reference/` を絶対パスへ置換する仕組みなので、prefix が無いと
  **相対のまま残り、cwd 依存で切れる。** 案件フォルダで起動すると解決しない

**既存の参照切れ検査には映らない。** あれは `[a-z0-9-]+\.md` でファイル名だけを拾い、
`reference/` 配下に存在するかを見る。**パスの形は見ていない。**

**直したもの**

- 22箇所すべてに `reference/` を付けた。**ローカル版47行が絶対パスになり、相対の残りは0**
- `build-skill.sh` に**検査5d**を追加。prefix の無い参照があれば `die` する。
  **わざと1箇所戻して発火を確認した**

**その検査自体が1度落ちた**

最初の実装で `grep` が0件のとき 1 を返し、`set -euo pipefail` でビルドが静かに止まった。
**リポジトリに記録がある失敗そのもの**（マーカー残留検査で同じことをやっている）。
`{ grep ... || true; }` の形へ直した。

**問題なかったもの**

- 配布物に案件固有語は0件。絶対パスもzip/ChatGPT側は0件
- ローカル版だけが private のジャンル4ファイルを絶対パスで指す（**設計どおり**）
- マーカー残留0、孤児ファイル0、ディレクトリREADMEの取りこぼし0
- **ローカル版の `/c/Users/...` 形式が実際に読めることを確認した**
- 工程1の「読まない」に、後から足した評価系ファイル
  （`trailer-structure` / `book-trailer` / `cross-media-style-translation`）が入っている

## 2026-08-31（生成モードの選択判断・6項目すべて反映）

`research-plan-2026-08-31-generation-mode.md` に沿った調査。出所27件、差分6項目。
**計画の制約を全部守っている**——新設テンプレート0件、成果物3ファイル、
Q5に最も厚く配分、飽和で停止。

**Q5が決着した。そして仮説は棄却された。**

**corpus 9本中8本が参照ベースだったのは、収集元の選択バイアス。**
あの9本は Higgsfield の**アセット先行型ケーススタディ**から採ったもので、
人物・場所・小道具を先に作ってから参照付きプロンプトを書く工程を明記している記事群。
**参照付きが多数になるのは制作工程上ほぼ必然。**

**反証が決め手。ByteDance の Seedance 2.0 公式発表にあるラベル付き作例9つは
T2V 5 / I2V 2 / R2V 2。** 公式は R2V を標準にしていない。

| 仮説 | 判定 |
|---|---|
| Ref2V が常に高品質だから8/9になった | **棄却。** 同条件比較なし。公式9例のR2Vは2件 |
| UIが Ref2V を必須にした | **単独原因として棄却。** 画像入力は optional、T2V例もある |
| **アセット先行のケーススタディを選んだ** | **主因として採用** |
| 界隈の慣習 | **保留。** 一次発言も利用率統計も特定できず |

**「UIの都合を法則として記録する」という、依頼書で一番警戒した失敗は回避できた。**

**入れたもの（6件）**

- `principles/prompt-compilation.md`「**生成モードは「境界」と「再利用」で選ぶ**」——
  7行の判断表と4段の判定手順。**品質の序列で選ばない。何を時間境界として固定し、
  何を再利用属性として渡すか。** `models/video/wan.md` の排他制約を、
  **モデル固有事実から選択原則へ持ち上げた**
- `skill/SKILL.md` 4B に3行の圧縮表。**「公開プロンプトの多数派で決めない」**を明記
- `models/video/minimax-h3.md`——**`retention_analysis` は保証ではなく宣言。**
  `fully_preserved` は要求のマーカーであって実測値ではない。
  **`<Subject>` と `<Picture>` の使い分け**（抽象化された可視内容か、具体的な構図アンカーか）も
- `models/video/seedance.md` に「**一次で確認できた仕様**」節。
  **2.0 が 9/3/3、2.5 が 30/10/10。** 公式作例が `@Image 1` を絵コンテ、2を人物、
  3を場面、4を小道具に割り当てている——**役割別の入力は公式の想定内**
- `platforms/higgsfield.md`——**開発元の 30/10/10 とホストの「50 references」を別行に。**
  同じ数え方と確認できない。**「白い人形の動きをコピー」は販促表現で、
  外観と動きの分離保証ではない**
- `templates/shot-schema.md` と `principles/shot-structure.md`（3箇所）

**覆したもの**

**`FIRST FRAME (locked)` を「スタート画像が無いときの代替」と書いていたのを取り下げた。**
あれは**望む冒頭構図の言語記述**であって、実画像の first frame 入力と同じ固定ではない。
**時刻0の画素配置そのものが MUST なら I2V / FL2V を選ぶ。**
**Ref2V で `<Picture>` を構図アンカーにしても、出力の第1フレームがその画像になる契約ではない。**

**受け取り側で確認したこと**

- **grep の主張がわずかにずれていた。** `"FIRST FRAME (locked)"` は**2ファイル3箇所**
  （差分は2箇所と記載）。`"FIRST FRAME"` 全体は**2ファイル**で、差分が挙げた
  `templates/action-sequence-schema.md` には**存在しない**。3箇所すべて直した
- `retention_analysis` は**1ファイル2箇所**（差分は1箇所と記載）
- **Seedance 2.5 の 30/10/10 を一次で確認した。** 開発元公式に
  "up to 30 images, 10 video clips, and 10 audio clips" とある。参照対象も
  composition / scenes / styles / characters / props / clay render / motion で一致
- **`platforms/higgsfield.md` には既に 30/10/10 が DomoAI 由来として載っていた。**
  今回の確認で**出所が第三者から開発元公式へ上がった**

**残った問い（実験でしか出ない）**

報告書自身が「**仕様・役割・入力上限・偏りの原因は外部情報の天井にほぼ達した**」と書き、
E1〜E4の実験計画を置いている。**保留索引へ足した。**

- E1 同じ一枚を first frame と reference で渡す（各3回）
- E2 構図アンカーの強さ（I2V / `<Picture>` / `<Subject>` の3条件）
- E3 動きだけの移送（動き参照のみ／人物参照のみ／両方）
- E4 漫画コマの役割分担と混線（1枚→4枚。**入力上限ではなく実務上の安定上限**）

## 2026-08-31（測る手段を残す／動画解析レイヤーは pilot 待ち）

3本受領。**性質が違うので扱いを分けた。**

| 文書 | 性質 | 扱い |
|---|---|---|
| 動画解析の研究報告書（672行、Codex） | 外部調査。RQ1〜12。**全項目が自社未検証** | **反映しない。** 本人が「pilot前に実装するな」と書いている |
| 動画解析の提案書（970行） | 設計依頼。**何を作らないかが中心** | 同上 |
| `diff-2026-08-28-analysis-tooling.md`（Claude） | **DIFF-FORMAT準拠。自社検証6項目。実施済み** | **反映した** |

**監査への反証**

- 「`ffprobe` を実務手法として記載した箇所は無い」→ **`platforms/magnific.md` に1箇所ある。**
  ただし観測の出所としてであって手法としてではないので、**趣旨は正しい**
- 文書数49は正確

**監査より状況が悪かった**

差分が「測ったから書けた」と挙げた5件を照合したところ、**2件がリポジトリに存在しなかった。**

| 測った値 | リポジトリ |
|---|---|
| 初稿の Integrated ラウドネス（−13.9 LUFS） | **0件**（`LUFS` 自体が0件） |
| 素材PDFの1ページだけ解像度が半分（1557×2301） | **0件** |

**手段が残っていないだけでなく、結果も落ちていた。** 差分の主張がそのまま実証された。

**入れたもの**

- `OPERATIONS.md`「生成結果と素材を測る」（約110行）
  - **疑い → 測る手段**の索引9行。`skill/SKILL.md` の症状索引が
    **画の症状 → 読むファイル**なのに対し、これは**疑い → 測る手段**。並列
  - **時系列グリッド（毎秒3〜4枚）が壊れ方の言語化に最も効いた。**
    キーフレーム3回失敗の「埋め方は毎回違うが最後が跳ぶのは共通」は、
    グリッドで見たから分かった。**目視だけなら3回とも「うまくいかない」で終わっていた**
  - 同じ値が複数回出たら経路の仕様を疑う／素材は全ページ走査する
  - **LLMに動画レビューを頼むときの規則**
- `principles/still-to-motion.md` の「素材の物理仕様を先に測る」に**全ページ走査**を追記
- `skill/SKILL.md` の症状索引から測定索引への経路

**LLMの動画入力について、書いておくべき事実があった**

> **Claude に動画を渡しても、animated image は最初の1フレームしか使われない**
> （Anthropic公式のvision入力仕様。JPEG / PNG / GIF / WebP）

**Gemini は default 1 FPS でサンプリングされ、速い動きや急なカット変化を見落としうる**
と公式が明記している。**「LLMが動画を見た」という記録を証拠にしない**と書いた。
渡すもの（ffprobe出力、検出ログ、ショット境界、timestamp付きコンタクトシート、
異常候補周辺の高密度フレーム）と、報告に必須の7項目（観察範囲・観察していない範囲を含む）も。

**分けた判断**

**A層（ffprobe / black / freeze / silence / loudness / contact sheet）を
「動画解析レイヤーの導入」として扱わない。** あれは**納品前の当たり前の検査**で、
pilotの承認を待つ性質ではない。一括で扱うと**A層まで承認待ちで止まる**——
実際いま、初稿のラウドネスを測ったのに記録が残っていなかった。

**層別の判断規則・回帰判定・知見状態への接続が「レイヤー」で、そこはpilotが要る。**

**見送りに同意した項目**

- **独立した第6工程にしない。** 症状索引・工程5・Golden Shot から辿る横断層
- **`templates/video-analysis-report.md` を作らない。** 作れば**16本目の見送り破り**
- **合成スコアを作らない。** 既存の「自動指標だけで採否を決めない」と同じ
- **C層（FVD / VBench / 物理VLM）除外。** corpus の評価章に既に整理があり、
  そこでも単独採用不可としている

**確認した事実**

- **A層はすべてローカルで完結する**（ffmpeg 8.1 / ffprobe / python + numpy, PIL, PyMuPDF）。
  報告書の停止条件「private素材を外部serviceへ送る必要があり、許可がない」は**A層では発生しない**
- **`cv2` が現環境に無い。** B層の tracking / optical flow には追加インストールが要る

**停止点**

**pilot素材が未指定。** B層とレイヤー設計はここで止まる。

## 2026-08-31（見送ったものの索引を作る）

**原本は捨てていなかったが、索引が無かった。**

- 原本：`video-corpus/diffs/` に**71本**、`video-corpus/research/` に**調査原本6本**。
  clean・push済み。**保管は問題ない**
- 見送りの記録：**`CHANGELOG.md` の26箇所に散在。1200行超のどこかにある状態**
- 既存の索引：`OPERATIONS.md`「保留中の検討事項」が**2項目のまま更新されていなかった**
  （うち「知見のSkill化」は**とっくに完了している**）。
  `CLAUDE.md`「一度検討して却下したもの」は1項目

**26箇所に散っているものは、無いのとほぼ同じ。**

**`OPERATIONS.md`「保留中のもの」として索引を作った。**

| 区分 | 件数 | 何を待つか |
|---|---|---|
| 新設を見送ったファイル | **15本** | 1案件で手書きして、埋まった欄だけ昇格 |
| 案件が来たら着手 | 5件 | 会話劇／音響の距離・主観／小説原作／現行フロー1案件／工数3案件 |
| 一次情報待ち | 5件 | Higgsfield公式／字幕可読性／Grønの残り2類型／Magnificの2K／原典3冊 |
| 積んである実験 | 8件 | **順序つき。先頭は参照プローブ**——落ちたら他が無意味 |
| 判断待ち | 2件 | 公開範囲、4つ目の基準 |

**却下は `CLAUDE.md` 側に置いたまま。保留と却下を混ぜない。**
却下は「もうやらない」で、保留は「条件が来たらやる」。**索引を分けないと、
却下したものが再提案されるのと、保留したものが忘れられるのが同時に起きる。**

**腐らせないために、取り込み手順に1行足した。**

> **見送ったものは `OPERATIONS.md`「保留中のもの」へ1行足す。** CHANGELOG に書くだけだと
> 埋まって見つからなくなる。**何を待っているかを書く**——案件条件か、一次情報か、実験か

**古い記述を1つ直した。** 「知見のSkill化（保留中）」——`skill/SKILL.md` は451行あり、
zip も ChatGPT 版も配布済み。**保留ではない。**

## 2026-08-31（知見ギャップ調査・22項目中12件を反映）

調査計画1本＋成果物6本＋差分4本＋**束索引1本**（Codex / GPT-5、監査コミット `b9875bb`）。

**フォーマットの改訂が即日効いた。** 前日に足した項目が全部使われていた。

- **束索引**——触るファイル一覧、覆す項目のgrep結果、**新設ファイルの衝突確認**、推奨順序
- **引用ごとに「本文を一次で確認」**。標本数まで書いてある
- **重複宣言**——「項目5・6と同じ外部資料に基づき、独立した別証拠ではない」
- **public確認の4箇所**にチェックが入っている

**当たり直した引用**

| 引用 | 結果 |
|---|---|
| Dávila 2010 | **ほぼ一致。** Group A 16 / B 26 / C 23、片方の作品が23人中22人「読みたい」、もう片方が24人「読みたくない」まで一致。**ただし差分の「49人中24人」は、原文では回答47前後で52%。分母が少しずれている** |
| Grøn 2014 の5類型 | **類型が5つであることと、`animated book cover` / `partial adaptation` / `literary short film` の3つを独立に確認。** 残る2つは本文を読んだという差分の報告のみ |

**覆したもの**

- `principles/book-trailer.md`「5類型については」を**5つの表**に置換。
  **「本文に当たるまで採らない」としていた判断を取り下げた。**
  ただし**確認できた3つとできなかった2つを分けて書いた**
- 同ファイルの Dávila「未確認」を解消し、**「情報量と読書意向は単調な関係にない」**を追加。
  **数字は原文で確認した側を使った**

**新規（`OPERATIONS.md`）**

- **失効管理。** 確認日だけでは足りない。モデル依存の知見に
  **対象／検証／失効トリガー／状態／後継**を持たせる。
  **`platforms/model-quirks.md` は信頼度が一番高いが、失効すると損害も一番大きい**
- **旧知見を消さず、後継で履歴化する。** `CHANGELOG` の「覆った理由を残す」を項目単位へ
- **Golden Shot。** 能力別に固定した試験を持ち、モデル更新の前後で同条件で回す。
  **いま失効すると痛い自社検証5件を名指しした**（H3の読み上げ、FL2VAのジャンプ、
  カメラ移動、Magnificの5.166秒、面積平均での縮小）。**どれも再試験の手順が無い**
- **予告編の成果測定。** 視聴→理解→自分向け判断→好奇心→到達→購入を潰さない。
  **ホールドアウトが無いなら「関連」と書く。「因果」と書かない。**
  そして**測る器が今は存在しない**ことを明記した

**追記（`principles/trailer-structure.md`）**

- **人物の状態は造形とは別に台帳を持つ。** 参照画像が固定するのは顔と衣装であって状態ではない。
  **原作での順番と予告編での提示順を別欄にする**。`directing.md` の Performance Context は
  **1台詞の記述であって、ショットをまたぐ遷移ではない**
- **カット接続を、前カットの終端と次カットの始端の対で書く**（注意の掛かり先／画面位置／
  視線／動き／サイズ／輝度色フォーカス／遮蔽／音の跨ぎ）
- **観客の信念を4つに分ける**（確定した事実／推論／誤認／未解決）。未読者と既読者を別列に
- 非物語カットに**観客の理解が何から何へ更新されるか**を必須化。
  **書けないなら尺稼ぎ。** 画面外の対象をシーンまたぎで引き継ぐ規律も追加

**作らなかったもの（テンプレート5本）**

`narrative-state-ledger` / `cut-interface` / `ambiguity-register` /
`relational-performance` / `golden-shot-suite`。**束索引が「新規候補」としており、
報告書自身が「完全templateは3作品程度での記入負荷と検出効果を見てから確定する」**と
書いている。**これまで9本のテンプレート提案を同じ理由で見送っているので、判断も揃えた。**
欄の中身は各原則の本文へ埋め込んである。

**反映しなかったもの**

- **`relational-acoustic` の4項目**（二人芝居の共有タイムライン、間の長さ、
  固定ミリ秒ではなくcueと許容窓、Acoustic POV の空間/主観の分離）。
  **調査計画自身が問い6・7を「案件が来てから」として着手条件を書いていた**——
  会話劇を含む構成を採用したとき、音響設計に距離や主観を持ち込む案件のとき。
  **どちらもまだ来ていない。** 原文は corpus にある

**計画との差**

- 計画の制約「**成果物は3ファイルまで**」に対し**7ファイル。** ただし増えたのは調査ノートで、
  **差分は4本＋索引に収まっており、分岐は起きていない。** 制約の趣旨は満たしている
- 計画の「**スキーマやテンプレートは REPORT.md の中に置く。別ファイルに切らない**」に対し、
  **差分は5本の新設テンプレートを提案した。** ここは計画から外れている
- 計画の「第1弾は問い1・3・10」に対し、**問い2・6・7・9も含まれた。**
  問い1（生成可能性バイアス）は**差分の項目としては「直接実験前に事実化しない」という
  抑制の形**で入っており、断定を避けたのは適切
- ソース35件（目安50〜80）。**飽和で止めたなら計画どおり**

## 2026-08-31（差分フォーマットに6つ足す／指示文を同期）

**2日で約20本の差分を処理して見えた穴を、`DIFF-FORMAT.md` へ戻した。**
6項目の型そのものは機能した——**「判断つかない」を書ける設計**、**数字の追加規則**、
**「覆す」に理由必須**の3つが特に効いた。穴は6つあり、全部に実例がある。

| # | 穴 | 実例 |
|---|---|---|
| 1 | **引用を「開いたか」が項目単位に無い** | URLを貼った引用で**題名が実物と違った**（Oja 2019）。書き手を「現役予告編編集者」としていたが**開くとゲーム予告編の編集者**だった |
| 2 | **公開判定がヘッダ1行で、本文のどこを見たか残らない** | 「クライアント名・書名・著者名・キャラクター名は除いてある」と書かれた差分の**プロンプト例に原作の台詞が原文で入っていた。台詞が列挙に無かった** |
| 3 | **束で出すときの型が無い** | 5本が同じ4ファイルを取り合い、**順序と欄名衝突を決めるために「反映順序」の差分をもう1本書く必要が出た** |
| 4 | **同一観測の重複を宣言する欄が無い** | **同じ3回の生成**が3本の差分に別名で分散（版面差／プロンプトの長さ／制御しすぎ）。**独立3件に見えた** |
| 5 | **「覆す」で覆される箇所の全数を書かせていない** | `model-quirks.md` だけ挙がり、**同じ記述が `still-to-motion.md` にもあった** |
| 6 | **配布物への影響を書く欄が無い** | `skill-routing` の差分が丸ごとこのために書かれた |

**足したもの**

- 出所欄に**「一次で確認／要約から」の区別**を必須化（1）。
  **書き手が自覚しているときは「原著未確認」と自己申告できていて機能した。
  自覚していないときだけ素通りする**ので、欄で強制する
- 公開判定を**列挙形から「どこを見たか」へ**（2）。地の文／コードブロック・プロンプト例・YAML／
  引用と実例／ファイル名と見出し
- **「2本以上を同時に出すなら、束の索引を付ける」**を新設（3）。
  **新設ファイル名の重複と、同じ新設ファイルへの欄名衝突は、束の側でしか見えない**
- 検証状態に**「同じ生成・同じ実験から書いたならどの項目と同じか」**（4）。
  **競合する仮説ならそう書く。片方を根拠に片方を補強しない**
- 「覆す」に**grep結果（何ファイル何箇所）**（5）。**書き手のほうが安い**——clone している側は
  grep できるが、受け取り側は「そこにも書いてある」ことを知らない
- 配置先に**「SKILL.md のどの工程から辿り着けるか」**（6）

**指示文を同期した（ここが一番効く）**

**受け取り側のチェックは13項目まで育っていたのに、他のLLMへ渡す指示文は初期のままだった。**
毎回こちらで見つけている問題を、書き手側へ伝え返していなかった。

- 指示文の項目2・3にURL確認レベルと重複宣言を追加
- 守ってほしいことに3つ——コードブロックの中も見る／**他のLLMの回答を出発点にしているならそう書く**／束の索引
- **「現物を見せた場合の追加」ブロックを新設。** grep、SKILL.md のルーティング、
  実在するパスで書くこと。**clone させたときにしか要求できない項目を分離した**
- 前書きの「見せる範囲は `principles/` と `platforms/` が目安」に実態を併記。
  **実運用では public 全体を clone させている。** 衝突の見落としのほうが不利益が大きいと
  判断した（**この判断は未検証**）

**受け取り側にも2つ**

- コードブロック・プロンプト例・YAML の中も見る。**ヘッダの列挙を信用しない**
- **同じ生成から出た項目が別々の裏づけとして並んでいないか**

**足さなかったもの**

- **項目ごとの信頼度スコア。** 4分類で足りる。数値化すると比較したくなり、
  比較できないものを比較する
- **反映の優先度。** 受け取り側の判断。既存の「自社に当てはまるかの判断は書かない。
  それは受け取り側の仕事」と同じ理屈

## 2026-08-30（「読む量」と「書く量」を分ける）

**根拠の無い制限を外した。**

`skill/SKILL.md` の 4C は「**このカットに関係するものだけ読む。全部読まない。**」だった。
**この制限に根拠が無い。**

- **プロンプトに書く量**を絞る根拠は強い。`prompt-compilation.md`「予算は文字数ではない」、
  ByteDance の「一定量を超えると追加素材が結果を悪化させる」、
  **自社の観測**（1000語規模で禁止を列挙した3回が破綻し、1〜3文で成立した）
- **モデルが読む量**を絞る根拠は、**工程1の1件を除いて無い。**
  `directing.md` を読んでもその内容がプロンプトに入るわけではない。**間に工程4Dがある**

**実測**（2026-08-30）。

| | 文字数 |
|---|---|
| SKILL.md | 24,633 |
| 工程2で「常に読む」4ファイル | 58,370 |
| 工程4Cの表に出る全10ファイル | **139,956** |
| reference 全部 | 394,911 |

**1工程を全部乗せても6〜14万字。** 30秒ティザー1本の規模に対して問題にならない。
全部乗せ（39万字）だけは1コンテキストに入らないが、**1工程でそれが要る場面はない。**

**直したもの**

- 4C の見出しを「読む知識を**絞る**」→「読む知識を**選ぶ**」。
  「常に／条件つきの区別であって、読む量の上限ではない。**迷ったら読んでよい**」
- 「使い方」の「全部読み込むのは避ける」を、**工程1に限定した理由つき**に書き換え
- 工程1の禁止に「**これは分量の話ではなく挙動の話。だから他の工程には持ち込まない**」を追記
- `principles/prompt-compilation.md`「予算は文字数ではない」に、
  **この予算を読む量へ持ち込むな**という節を追加。**混同の出所がここだった**

**残した例外**

- 工程1の「読まない」は残す。**自社の失敗として記録がある**——
  「着想と制御を同じ一手でやろうとすると、制御が発想を食う」。
  **分量が減っても正しい**

**注意**

- **ChatGPT 版では「読む／読まない」がそもそも効かない。** ナレッジがフラットに置かれ、
  モデルが選ぶのではなく類似で引かれてくる。**絞る設計は最初から半分しか機能していない**
- **読む量の上限そのものは未検証。** 「6〜14万字なら問題ない」は分量の見積もりであって、
  読ませた場合の出力品質を比較したものではない

## 2026-08-30（監査の結果を取り込み手順へ戻す）

上の7件から逆算して、`CLAUDE.md` と `DIFF-FORMAT.md` に2項目ずつ足した。

- **「覆すなら、覆される文言そのものを grep する」。** 7件中4件がこれ。
  **検索語は手元にある——いま消そうとしている文がそれ。** 発火するのは覆す項目だけなので、
  47項目の束でも5件程度にしかならない
- **「差分の『提案する配置先』を消し込みリストとして扱う」。** 1件はこれ。
  `merge-order` が「工程5も同様に条件化」と場所を名指ししていたのに実行しなかった

**残る2件は手順では防げない。** ディレクトリREADMEの取りこぼしは `build-skill.sh` の
検査5cへ渡した。同一ファイル内3行差の自己矛盾は、**編集後にそのファイルを頭から
読み直す**しかない。

**定期的な突き合わせは廃止しない。** 上を全部やっても漏れたものを拾う最後の網であって、
代わりにはならない。ただし**毎回やるには高い**ので、頻度は落とせる。

## 2026-08-30（整合性の監査・7件の矛盾を修正）

**2日で15本の差分を3つのLLMの出力から入れたので、全ファイルの突き合わせをやった。**
39ファイル・6426行。**7件が実際に食い違っていた。**

**直したもの**

| # | 場所 | 何が食い違っていたか |
|---|---|---|
| 1 | `principles/README.md` | **`book-trailer.md` `visual-style.md` `cross-media-style-translation.md` の3ファイルが一覧から落ちていた。** 行数も「14ファイル・約2000行」のままだった（実際は17・約3000） |
| 2 | `skill/SKILL.md` 工程5 | **「30秒を通して切れない音を1本敷いたか」が必須チェックのまま。** `b1beeac` で `sound.md` 側を条件化したのに工程5へ届いていない。**`merge-order` の差分が「工程5も同様に条件化」と名指しで指示していたのに漏らした** |
| 3 | `principles/workflow.md` 編集の5つの破綻 | 同じ行が必須のまま |
| 4 | `principles/workflow.md` 冒頭3秒の設計 | **`trailer-structure.md` が「`workflow.md` と工程2の冒頭チェックを全案件に使わない」と書いているのに、`workflow.md` 側に何も書いていなかった。片側だけの記述** |
| 5 | `templates/shot-schema.md` | **「COPY と TYPOGRAPHY は生成に投げない。日本語は字形の失敗コストが高いので」が残っていた。** `5d5bf0b` で `typography.md` が取り下げた前提。**テンプレートのほうが実務で読まれるので、ここが一番実害が大きい** |
| 6 | `templates/house-style-live-action.md` | **冒頭1行「毎回そのまま貼る固定文」と、3行後の「実写調のカットにだけ貼る」が自己矛盾。** `911a651` で下に条件を足したとき、上を直していなかった |
| 7 | `genres/README.md` | **「house-style-live-action.md の内容は genres/ へ移すこと」と、`visual-style.md` の「12軸を1つ埋めた実例として置く」が衝突。** 移さない側へ判断を揃えた |

**5と6は、同じ修正の取りこぼし。** 原則ファイルと Skill は直したが、
**テンプレート側が両方とも古いまま**だった。

**問題なかったもの**

- **相互のファイル参照は全部実在した**（`CHANGELOG.md` の履歴記述と
  `visual-style.md` の「作らなかったファイル」の列挙を除く）
- `models/README.md` の一覧は実ファイル9件と一致
- **同じ数値が複数箇所に出るケースで食い違いなし**（3113×4601、σ1.2、5〜8秒、下20%）
- 未来日2件は `DIFF-FORMAT.md` のファイル名の**書式例**。正常
- `platforms/model-quirks.md` のズーム余地は、但し書きが隣接していて読み違えない

**再発防止**

- `build-skill.sh` に検査5c（**ディレクトリREADMEの取りこぼし**）を追加。
  `principles/` `templates/` `platforms/` の各 `.md` が、そのディレクトリの
  `README.md` の表に載っているかを見る。**ダミーファイルで発火を確認した**
- **これはフロントマターを却下したときに予告した二重管理そのもの。**
  「判断は SKILL.md に一本化、READMEは一覧だけ」にしたが、
  **その一覧すら手で腐る。** 一覧の完全性だけなら機械で見られるので、そこを機械に渡した
- **中身の食い違い（2〜7）は機械では捕まらない。** 上の7件は全部、
  **1つの修正を複数ファイルへ届けきれなかった**もの。`CLAUDE.md` の取り込み判定に
  「SKILL.md のどの工程から辿れるか」はあるが、**「同じ主張を書いた他のファイルはどこか」がない**

## 2026-08-30（視覚・時間スタイル層／成果物12本・差分2本から3件を反映）

Codex の大規模調査（成果物12本、資料114件）＋差分2本
（`visual-style-layer` 7項目、Claude による `visual-style-review` 7項目）。
**成果物は要約せず `video-corpus/research/visual-style-2026-08-30/` へ保管。**

**7項目すべてが自社未検証**（差分自身が「自社検証はゼロ」と明記）。
`8d431c4` の「他のLLMの調査結果」の等級をそのまま適用した。

**当たり直した引用**

| 引用 | 結果 |
|---|---|
| ARRI Film Lab | **一致。** grain / color / halation / gate weave が**独立した4設定**。grain は 50D / 200T / 500T 相当、halation は明部の赤橙のにじみ、gate weave はフィルム特有の微細な不安定さ |
| Cambridge Companion "Motion and Emotion in Anime" | **一致。** 著者 Sheuo Hui Gan、Jaqueline Berndt 編、2024-11-07。抄録は「**感情を引き出す運動は抽象化・誇張・様式化された形に依存し、表現のしかた自体が注目の中心になる**」。**limited animation を選択として扱う**読みを支持する |

**新規ファイル**

- `principles/visual-style.md`（263行）— **リポジトリに無かった層。**
  8つの層を混ぜないこと、記述する12軸、**時間にも絵柄があること**、実写側の分解、
  媒体モード8つ、原作からの抽出手順、三段階の継承。
  **`genres/README.md` の2軸を置き換えない**——ジャンルは訴求と構成、こちらは知覚実装
- `principles/cross-media-style-translation.md`（136行）— preserve / translate / invent / omit、
  原作タイプ別の翻訳表、筋を進めないカットの10分類、二重の文脈

**この2つで、2日前に「アニメの絵柄について記述が無い」と確認した穴が埋まった。**
`templates/house-style-live-action.md` は**12軸を1つ埋めた実例**として位置づけ直した
（普遍的な分類ではない）。**アニメ側のハウススタイルブロックは依然として無い**ので、
工程4C にその旨を書いた。

**先に入れたもの（差分が「実験を待つ理由がない」としていた領域）**

- `OPERATIONS.md` に「**スタイル参照と学習素材の権利**」。
  参照の6分類（**external inspiration は分析のみ・本番投入しない**）、
  **`permission_basis` が空の画像を本番参照や学習へ使わない**、
  **negative prompt へ作品名を直送しない**、固有名によるスタイル指定の5つの問題、
  リリース前チェック8項目、public / private の切り分け
- **これだけは未検証の設計提案ではなく手順。** 次の案件から効く

**追記**

- `templates/shot-ir.md` に**5行だけ**（プロファイル／変体／例外／例外の理由／戻す条件）。
  既存の「狙い」3欄との役割差も書いた。**全体仕様をショットへ複製しない**

**作らなかったもの（5ファイル）**

- `templates/style-specification.md` / `style-reference-card.md` / `style-test-matrix.md`、
  `principles/style-evaluation.md`、`platforms/style-compiler.md`。
  **理由は `trailer-structure.md` のときと同じ**——スキーマの名称と項目が調査側の提案で
  自社未検証。`templates/` は埋めて使う型を置く場所で、**未検証の様式を置くと
  使われたときに検証済みに見える**
- **Claude の review 差分が「120フィールドは増えている。全フィールドを必須にしない」と
  指摘していた点を、作らないことで回避した。** 1案件で手書きして、
  **埋まった欄だけをテンプレートへ昇格させる**

**受け取り側で強めたこと**

- **媒体翻訳の資料が薄いことを、ファイル冒頭に書いた。** 資料114件のうち
  漫画・適応・媒体文法は**9件だけ**（アニメ制作工程28件、生成AI製品27件に対して）。
  **事業の観点ではここが未映像化書籍の案件で使う部分**なのに裏づけが一番薄い。
  review 差分の指摘どおり
- 翻訳表の個々の対応は**統合推論であって実証されたものではない**と明記した

**ルーティング**

- 案件条件に「**媒体モードとルックが未確定か**」を追加 → `visual-style.md`
- 工程1の「読まない」に `cross-media-style-translation.md` を追加。**評価が入るので Think に持ち込まない**
- 工程2の読む表に `cross-media-style-translation.md`（原作から画を起こす場合）
- 工程3A の冒頭に `visual-style.md`。**時間文法は工程3で決めないと工程4で取り返せない**
- 工程4C の表に**アニメ調の行**。ハウススタイルブロックが無いことを明示
- **孤児検査（`57ad187` で追加）を通した。** 新規2ファイルとも経路がある

**未検証比率について**

review 差分が「未反映の差分が積み上がっており、一度に反映するとリポジトリ全体の
未検証比率が跳ね上がる」と指摘していた。**そのとおりなので、テンプレート5本を作らず、
原則2本と手順1本に絞った。** `EXPERIMENT-PROTOCOL.md` の**実験前の停止条件5つ**
（権利、プラットフォーム条件、同意、原作未公開情報、固定素材と評価者と予算上限）は
corpus 側に保管してある。**生成を始める前にそちらを見る。**

## 2026-08-30（フロントマターの提案を却下・孤児検査を追加）

提案1本（`proposal-frontmatter-for-claude-code`）。**判断を求められたので却下した。
理由を `CLAUDE.md` に残した**——提案者自身が「後で同じ提案が再浮上する」と書いていたため。

**却下の骨子**

- `process` `condition` は**ルーティングの二重管理。** 同じ判断が SKILL.md と37ファイルに載る。
  **同じ結論をこれで3回出している**（ディレクトリREADME／各ファイル冒頭の工程番号／今回）
- `confidence` は**ファイル単位のラベルに意味がない。** 1ファイル内に自社検証と推測が混在する。
  **冒頭に散文で書くほうが条件を書ける**（`still-to-motion.md` `trailer-structure.md` が実例）
- `updated` は **git が持っている**
- 生成方式なら二重管理は消えるが、**正本の SKILL.md からルーティング表が消える。**
  入口を1つ壊して索引を得る取引で、37ファイルでは割に合わない
- 再検討する条件を書いた——**ファイル数が倍（70前後）になったとき**

**代わりに入れたもの**

- `build-skill.sh` に**孤児ファイルの検査**（5b）。既存の参照切れ検査は
  「SKILL.md が名指しするファイルが存在するか」の一方向だけで、
  **本文にファイルを足してルーティングを書き忘れると、参照切れは1件も出ないまま
  誰も辿り着けないファイルができる。** 実際に 2026-08-30 にやった
- `models/` は除外。**意図的に名指ししていない**（工程3C・3Dと4B・4Dで抽象的に指示）
- ダミーファイルで**発火を確認した。** 現状の37ファイルでは0件
- **既存ファイルの中身が増えたときの追随は機械で捕まらない。** そちらは `CLAUDE.md` の
  取り込み判定で人が見る（`8d431c4`）

**退避漏れの回収**

- 反映済みなのに `video-corpus/diffs/` へ入れ忘れていた差分が3本あった
  （`repo-navigation` / `skill-routing` / `README-patch-proposal`）。回収した
- あわせて未反映・未受領だったものも回収。`diff-2026-08-28-trailer-structure.md`
  （Claude作成13項目。**Codex の5本に包含されるので反映しない**）、
  `trailer_scene_selection_and_structure_deep_research_2026-08.md`（**Codex 5本の原文**、1270行）、
  初期の `2026-08-26_diff-2.md` と `diff-file.md`。**diffs/ は63件**
- 案件のカット表1本は `video-projects/deliverables/` へ（**public には置かない**）

## 2026-08-30（反映順序の指示を後から受領・工程2を畳み直した）

差分1本（`merge-order`）。**新しい知見ではなく、上の5本をどう入れるかの指示。
反映後に受け取ったので、突き合わせに使った。**

**指摘のとおりだった1件を直した**

- **工程2が肥大していた。** 「読む」が散文4段落・6ファイル・15行になっていた。
  差分は「読むファイルが6以上、手順が6以上になる」「**`skill/SKILL.md` の設計思想は
  読むものを絞ること**」と警告していた。**工程4C と同じ表の形に畳んだ**
  （常に4／台詞を使う／未映像化書籍）。15行 → 8行
- あわせて**訴求の決定を試し撮りの前へ移した。** **何を試し撮りするかは訴求で決まる**ので、
  後ろに置くと「何を売るか決めないまま3〜5カット撮る」ことになる。
  これは保留した制作フローの順序変更（Brief → breakdown → Think）とは別の、
  工程2の中だけの並べ替え
- **工程1の「読まない」に `trailer-structure.md` と `book-trailer.md` を足した。**
  埋めた `trailer-structure.md` には hard gate・集合としての候補選定・spoiler・
  忠実性と広告適合性という**評価**が入っている。**Think に評価を持ち込むと発散が狭まる**
  （差分も「工程1では新規ファイルを必読にしない」を守るよう指定していた）

**指示と一致していたもの**

- **第1段（覆す5項目を先に）** — 4件入れた。5件目の制作フロー順序変更だけ保留
- **第2段（trailer-structure を5本まとめて埋める。1本ずつやらない）** — そのとおりにした
- **第5段（一案件を通してから残りを入れる）** — テンプレート4本と `non-plot-shots.md` を
  作らなかった判断と一致
- **pillow shot の上書きが正しい理由** — 差分は「覆す対象は Claude が二次資料だけで書いた
  記述。Codex は Burch 原典・伊藤 2013・Rybin 2021 に当たっている」としている。**同意する**

**指示と違う判断をしたもの**

- **第3段（テンプレート4本を欄統合してから一度に作る）を実行しなかった。**
  作らないほうを選んだ。**結果として、差分が挙げた3つの欄の衝突は発生していない**——
  `decision_owner` / `approver` の二重も、`answer_disclosure` / `ending_disclosure` の重複も、
  `non_plot_function` の置き場所も、**欄名ではなく散文で書いたので起きない。**
  実際に本文へ埋めた欄名は衝突しない9つだけ（`exact_or_inferred` `continuity_target`
  `discontinuity_purpose` `previous_function` `shot_function` `next_function` `audio_role`
  `loss_if_removed` と単位名）

**数のずれ**

- 前のエントリで「約45項目」と書いたが、**実測47項目**（10 / 12 / 8 / 8 / 9）。差分の数が正しい

## 2026-08-30（予告編構成論・Codexの調査5本／棚を埋めた）

差分5本（`trailer-rhetoric-structure` / `source-breakdown-scene-selection` / `non-plot-shots` /
`book-trailer-representation` / `radio-cut-trailer-sound`）。約45項目。
**全部が他のLLM（Codex / GPT-5系）の文献調査で、自社検証は3項目だけ。**
一昨日 `8d431c4` で足した「他のLLMの調査結果」の等級を、そのまま適用した。

**当たり直した引用と、見つかったずれ**

| 引用 | 結果 |
|---|---|
| Grøn (2014) *SoundEffects* 4(1) | **抄録を確認。** 題名・著者・"tentative book trailer typology"・"question of representation"・「読書体験の virtual quality を侵害しうる」まで一致。**5類型の名前は抄録から確認できず** |
| Oja (2019) *Sign Systems Studies* 47(1–2) | **題名が差分と違った。** 差分は「The trailer as a model of a film and the film as a model of the trailer」としていたが、実際は **「On the concept of the deceptive trailer: Trailer as paratext and multimodal model of film」**。年は一致。**著者名は確認できず** |
| Derek Lieu "Basic Trailer Story Structure" (2017) | **2点ずれ。** ①本人は**ゲーム予告編**の編集者。②**三幕ではなく4区分**（cold open / introduction / escalation / climax）。本人が「三幕だが頭に1つ足す」と書いている。**「title 後の button」はこの記事に出てこない** |
| Kernan (2004) 第2章 | 出版社の本文ページは取得できず（HTTP 405）。**書誌のみ** |

**埋めたもの**

- `principles/trailer-structure.md` を **88行 → 252行**に。**「空であることを見せるファイル」から
  中身のあるファイルになった。** 訴求の三分類、primary / supporting appeal、dominant による
  不一致検査、視聴者の知識曲線、spoiler の2分類、構造の開始形、story chunk、原作の分解、
  単位の分離、集合としての候補選定、ショット間の6関係、Kuleshov、イベント境界、
  連続性の守る／破る、物語を進めないカットの用語分離
- `principles/book-trailer.md`（101行）を新設。**完成映画の予告と素材条件が違う**——
  顔・場所・出来事・音を新たに解釈して作る。原作にあるものと足したものを分ける、
  具体化そのものを承認項目にする、表象予算、素材別の表象責任

**覆したもの**

- **「30秒を通して切れない音を1本敷く」を必須条件から外した。** 「連続性を優先する場合の
  開始形」に狭め、**幕転換・shock・重要台詞・時間や場所の断絶では意図的に切る**を併記
- **Skill 工程2の「最初のフレームに動きがあるか」を条件化した。**
  「**媒体の視聴開始条件に合う最初の認識イベントがあるか。動きを使わないなら理由を書く**」へ。
  劇場・書影・意図的な静止まで縛っていた
- **`workflow.md` の「ペースだけが個別判断」を狭めた。** 列挙した5つの**技術的**破綻の話で
  あって、**構成判断まで一括処理できるという意味ではない**
- **pillow shot の定義を狭めた。** 前に書いた「物語を進めないカットを意図的に挟む技法」は
  一般化しすぎで、**任意の非物語ショットの包括名に見える。** 原典は小津作品についての語

**作らなかったもの**

- **テンプレート4本**（`trailer-outline.md` / `trailer-brief.md` / `source-breakdown.md` /
  `cut-pair.md`）**を新設しなかった。** スキーマの名称と項目が**調査側の提案で自社未検証**
  であることを差分自身が明記している（「Trailer Evidence Unit という名称とスキーマは
  本調査による提案」「representation budget という名称と0〜3案は本調査の提案」
  「10項目という数と内容は本調査の提案」）。`templates/` は埋めて使う型を置く場所で、
  **未検証の様式を置くと、使われたときに検証済みに見える。**
  `principles/directing.md` の Performance Context を保留しているのと同じ判断
- 提案されていた**欄名は各節に埋め込んだ。** 1案件で手書きして残ったものをテンプレートにする
- **`principles/non-plot-shots.md` を作らなかった。** 原典（Burch 1979、Rybin 2021、伊藤 2013）を
  読んでいないので、`trailer-structure.md` の1節に収めた。**用語を分けるという指摘自体は採った**
- **Grøn の5類型の名前を書かなかった。** 抄録で確認できたのは「暫定的な類型がある」ことまで。
  **類型があるという事実だけで当面は足りる**——案件ごとに表象を1つに決め打ちせず候補として
  並べる運用は、名前がなくてもできる

**保留**

- **制作フローの順序変更（Brief → 初回breakdown → Think → 試し撮り → 構成）は採らなかった。**
  提案の中身は妥当だが、**現行フロー自体がまだ1案件も通していない**（`workflow.md` 冒頭に
  その注記がある）。**順序を二重に変えない。** 案として本文に併記し、1案件で試してから決める

**ルーティング**

- 工程2の「読む」に `trailer-structure.md`、案件条件に**原作の媒体と公式視覚素材の有無**を追加
- 工程2の手順に**訴求の決定**と **radio cut** を挿入（手順2と3）。
  **radio cut を工程5に置くと、画の本生成後に音声骨格の欠陥が見つかる**

## 2026-08-30（SKILL.md のルーティングが本文に追随していなかった）

差分1本（`skill-routing`）。**直近2コミットの取りこぼしの指摘。全部当たっていた。**

**3つの欠落を直した**

- **工程2から `principles/directing.md` への経路が無かった。** `8ba384e` で同ファイルに
  台詞の演技設計を約99行足したが、あのファイルは**工程4Cの「芝居がある」からしか辿れない。**
  原作から人物の知識・目的・関係・subtext を抽出するのは**どの台詞を使うか決める工程2の作業**
  なので、条件付き（「台詞を使う構成なら」）で経路を足した
- **症状索引8項目がすべて画の症状だった。** 声・演技の5行を足した——声質が振れる／読み上げが
  平板／**日本語の言い方が不自然**／漢字の読みが違う／台詞が読み上げられない・勝手に読まれる。
  **どれも2026-08の案件で実際に起きたもの**
- **出所の割引表に「他のLLMの調査結果」の等級が無かった。** 5等級のどれでもない——
  **外部情報とは違う**（文献ではなく文献についての要約だから）、**推測とも違う**（出典が
  付いているから）。**しかし出典の実在と記述の一致は、確認するまで分からない**

**構造的な原因のほうを直した（差分もこちらを重要としていた）**

- `CLAUDE.md` の取り込み手順に1行足した。
  **「この差分で本文に足した内容は、SKILL.md のどの工程から辿り着けるか。
  辿り着けないならルーティングを直すか、直さない理由を書く」**
- **`build-skill.sh` では代替できない**ことを併記した。あれが検査しているのは
  **SKILL.md が名指しするファイルが存在するか**であって、
  **本文が増えたのにルーティングが古いままか**は見えない。
  「参照されるべきものが参照されていない」状態は構造上あの検査に映らない。
  実際 `8ba384e` では**参照切れが1件も出ていない**
- `DIFF-FORMAT.md` の受け取り側チェックにも2項目足した（LLMの調査結果の扱いと、
  ルーティングの要否判定）

**記録として残したもの**

- **`models/` 配下が SKILL.md から名指しされていないのは意図的。** 工程3C・3Dと4B・4Dで
  「対象モデルの `models/` ファイルを読む」と抽象的に書いてあり、**モデルが増えるたびに
  SKILL.md を直さなくて済む設計。** 差分が「次に同じ検査をした人が欠落として報告し直さない
  ため」に記録を求めていたので、SKILL.md 本体に1節として置いた

## 2026-08-30（演技設計の大規模調査・8項目中7件を反映）

差分1本（`performance-context`）＋原文の調査報告書（21章1677行）。
**原文は `video-corpus/` に保管。本体には要約ではなく、必要な結論だけを入れた。**

**引用の再確認**（2026-08-30）。差分が「確認済み」としていたもののうち、
**こちらで未確認だった2件に当たり直した。両方とも実在し、内容も一致。**

| 引用 | 確認 |
|---|---|
| Yadavalli, Pimentel, Regev, Wilcox, Warstadt / arXiv:2512.16832 | 題名・著者5名・**「一桁以上」と、その条件節「長期文脈が使えないとき」まで一致** |
| Chen, Xue, Guo "ActorMind" / arXiv:2604.11103 | 題名・著者・**4エージェント構成・7,653発話/313場面/6役**まで一致 |

**前回の保留が1件解けた**

- 「音声側の情報量がテキストより一桁以上大きい」は、`ef58839` で**論文を特定できないため
  保留**にしていた。**特定できたので入れた。** Regev et al. と著者が重なる同じ系列の仕事
- **条件節を落とさない**ことを本文に明記した。**「長期文脈が使えないとき」という限定つき**で、
  長期文脈が使えるなら差は縮む。**「音声のほうが常に情報が多い」と読むと誤り**

**新規（`principles/directing.md`）**

- **日本語では、英語の直観をそのまま移せない。** 「最後の単語を強調」「語尾を上げる」を
  機械的に適用しない。日本語自発音声では **F0 が常に強い prominence cue とは限らない**。
  **自社は既にこれを踏んでいる**——2026-08 の案件で英語の地の文に「語尾を上げ切らない」に
  相当する指定を書いた。**このリポジトリは日本語案件しか扱っていないのに、音声の指示は
  英語の作法で書いていた**
- **感情ラベルは制御変数として狭すぎる。** emotion のほかに appraisal / objective /
  speech act / target / suppression / trajectory の6つ。**とくに発話行為の粒度が効く**——
  「悲しい」より「確認」「懇願」「否定してほしい質問」のほうが決まる
- **仕様に入る情報を3種類に分ける。** Quoted fact（原作に明記）／ Derived state（推論）／
  Creative direction（予告編機能のため）。**混ぜると予告編上の創作判断が原作設定として
  固定される。** 構造は `CLAUDE.md` の出所の書き方と同じで、**それを演技仕様の内側にも持ち込む**

**書き換え**

- 「分かっていないこと」を**「中間工程の不在」**という形に立て直し、空いている6段を並べた。
  **台詞が薄いのは音声モデルの品質のせいではない。** 4段目（Source → Trailer）が
  **ただの style transfer ではなく、何を保存し何を変えるかの編集判断**であるのが要点
- 「次に確かめること」に**最短の実用解の5段**を足した。**全自動化を先に目指さない。
  独自のTTSを学習する優先度は低い**——ボトルネックは文脈から演技仕様を作る層で、
  そこは model-agnostic に作れる

**追記**

- `models/video/wan.md` の声の式に、**「感情」以外の制御変数を持っていない**旨。
  式の更新は実験の後で決める
- `OPERATIONS.md`「画質の判断」の隣に「**音の採否も同じ**」。
  **「良い音か」ではなく「このカットで機能するか」。** F0-RMSE も MOS も
  タイムライン上での適合を測らない

**書かなかったもの**

- **Performance Context の完全なスキーマは、まだ手順として書いていない。** 実験（3条件×3回）を
  やるまで、というのは `ef58839` からの継続。**ただし原文が corpus に入ったので、
  「完全なスキーマは調査報告書12章」と参照先を書けるようになった**
- **原著未確認のもの（BATONVOICE / CEAEval / Harness TTS / ChainTalker / IIICSS /
  DailyTalk / InstructTTS / PromptStyle）は名前だけ記録し、数値は書いていない。**
  Harness TTS の routing 精度は「明示的な指示と暗黙の指示で大きく差が出る」という
  向きだけ書いた。**原作の subtext はほぼ暗黙の指示にあたる**ので、事実なら効く

**先行差分との関係**

- 差分は `diff-2026-08-28-dialogue-intonation.md` を「破棄してよい」としていたが、
  **あちらは `ef58839` で既に反映済み。** 破棄ではなく**重複を避ける形で差分だけ足した**。
  ADR のキューシート／二重性／Regev の3〜8語と誤引用注意／LibriQuote／Conversational TTS／
  ElevenLabs の previous_text・next_text／抑揚スロットの欠落は、**すでに本体にある**

## 2026-08-30（案内構造の修正・提案4件中3件を採用）

差分1本（`repo-navigation`）＋ README追記案。**知見ではなくリポジトリ自体の構造の話。**

**指摘は正確だった。** `README.md` の「構成」節は `skill/` も `build-skill.sh` も挙げて
いなかった。**リポジトリで最も完成度の高い誘導文書が、入口から辿れない状態。**

**入れたもの**

- `README.md` 冒頭に「どこから読むか」。**入口を3つに分けた**——制作で使うなら
  `skill/SKILL.md`、書き込むなら `CLAUDE.md`、**知見を持ち込むなら `DIFF-FORMAT.md`**。
  提案は2つだったが、実際の運用では差分の持ち込みが3つ目の入口として立っている
- `README.md`「構成」に `skill/` と `build-skill.sh` を追記
- `principles/README.md`（30行）／`platforms/README.md`（18行）／`templates/README.md`（15行）
  を新設。**一覧と一行説明だけ。「どの工程で読むか」は `skill/SKILL.md` へ誘導する**
- `models/image/` `models/video/` には README を**作らなかった。** `models/README.md` に
  既に一覧があったので、そこを表にして一行説明を足すだけで足りる
- `principles/still-to-motion.md` の冒頭に1行。**このファイルは案件全体の条件で、
  画像の工程とプロンプトの工程の両方で参照する**

**build-skill.sh を直した**

- **ディレクトリ直下の `README.md` を配布物から除外した。** 除外しないと
  `principles/README.md` `platforms/README.md` `templates/README.md` の3つが
  ChatGPT版のフラット化で衝突して**ビルドが落ちる**（既存の衝突検査に引っかかる）。
  除外は回避策ではなく設計判断——**ディレクトリREADMEは人間がリポジトリを歩くための案内で、
  配布物にはSKILL.mdの誘導が既にある。** 両方入れると片方だけ古くなる
- 除外は `collect_md()` と衝突検査の**両方**に入れた。前者だけだと検査側が拾って落ちる

**採用しなかったもの**

- **提案4「各ファイルの冒頭に、どの工程で読むかの1行」は入れなかった。**
  36ファイルに工程番号を書くと、**SKILL.md の工程が変わったとき36箇所が黙って古くなる。**
  実際に `911a651` で工程4Cのルーティングを変えたばかりで、**その種の変更は起きる。**
  そして `build-skill.sh` はファイル冒頭の記述を検査していない
  （差分自身がこの二重管理の危険を提案3について指摘していた。**提案4にも同じ話が効く**）
- 代わりに、**工程番号を書かない形で、単独で開くと誤用しうるファイルにだけ**入れた。
  `still-to-motion.md`（案件全体の条件）と `house-style-live-action.md`（実写用。
  `911a651` で対応済み）の2つ。**ルーティングが変わっても腐らない書き方にしてある**

**受け取り側で見つけたずれ**

- 提案の `principles/` 一覧が**13ファイル**になっていた。**現物は14**——
  `trailer-structure.md` が同日に増えている。実ファイルから作り直した
- 差分が `skill/SKILL.md` の良い点として挙げていた
  **「未反映の差分があるときの但し書き（「差分9番が未反映ならこの節は無い」）」は、
  実際には古い残骸だった。** 参照先の節は既に存在していたので、同日に削除済み

## 2026-08-30（構成論の棚が無い・地図として反映）

差分1本（`repo-gap-structure`）。**「何が分かったか」ではなく「何が無いか」を書く差分。**
差分自身が「そのまま本体へ写すのではなく、次に何を読むべきかの地図として使う」としており、
**その指示どおり、二次資料の要約は一切書いていない。**

**受け取り側で欠落を再確認した**（2026-08-30）。指摘は正確だった。

- `principles/` は13ファイル。**予告編そのものの構成論・編集論を主題とするファイルは0**
- `workflow.md` の出所は Higgsfield Academy が2件、AI映像制作の実務記事が1件、
  あとは自社の整理。**映画・映像の専門文献は0件**
- **差分が挙げていない、もっと鋭い形が見つかった。** Skill の工程2「構成・カット表」が
  読むものは `templates/shot-schema.md`（型）と `sound.md` `typography.md`（要素別）。
  **5工程のうち、自分の原則ファイルを持たないのは工程2だけ**

**新規ファイル**

- `principles/trailer-structure.md`（88行）— **中身はほとんど空。空であることを見せるための
  ファイル。** 欠落の記録、断片がどこに間借りしているか、用語と原典、読むべき文献、
  埋め方の規律。**二次資料しか見ていない用語（枕ショット、カーナンの3訴求）は、
  名前と原典だけを書き、要約は書いていない**

**今すぐ使える1件**

- `genres/README.md` の2軸に**「既知性への訴求」が無い**。書籍なら著者・原作・シリーズ・前作。
  実際に案件のエンドカードは前作への訴求を入れていたが、**設計としてではなかった**。
  **これは文献を読まなくても効く**ので、引きの型の3つ目として並べた

**追記**

- `principles/workflow.md` の「編集の5つの破綻」に、**この節が本来このファイルのものではない**
  ことを書いた。**移すのは中身が増えてから。今移しても場所が変わるだけ**
- `skill/SKILL.md` 工程2 に、構成論の原則ファイルがまだ無いことを明示

**ついでに直したもの**

- `skill/SKILL.md`「静止から始める場合」に **「差分9番が未反映ならこの節は無い」**という
  古い但し書きが残っていた。`workflow.md`「静止から始めてよい場合」は既に存在するので削除

**書誌の確認**

- Walter Murch, *In the Blink of an Eye: A Perspective on Film Editing* を一次で確認
  （Silman-James Press、初版1992、第2版2001、Coppola の序文つき）。
  差分が「Claude は書誌を確認していない」としていた1冊

## 2026-08-29（切り出された台詞の演技・11項目中9件を反映、2件を保留）

差分1本（`dialogue-intonation`）。**このリポジトリで初めての、外部研究が主体の差分。**
出発点は ChatGPT の回答で、そこに挙がった文献を差分の作成側が一次情報に当たって検証している。

**受け取り側で引用の実在を再確認した**（2026-08-29）。差分に書かれた**3件はいずれも実在し、
主張の内容も一致した。**

| 引用 | 確認 |
|---|---|
| Regev et al. (2025) ACL / arXiv:2503.11630 | 題名・著者8名・**過去3〜8語／未来1〜2語**まで一致 |
| Michel, Epure, Cerisara (2026) Findings of ACL / arXiv:2509.04072 | 題名・著者・**LibriQuote 5.3K時間・speech verb と adverb の疑似ラベル**まで一致 |
| ElevenLabs `previous_text` / `next_text` | 公式APIリファレンスに存在。**読み上げられず連続性にのみ影響**することを確認 |

**新規節**

- `principles/directing.md`「切り出された台詞に演技を与える」— **実写の予告編では音声ごと
  切り出されるが、AI映像では工程が逆**という問題の立て方。以下を含む
  - **業界はADRのキューシートで解いている。** 映像／元の音声／キューシート（文脈と感情の
    ノート）を渡す。**取り除かれた文脈を、文章にして俳優へ戻している。**
    既存の「音声指定は内容・声色・禁止事項の3点セット」には**文脈が入っていない**
  - **本編どおりの演技を再現すればよいわけではない。** 予告編には文脈が2つある
    （Source performance × Edited context）。**差分の作成側がこれを一番価値のある部分として
    挙げており、同意する**
  - **抑揚のスロットが空欄のまま運用されていた。** `wan.md` の式に抑揚があるのに、1案件で
    速さと調子しか埋めなかった。**式が使われない理由は、埋め方が書かれていないことかもしれない**
  - 次に確かめること — Performance Context という中間表現の提案。**差分の指示どおり、
    検証するまで手順としては書かず、実験計画としてだけ置いた**
- `principles/sound.md`「読ませない文脈を、読ませるテキストの隣に置く」— ElevenLabs の
  `previous_text` / `next_text`。**ADRのキューシート・LibriQuote の疑似ラベル・このAPIが
  同じ形に収束している**
- `principles/sound.md`「声の演技についての参照文献（外部・自社未検証）」

**追記**

- `models/video/wan.md` の声の式に、抑揚が埋められていない旨の注記

**反映しなかったもの（差分の指示どおり）**

- **「音声側の情報量がテキストより一桁以上大きい」（ACL 2026）** — ChatGPT の回答に
  あった主張だが、**差分の作成側が該当論文を特定できなかった。** 論文が特定できるまで書かない
- **Higgsfield が演技指示を書かせる方向へ進んでいる（4件）** — ChatGPT の回答由来で
  **一次情報が未確認。** `DIFF-FORMAT.md` の「外部調査由来のツール候補が入っていないか」に
  直接あたるため、**flag 付きでも public には置かない。** 自分のアカウントで公式資料を
  確認してから入れる

**受け取り側で強めたこと**

- **Regev et al. の誤引用に対する注意を、箇条の中で一段強く書いた。** この論文が測っているのは
  **語単位の局所窓**であって「数分間の出来事が声に残る」規模の主張ではない。
  **このリポジトリの問題設定（俳優の経験まで生成する）は、まさにこの論文を誤って
  引きたくなる形をしている。** 差分がその危険を自分から書いていたので、そのまま採った

## 2026-08-29（成功プロンプトの実物と、制御しすぎの回避・5項目）

差分1本（`channels-and-control`）。**1件を伏せて反映した。**

**public に出す前に伏せたもの**

- 実例プロンプトに**原作の台詞が原文のまま入っていた。** 差分のヘッダは
  「クライアント名・書名・著者名・キャラクター名は除いてある」としていたが、**台詞は
  除かれていなかった。** 同じ差分のもう1本（タイトルカード）は `"（告知の一文）"` の形で
  伏せてあり、**片方だけ抜けていた。** 台詞を `「（台詞）」` に置換して反映し、
  原文は `video-corpus/diffs/` にのみ残した

**新規節**

- `principles/sound.md`「画面に出る文字と、音声で流す台詞は別チャンネル」—
  **吹き出しのないコマは音声チャンネルが空いている。** 画面は原稿のまま保ち、意味とテンポの
  補いを音だけで行うと、「文言を出すと訴求方針と衝突する／出さないと理解できない」が両立する
- `OPERATIONS.md`「クライアントの指摘は、指摘された範囲だけを直す」—
  縦横比の破壊についての指摘を「生成側でカメラを動かさない」まで広げ、
  **その案件で最も価値のあった手法を最初に捨てていた**
- `platforms/model-quirks.md`「成立した実例 — ページ → コマへの寄り＋飛び出し」
- `models/image/nano-banana.md`「成立した実例 — 日本語の縦型タイトルカード」

**既存への統合**

- `principles/prompt-compilation.md` の「長い禁止の列が効いていない可能性」に、**書き方の
  指針**を足した。**否定を1つ書くたびに「そこは壊れる」という前提を成果物に書き込んでいる。**
  まず短く書いて、壊れたところだけを1箇所ずつ足す。差分が統合を指示していた
- `principles/still-to-motion.md`「発生を描くなら、発生前の絵を探す」に**第3の手**を足した

**受け取り側で足した読み**

- **「発生は描けない」（既存）と「飛び出しが成立した」（今回）は矛盾していない。**
  成立した側は**飛び出しの動作を作り込んでいない。** ページから下段のコマへ寄る
  カメラ移動を書いただけ。**モデルの挙動は変わっておらず、発生そのものは今も描けない。**
  変わったのは、被写体を大きくするしかなかったスケール変化に**カメラの接近という理由が
  与えられた**こと。**失敗時の「巨大化」と成立時に起きていることは同じで、動機づけの
  有無だけが違う。** 選択肢を3つ（発生前の絵／カメラで代替／諦める）として書いた
- **「1000語で3回破綻」は、これで3本目の差分に出てきた同じ3回。**
  `h3-keyframes`（FL2VAのジャンプ）、`h3-camera`（長いプロンプト）、今回（制御しすぎ）。
  `prompt-compilation.md` に既に「競合仮説であって独立した証拠ではない」と書いてあるので
  追記のみに留めた
- 運用の「指摘を広げない」と、プロンプトの「禁止を広げない」（`guards.md`）を相互参照で
  繋いだ。**根は同じ**

## 2026-08-28（同案件の後半・21項目）

差分3本（`endcard-sound` / `h3-camera` / `text-budget`）。エンドカード、H3のカメラと音声、
画面文字の読字予算。**1項目は重複のため反映せず、2項目は差分の判断を変えた。**

**覆したもの**

- `principles/typography.md`「日本語の責任分配」— **「日本語だから生成させない」を取り下げた。**
  現行の画像生成モデルは日本語の字形を正確に出せる。**この矛盾はリポジトリ内に既にあった。**
  `platforms/model-quirks.md` の自社検証（「日本語の文字を正確に出せる」）に対し、
  typography 側の**未検証の外部方針**が強く書かれたまま残っていた。
  ただし**どちらの記録もモデル名・時期・回数がない**ので、その旨を明記した。
  同節の「文字デザインと動きを同時に一発生成しない」は別の理由で有効なので残した
- `platforms/model-quirks.md` / `principles/still-to-motion.md` — **「素材を最終解像度に落とすと
  ズーム余地はゼロ」から「だから寄りは別クロップ」へ繋がる処方を、唯一の手ではなくした。**
  消えるのは編集側の自由度だけで、**生成側でカメラは動かせる。**
  差分は model-quirks だけを覆す対象に挙げていたが、**同じ記述は still-to-motion にもある。
  両方直した**

**新規節**

- `principles/still-to-motion.md`「素材に焼き込まれた文字は、読字予算になる」— 20カット構成で
  割当秒数に対し**2倍から10倍の超過**。最長カットは61字に1.5秒。12カットへ削減した。
  図解ページ優先、通過するコマは予算に入らない、も同節
- `principles/typography.md`「エンドカードは分割しない」「免責表示は点滅させず、対象ブロックを
  通して出す」
- `principles/sound.md`「音も表現ルールの対象」
- `OPERATIONS.md`「構成を確定する前に数える」— カット数・秒数・文字量。
  **画面に出る文字だけを通しで読む**検証（記憶で行わない）、構成パターンに属さないシーンを
  残さない、も同節
- `OPERATIONS.md`「素材を依頼する段階で、帯なしの版があるかを訊く」— **高解像度の書影を
  入手した時点で、低解像度では読めなかった帯の文言が読めるようになる**
- `models/video/minimax-h3.md`「話し方の指定をどこに書くか」
- `principles/prompt-compilation.md`「1件の観測 — 長い禁止の列が効いていない可能性」

**既存への追記**

- `platforms/model-quirks.md` H3節に4項目。**カメラを動かしても縦横比は壊れない**
  （3つの明示が条件）／**画面の日本語セリフを読み上げる。品質は実用に足りた**／
  塞ぐか使うかは方針で決まる／音の指定を足したときの副作用は未確認
- `principles/sound.md`「生成→選定ループ」に**同主調で床を2本作って差し替える**
- `models/image/nano-banana.md` に**「空けろ」だけでは空かない**

**受け取り側で変えたこと**

- **「床はカット単位で作るものではない」は反映しなかった。** 同日の `h3-keyframes` に同じ項目が
  あり、`236378a` で既に `sound.md` へ入っている。差分自身が重複確認を求めていた
- **読み上げの扱いが、同日の差分どうしで反転していた。** `h3-keyframes` は「読み上げうるので
  音のフィールドを埋めて塞ぐ」、`h3-camera` は「省くと読み上げる。品質は実用に足りたので
  資源になる」。**矛盾ではない——指定すれば止まり、省けば読む。** 方針で決まると書き分け、
  **事故になるのはどちらでもない中間（埋め忘れ）**だと明示した
- **「長いプロンプトは効いていない」の3回は、`h3-keyframes` の「FL2VAでジャンプする」と
  同じ3回。** 「版面差が原因」と「プロンプトの長さが原因」は**独立した2件の証拠ではなく、
  同じ観測に対する競合仮説。** 片方を根拠に片方を補強しない、と書いた。
  切り分け実験（同一カット・同一素材で長短各3回）は**1つで両方に効く**
- **「毎秒4文字」は保留せず、出所不明と明記して書いた。** 差分は `DIFF-FORMAT.md` の
  「数字には誰が測ったかを書く」に照らして保留を推奨していたが、**この数字を抜くと
  読字予算の節が運用できなくなる。** 文字数の実測（61字／20カット計数／12カットへ削減）は
  自社検証なのでそのまま書き、**秒あたりの変換値だけが出所不明**であることを節内に明示して、
  一次資料の調査を「検証したいこと」に立てた。「毎秒9〜10モーラ」も同じ扱い

## 2026-08-28（漫画原稿を素材にした実案件からの反映・18項目）

差分3本（`handoff` / `h3-keyframes` / `manga-source-prep`）。素材処理・H3の実挙動・受け渡し事故。

**新規節**

- `principles/still-to-motion.md`「素材の物理仕様を先に測る」— 解像度・ビット深度・網点線数を
  実測すると、出力解像度・ズーム上限・モアレ域が**算術で決まる**。1件の実測値を表で置いた
- `principles/still-to-motion.md`「網点素材の縮小」— **一律ディスクリーンをやめる。**
  面積平均で縮小すれば無処理でも網点は平坦に落ちる。ぼかしは
  **リサンプラーを制御できない工程（NLEのズーム・パン）にだけ弱くかける**
- `principles/still-to-motion.md`「先に、素材に文字なし版が隠れていないか調べる」—
  校了PDFは絵柄ラスタ／写植テキストに分かれていることがあり、**clean plate 工程が丸ごと消える**
- `principles/still-to-motion.md`「発生を描くなら、発生前の絵を探す」— I2Vは結果が描かれた
  素材から発生を遡れない
- `principles/vertical.md`「縦パンは、素材の高さがフレームを超えていないと成立しない」
- `principles/sound.md`「静止画素材では、声の手掛かりが画に残らない」
- `platforms/magnific.md`「出力の実効値」— **H3の出力尺が5.166667秒（24fpsで124フレーム）に
  固定**。モデル仕様の「4〜15秒」とは別レイヤーの話
- `OPERATIONS.md`「生成プロンプトと素材の受け渡し」— `.md` 経由のアンダースコアのエスケープ、
  zipの日本語名でフォルダ構造が壊れる件、画質判断の手段

**既存への追記**

- `platforms/model-quirks.md` H3節に4項目。FL2VAで**最終フレームへはジャンプで着地する**
  （3回とも共通、モーフィングには逃げない）／I2VAの巨大化／音の指定は画への干渉を止めるため
- `principles/still-to-motion.md`「2枚の間を補間する」に**効く範囲の限定**。
  中間動作は取れるが着地は取れないので、**決めポーズへの着地は編集で持つ**
- `principles/guards.md` に**禁止を広げすぎる副作用**。変倍（比率の破壊）と拡大（比率を保つ
  スケール）を混同して拡大を全面禁止すると、遠近として正しい動きまで潰れる
- `principles/sound.md`「環境音は連続性の担保」に**これは編集工程の指示**という但し書き
- `OPERATIONS.md` 承認設計の節に「**承認された資料がその段階の要件を満たしているとは限らない**」。
  節のヘッダが「自社未検証」なので、**サブ節を分けて自社検証と明示した**
- `DIFF-FORMAT.md` のエスケープ検査の適用範囲を、**入ってくる原文だけでなく出ていく
  プロンプトにも**広げた

**受け取り側で直したもの**

- 差分は「素材段階での一律ディスクリーンは**既存を覆す**」としていたが、
  リポジトリ本体を検索した結果、`ディスクリーン` `descreen` は**0件**で、その方針は明文で
  入っていなかった。**新規として入れた**（差分自身が「入っていなければ新規」と自己申告済み）
- 「検証の指標を作るより目視比較のほうが早い」は**そのままだと危ない**ので条件を付けた。
  同じ案件に**逆の事例**がある。σ1.2 のプリブラーが適用済みかの事実確認は目視では決まらず、
  数値でしか確定しなかった。**しかも最初に選んだ指標が現象に対して不適切で誤判定を出した。**
  「良く見えるか」は目視、「適用されたか」は数値、と分けて書いた
- FL2VAのジャンプについて、**既存の「1生成内でのハードカットが成立する」と同じ性質ではないか**
  という読みを推測として併記した。切り分け実験はしていない
- 網点1セル 10.06px は実測値。**公称は `dpi ÷ 線数` = 10.0** なので両方書いた。
  σ の目安を**セル幅 × 0.35 前後**として一般化した
- 中間倍率の未検証（0.347倍と1.00倍の両端しか見ていない）を**「分かっていないこと」として
  明示**した。ズームはその間を連続的に通過する

## 2026-08-28（工程1が圧縮される問題）

- **実運用で工程1の手順が走らないことが分かった。** 制約を1つしか使わず、20案で止まり、
  ★で評価して絞り込む、という出力になった。**「Skillを最初から最後まで通して」と明示すれば
  走るが、普通の依頼では走らない**
- 原因は2つ。**ユーザーが「20個出して」と件数を指定すると、それを総数として読んで
  「制約ごとに20案」が潰れる。** そしてスキル側に、件数の衝突をどう読むかの記述が無かった
- 対策：工程1の手順の冒頭に**実行表**を置き、発散前に「使う制約3つ以上／1制約20案／
  総件数＝制約数×20／制約ごとに分離／評価禁止」を固定させる。
  **ユーザー指定の件数は1制約あたりの数として読む**と明記
- **「1つ目の制約で20案出すと、それらしい分量になるので終わった気になる」**を
  最頻の失敗として本文に書き、総件数の検算をチェックリストに追加した

## 2026-08-28（工程4Cのルーティング修正）

- **バグ修正**：SKILL.md 工程4C が `templates/house-style-live-action.md` を「常に読む」に
  していた。**あのファイルの本文は `photorealism` と `NOT a cartoon` を含む。**
  漫画原稿を素材にするカットに貼ると、素材の画風と正面衝突する。
  「実写調で作る」ときだけに条件付けし、**素材が原稿・イラストのときは貼らない**行を追加した
- あわせて `templates/house-style-live-action.md` の冒頭に適用条件を明記。
  素材があるカットでは look の正本は素材のほうで、ハウススタイルブロックは貼らない
- 4Dのチェックリストにも「素材が原稿・イラストなら貼っていないか」を足した
- **発見の経緯**：ChatGPT に SKILL.md を読ませて漫画原稿案件を一周させたときに出た指摘。
  リポジトリは `genres/README.md` で実写調とアニメ調を分けていたのに、
  **SKILL.md のルーティングがその軸を使っていなかった**

## 2026-08-28（自社検証が外部情報を上書きした2件）

- **覆した**：`principles/still-to-motion.md` の「読める文字を生成中ずっと保持させない」を
  条件付きに修正。**元の記述は外部の実務記事に基づく自社未検証のもので、H3で漫画コマを
  動かした自社検証がそれを上書きした。** 軽い微動の範囲では文字を残したままで破綻しない。
  clean plate 化は必要なコマだけに限定する。あわせて **clean plate 化が原作から遠ざかる操作
  でもある**点（自由度と原作忠実度のトレードオフ）と、外すときの実体が「消す」ではなく
  「埋める」である点を追記した
- **条件が付いた**：`platforms/model-quirks.md` の「寄りカット用のクロップ素材の準備工程は
  不要」は、**生成側にハードカットを割らせる前提での結論だった。** 固定フレーム＋微動に振り
  カットを編集側で割る方針では逆になる。**素材を最終解像度の台紙に落とした時点で、編集側の
  ズーム余地はゼロ**（等倍でもアップスケールになる）。寄りは素材段階でクロップを別に作る。
  ズーム余地の消失は解像度からの帰結であって生成の検証ではない、と出所を分けて書いた
- 追記：`platforms/model-quirks.md` に「漫画コマは吹き出しと擬音を残したままでも破綻せず
  動く」（自社検証）
- 訂正：OPERATIONS.md の public/private 判断で「自社検証＝model-quirks.md」と読める書き方に
  なっていた。**`principles/` にも「（自社検証）」タグの項目が散在しており、
  `shot-structure.md` だけで12件、全体で22件ある。** 行数ではなくタグで数えるよう直した

## 2026-08-28（public/private の見直しを棚卸しへ）

- 追記：OPERATIONS.md「リポジトリを private にするか」。**判断せずに先送りするのではなく、
  月次の棚卸しで見直す形にした。** 目安は3つ（自社検証の分量が外部情報に近づく／
  クライアントから問われる／事業の資産として扱い始める）
- **現時点の判断は public のまま。** 自社検証34行に対して外部情報1034行で30倍の開きがあり、
  他人の調査で辿り着けるものが大半のうちは公開しても失うものが少ない。
  逆に public であることには「認証なしで clone できる」という実利がある
- private にする前に、**代替の受け渡し手段を先に決める**ことを明記した。
  現在の「差分を外で書いて中で反映する」分業が成立しなくなるため

## 2026-08-28（Phase B：画像側の知見）

**画像生成の知見は実質13行しか無かった。** `model-quirks.md` に3項目と `shot-schema.md` の
型1つだけ。一方 `shot-structure.md` は「画像生成の回数は案件全体で3〜7枚」と書いていて、
**上流固定の中心が画像生成**。投資対効果が一番高いところが一番薄かった。

- 追加：principles/image-production.md。**「アセット作成」1工程では粗すぎる。**
  画像タスクを12に分類、探索と本番を分ける、画像の3分類（証拠／方向／最終フレーム）、
  **動きやすい画という評価軸**（画として美しいことと動画の起点として良いことは別）
- 追加：principles/image-repair.md。**WHERE / WHAT / PRESERVE の3つを分けて書く。**
  PRESERVE が抜けるのが最大の失敗。OpenAI と Google の公式ガイドが独立に同じことを
  言っている（両方とも実物確認済み）。専用ツール → 局所編集 → 全体再生成の順に検討する
- 追加：principles/world-assets.md。場所を階層で持つ。**既存の「main / reverse の2枚」を
  否定せず、上流と下流を足した。** 30秒単発では2枚で足りる
- 追加：models/image/ に6本。gpt-image / nano-banana / seedream / krea-2 / soul / midjourney。
  **gpt-image と nano-banana と krea-2 は公式ドキュメントの実物を取得して確認**（Tier A）
- **ホストとモデルの分離を実際に適用した。** platforms/higgsfield.md から Soul ファミリーを
  models/image/soul.md へ、platforms/krea.md から Krea 2 を models/image/krea-2.md へ。
  Phase A で作った構造がここで効いた
- 追記：platforms/higgsfield.md に Higgsfield Edit と Cinema Studio 4.0。
  **4.0 は制御がプロンプトからUIへ大きく移った。** 感情・照明・色・カメラが選択項目になる。
  既存則が間違いになるのではなく、prompt-compilation.md の分類Bに該当する項目が
  このホストでは多い、ということ。**なお解像度と尺について公式3ソースが食い違っている**
- 追記：principles/prompt-compilation.md の分類Bを画像側へ拡張。
  **プロンプトに書くべきことが他の経路へ移っていく**というのが、
  「知見が増えてもプロンプトは長くならない」のもう1つの理由
- **SKILL.md の工程3を「アセット作成」から「画像」へ改称し、3A〜3Dに分解。**
  工程4と同じ構造にした。従来はモデルを聞いていなかった
- **未解決を記録**：肯定形と否定形の扱いが Google と OpenAI で食い違う。Google は
  「no cars ではなく empty street」、OpenAI は「no watermark, no extra text を明示的に書け」。
  既存の guards.md は動画側の corpus 由来。**画像では否定の扱いがモデルによって違う可能性**
- Midjourney は `models/image/midjourney.md` として作成。差分の推奨は candidates.md への
  縮小だったが、**candidates.md は「分業先のツール候補」であってベースモデルの棚ではない**
  ため、他の画像モデル5本と同じ扱いにした。契約の判断が付くまで
  「調べたが採用していない」記録として置く

## 2026-08-28（models/ の新設）

- 構造変更：`models/` を新設し、`platforms/` にあったベースモデルのファイル3本
  （minimax-h3 / seedance / wan）を `models/video/` へ移動。**ホストとモデルを混ぜない**
  という既存の教訓を、ディレクトリ構造に反映したもの。`platforms/` の862行のうち474行が
  実際にはモデルのファイルだった
- `build-skill.sh`：`DIRS` に `models/video` と `models/image` を追加。
  `find -maxdepth 1` のため、`models/` だけでは配下が拾われない
- 参照の更新：7ファイル。`CHANGELOG.md` は履歴なので更新していない
- **知識の追加はゼロ。** 画像側の知見は Phase B で入れる

## 2026-08-28（プロンプトの組み立て工程）

- 追加：principles/prompt-compilation.md。**知見が増えてもプロンプトは長くならない。**
  最終プロンプトは成果物であって原本ではない。情報の4分類（参照が持つ／プロンプト外で
  固定／言葉でしか伝わらない／失敗を塞ぐ）、MUST/SHOULD/MAY の削る順序、
  組み立ては最低3回ある（試し撮り・本番・修理、モデル変更で4回目）、出所の7段階格付け
- 追加：templates/shot-ir.md。**モデルを決める前に埋めるモデル非依存の仕様。**
  shot-schema.md の上流に立つ。既存の shot-schema.md に Seedance 向けと H3 向けの型が
  同居していたのは、**同じ仕様から変換した2つの結果**として説明できるようになった
- 追加：platforms/minimax-h3.md。**MiniMax 公式 Skill の実物から確認した項目を Tier A と
  明示。** モードを最初に決める（T2VA / I2VA / FL2VA / L2VA / Ref2VA で文法が変わる）、
  出力フィールドの順序、参照役割12種
- 追加：platforms/wan.md。6つの式と、**first/last frame と参照が排他**という制約。
  フレームを渡すときは「動き＋カメラの動き」だけ書く（still-to-motion.md と一致）
- 追記：platforms/seedance.md に公式の式・記号・参照の予算。**ファイルの性質が変わったので
  冒頭を書き換え**、公式の二次紹介と公開プロンプトからの逆算を分けた
- 追記：platforms/higgsfield.md に「プロンプトが内部で書き換えられる」。
  **自社のプロンプトの良し悪しを Higgsfield 経由の出力だけで判断できない**
- **条件が付いた**：「秒数は 0.0s から書く」を全モデル共通則として書いていたが、
  **そもそも秒を使うかどうかがモデル・バージョンで反転する**（Seedance 2.0 は番号方式を
  公式推奨、2.5 は秒指定が主機能）。既存則は「秒を使う場合の書き方」として残し、
  使うかどうかの判断を platforms 側へ分けた。二次資料であり自社未検証
- **未解決を記録**：ByteDance は「複数アングルを1枚のグリッドにまとめない」と述べているが、
  キャラクターシートの構成がこれに該当するかは不明。判断材料が足りないので変更せず、
  templates/character-sheet.md に比較の宿題として残した
- **SKILL.md の工程4を4段階に分解**（4A 仕様 → 4B 出力先 → 4C 知識を絞る → 4D 変換）。
  従来はチェックリスト10項目が全部「〜したか」の確認形で、**検証であって選択ではなかった。**
  全項目を満たそうとすると全部載せる方向にしか働かない。**モデルを聞いていなかった**のも
  同じ問題で、モデルが決まる前にプロンプトの形は決まらない

## 2026-08-28（原稿の入力ルールとプラットフォーム比較）

- 追加：OPERATIONS.md「クライアント原稿・第三者著作物の入力」。**預かった原稿は一般的な
  参照素材と分けて扱う。** 入れる前の4段階の確認、入力先を増やさない、加工しても素材の性質は
  変わらない、入力経路の記録、暫定の判断表。**公開資料の読み取りであって法的助言ではない**
- **既存の評価に条件が付いた**：platforms/higgsfield.md の判断メモは学習利用を
  「実害は限定的。エンタープライズの価値は説明できる根拠」と評価していた。
  第三者から預かった素材については**実害の大小と説明可能性を分ける**必要が出たため、
  既存の評価は残したまま「原稿は別。説明できるかで判断する」を足した。矛盾ではなく、
  既存メモ自身が説明可能性を価値としている
- 追加：OPERATIONS.md「制作プラットフォームの選択」。既存「リスク管理」は2系統確保を
  要求していたが選択の軸を定義していなかった。**モデル数で比べない。同一ショットで測る。**
  記録項目に「プラットフォーム間で素材を移した回数」を足した（移すたびに契約確認・
  アクセス管理・削除管理の対象が増えるため）
- 追記：platforms/magnific.md に契約・データ面、Spaces とワークフロー、料金。
  **学習に使わないことと、外部事業者が一切処理しないことは別条件。**
  画像・音声とプロンプト文章は同じ保護レベルではない
- 追記：platforms/higgsfield.md に Enterprise の料金体系と原稿入力の運用
- 追加：platforms/krea.md。2系統目の比較対象として個別ファイルに立てた。
  **通常プランのデータ利用条件は公開資料から確認できなかった。**
  「学習される」とも「されない」とも書かない
- **candidates.md の位置づけを変えた。** 「比較候補」として作ったが、制作基盤の候補と
  分業先のツール候補が同じ粒度で並ぶ状態だった。**分業先の候補に限定**し、基盤の候補は
  個別ファイルへ寄せた。あわせて DomoAI を落とした（Magnific が既に Seedance をホストし
  Spaces を持つため用途が重複）。候補の出所が外部調査であることを冒頭に明記
- DomoAI を落としたことで切れる参照2件を付け替えた。**higgsfield.md の比較表には出所列を
  足して値を行内で検証可能にした**（参照先から記述が消えても辿れるように）。
  seedance.md の参照先は higgsfield.md へ
- DIFF-FORMAT.md の受け取り側チェックに「外部調査由来のツール候補が入っていないか。
  **こちらの工程上の必要から探したものか**」を追加

## 2026-08-28（Skillの発火条件とprivate参照）

- **SKILL.md の description に静止画素材とSNS短尺を追加。** still-to-motion.md が独立した
  principles ファイルになったのに発火条件へ反映されておらず、漫画原稿を素材にする相談で
  Skill 自体が起動しない可能性があった。106→133文字（上限200以内）
- build-skill.sh に `--with-private` を追加。**video-projects/genres/*.md への絶対パスを
  skill-local にだけ埋める。** zip と ChatGPT 版には入れない。案件記述をアップロード先に
  乗せる判断は別途要るのと、OPERATIONS.md の「クライアント案件は必ずプライベート領域に置く」
  と衝突しかねないため
- SKILL.md の「案件の条件」3つ目を `<!-- GENRES -->` マーカー化し、版ごとに文言を差し替える
  形にした。**コピーではなく絶対パス参照**なので、private の内容は dist/ に一切出ない
- DIFF-FORMAT.md の受け取り側チェックに「原文を corpus へ入れるならエスケープ記号・
  強調記号の混入を確認したか」を追加。**今回は照合したから気づけたが、次に別の原文が来たとき
  同じチェックが走る保証が無かった**

## 2026-08-28（差分ファイルの保管先）

- **差分ファイルの受け取り前の置き場が定義されていなかった。** CLAUDE.md は「受け取ったら
  反映してコミットする」としか書いておらず、反映が済むまでチャットの出力にしか存在しない
  状態になっていた。実際に5本を渡し直してもらう事態が起きた
- `video-corpus/diffs/`（private）を新設し、既存18本を保管。**反映するより先に置く**運用に
  した。反映済みかどうかは CHANGELOG が唯一の判定先とし、diffs/ 側には書かない
  （2箇所で管理するとずれるため）

## 2026-08-28（差分 14／15／8／9／12）

- 追記：platforms/higgsfield.md に Shots、Popcorn の詳細、**Higgsfield 上の Seedance 統合**。
  **ホストの統合仕様とモデル本体の仕様を分けた。** 同じ Seedance 2.5 でもホストごとに
  案内される入力の内訳が違う（Higgsfield は最大50 multimodal、DomoAI は 30画像/10動画/10音声）。
  ホストの案内値をモデル仕様として書かない。「4秒以上」を H3 の制約と気づかず普遍則にした
  事故と同じ形になり得る
- 追加：platforms/candidates.md（未契約・未検証の比較候補）。DomoAI / Runway / Adobe Firefly。
  **個別ファイルにせず1本にまとめた。** 独立させると platforms/ に現行と候補が同じ粒度で
  並び、magnific が現行という区別が読めなくなる。切り出しの条件を明記
- 追記：principles/sound.md に「プロンプト内の音は二層」（全体の契約／その瞬間のキュー）と
  「音が何を証明しているか」（作品の性質ごとに音の役割を1つ決める）
- 追記：principles/workflow.md に「一発で作らず、段階に分ける」（動かない側を先に固定する）、
  導入をショットの外で済ませる、編集のペースと止まり
- **条件が付いた**：「冒頭は動きから始める」を無条件則として書いていたが、静止そのものが
  意味を持つ素材（漫画原稿など）では例外があり得る。**自社の失敗例と構造が同じ**
  （静止した被写体へのプッシュインで平均再生8秒・スキップ38.6%）で、違いは静止の後に
  何が起きるかだけ。成立条件を3つ付けたうえで例外扱いにした。自社未検証
- 追記：templates/vocabulary.md に漫画記号と大気・光の語彙。**変数の体系は
  atmospheric-fx.md、ここは対訳のみ**という分担を明記
- principles/still-to-motion.md の「差分9番で追加予定・未着手」注記を解除。
  参照先の節が生まれたため

## 2026-08-28（Skillビルド）

- 追加：build-skill.sh と skill/SKILL.md。リポジトリから Claude Skill / ChatGPT ナレッジを
  ビルドする。**配布版は reference/ にコピー、Claude Code 用は絶対パスで本体を直接参照**の
  2系統を吐く。案件フォルダで起動すると相対参照が切れるため
- **`pipefail` と `grep -q` の組み合わせで、閉じているフロントマターを「閉じていない」と
  誤判定する不具合を潰した。** grep -q が先頭マッチで即終了し、上流の tail が SIGPIPE(141)
  を受けて pipeline が失敗扱いになる。SKILL.md が 128KB 付近で発火する。実測で境界を確認。
  パイプを使わない形に書き換えた
- awk への値渡しを `-v` から `ENVIRON` へ。**`-v` はエスケープシーケンスを解釈する**ため、
  コミット件名の `C:	emp` が `C:<TAB>emp` になっていた。`&` の展開と同じ系統
- skill-local の sed 置換で `&` がエスケープされていなかったのを修正
- 参照切れ検査の `find | grep -q .` を `-print -quit` へ。同型が3箇所あって2箇所だけ
  直っている状態を避けた
- SKILL.md の鮮度の文言を `<!-- FRESHNESS -->` マーカー化。**ローカル版は常に最新なのに
  「ビルド情報より新しい変更は入っていない」と書いてあり、事実と逆だった**
- .gitignore に dist/ を追加
- OPERATIONS.md の棚卸しに「差分を反映してコミットしたら ./build-skill.sh を走らせて
  上げ直す」を追加

## 2026-08-28（差分 13／16）

- 追加：principles/still-to-motion.md（静止画素材を動かす）。**ほぼ全部が自社未検証。**
  書影・挿絵・提供写真・漫画原稿・先に確定させたシートなど、完成した静止画を素材として
  動かすときの工程・演出パターン・原作からの距離。検証項目7件つき
- **ジャンル固有ではなく素材の条件として立てた。** 元案は漫画ティザーとして genres/ へ
  入れる想定だったが、同じ問題が漫画以外の素材でも起きる。principles/ へ出したことで
  genres/ の2軸（物語作品／実用書 × 実写調／アニメ調）を崩さずに済んだ
- genres/README.md：現状表に sf-action.md を追加、practical-manga.md を private へ。
  末尾に still-to-motion.md への導線を追加
- video-projects（private）に genres/sf-action.md と genres/practical-manga.md を新設。
  **一般化すると価値が消える具体は private 側**という 2026-08-26 の決定に従った

## 2026-08-27（差分 1／2／4）

- 追加：platforms/seedance.md。**仕様書ではなく未検証の仮説置き場。** 他人の公開プロンプトの
  書きぶりから「作者が何を警戒しているか」を逆算した失敗モードの候補18件と、
  生成1〜2回で切り分けられる検証計画14項目。回した結果は表の右列に日付つきで書き込み、
  検証済みになったら model-quirks.md の自社検証側へ移す
- 追加：principles/atmospheric-fx.md。`heavy fog` としか書けない状態から抜けるための、
  大気の変数分解。Unreal / Blender / PBRT / Production Volume Rendering の物理モデルから
  語彙を取る形にした。**大半が外部情報で自社未検証**
- 追加：templates/action-sequence-schema.md。アクションのビート列の型（9ブロック）。
  shot-schema.md（会話・芝居）の対になるファイル
- platforms/model-quirks.md の Seedance 節から seedance.md への導線を1行追加。
  **既存2行は model-quirks.md 側に残した。** あちらは自社の実体験、seedance.md は未検証の
  推測で、性質が違うものを混ぜると model-quirks.md の信頼度が薄まるため
- principles/typography.md の「atmospheric-fx.md は未着手」注記を解除

## 2026-08-27（差分 5／6／7）

- 追記：principles/shot-structure.md に制御の階層化、全ビート共通の再固定、編集文法の宣言、
  終端状態、運動の途中から始める、4秒の3相分割、進行を証明するアングル、位相ロック、
  同種2体の4軸固定、force evidence、参照の役割つき管理と `only` による支配範囲の限定
- 追記：principles/directing.md に努力と負荷（行為／難易度／身体症状／感情トーンの4層）、
  二人芝居の5層と圧迫構造、肌のリアリズム、音声指定の3点セット
- 追記：principles/guards.md に失敗の12分類、多参照シーンの統合条項、世界の複製を止める、
  破局的な事象の増幅を止める、同一プロジェクト内の要素の混入を止める、性的なドリフトを
  止める（画側・音側・状況で定義する）
- **条件が付いた**：「抽象語は数えられる条件に翻訳する（数で固定する）」を無条件則として
  書いていたが、高速で交差しブラーのかかる多体では狭い許容幅（bounded count）のほうが
  実用的である可能性が出た。既存則は捨てず、**対象が読めるかどうかで分岐**させた。
  自社未検証。検証は「5体か6体」を「正確に6体」に変えて比較すれば生成2回で切り分く
- **条件が付いた**：末尾処理を NEGATIVE GUARDS の一択として書いていたが、否定群を持たず
  末尾に肯定形の再固定（POSITIVE LOCKS）だけを置いて成立している例が見つかった。
  「末尾に置く」原則は同じで、**そこに置くものが否定とは限らない**という追加。
  使い分けの基準は自社未検証。検証は末尾の再固定を消して比較すれば生成2回

## 2026-08-27（差分 3／10／11）

- 追加：principles/typography.md（文字の設計）。テロップ・タイトルカード・
  モーションタイポグラフィを独立した知見層として立てた。**大半が外部情報で自社未検証**
- 追記：templates/character-sheet.md にシートの5層（Identity → Body → Wardrobe →
  State → Packaging）、理想形のレイアウト、model-facing sheet と production card の分離
- 追記：templates/shot-schema.md に文字カードのショット記述、ASSETS台帳の状態違い欄と
  文字アセット欄、参照の支配範囲
- **覆した**：シート右半分に3つの study（正面ポートレート／横顔／目のクローズアップ）を
  置く構成をやめ、顔のクローズアップ1枚にした。あわせて全身側を headless にした。
  旧根拠は「目のクローズアップが高解像度アンカーになる」（corpus/character-sheet-zjasmin）、
  新根拠は「シート内に顔が複数あると動画モデルがドリフトする」（Higgsfield公式）。
  **どちらも同一ソース由来で、どちらも自社未検証。** 高解像度アンカーは別シートへ逃がせば
  両立するため顔1枚を採った。次の案件で両形式を比較する余地は残る（切り分けコストは
  画像生成2枚分）
- 差分に含まれていた第三者シートの記載文（英文）は、**リポジトリの規則に触れるため雛形の
  形に置き換えて反映**した。原文は video-corpus 側に置く

## 2026-08-27（差分ファイルの型）

- 追加：DIFF-FORMAT.md。知見を持ち込むための受け渡し形式を固定した。
  ヘッダ（使用LLM・Web検索の有無・リポジトリの現物を見せたか）、項目ごとの6要素
  （主張／出所／検証状態／適用範囲／配置先／既存項目との関係）、数字を扱うときの規則、
  他のLLMへ渡す指示文、受け取り側のチェックリスト
- **差分を作るたびに元の会話から書き起こすため、前の差分で一般化した語が再び入り込む**
  という問題が4回続けて起きた。形式を決めて出所と配置先を差分側に書かせることで、
  受け取り側の確認を毎回の勘に頼らない形にする
- CLAUDE.md の「知見の取り込み」から DIFF-FORMAT.md を参照。反映前チェックを規則化し、
  **変更履歴や理由の説明文も検査対象**であることを明記した
- DIFF-FORMAT.md のヘッダに「publicに出す想定か」を追加。出所・適用範囲・配置先は
  形式で埋まるようになったが、**publicに出せるかの判定だけは差分の書き手にしか
  できない**。書き手ほど案件の中身に近く、混ざっていても気づきにくいため、
  欄として固定した

## 2026-08-26（差分その4）

- 追加：genres/（ジャンル別の知見）。**principles/ の一部にジャンル固有の内容が
  混ざっていることに気づいたため、分ける枠を先に作った。** 普遍則とジャンル則が
  混ざったまま増えるのが一番まずい
- **genres/ の実体は video-projects（private）に置くと決めた。** 差分その4で用意した
  crime-mystery.md は案件固有の記述が密で、一般化するとファイルを作る意味が消える。
  ジャンル別の知見は具体性が価値なので、
  **一般化ではなく置き場所で解決する**。public 側は枠と目次のみ
- 追記：OPERATIONS.md に制作の相場と歩留まり、クライアント承認の設計。
  **いずれも他社の実績値と業界標準で、自社では未検証。** 現行の進め方との差分表を併記
- 追記：principles/workflow.md の制作フローに未検証である旨の注記
- **数字の根拠を得た**：「アセットを先に作る」判断に裏付け。上流で固定すれば1ショット
  3生成、固定しなければ8〜10生成に膨れる（他社の制作記録）

## 2026-08-26（差分その3）

- 追加：principles/sound.md（音の設計）、principles/vertical.md（縦型9:16の構図）
- 追記：principles/workflow.md に冒頭3秒の設計、謎型と人物型の選択、編集の5つの破綻と処方、
  AIスロップは工程の失敗
- 追記：principles/shot-structure.md に通過・変質系のカメラ技法
- 追記：templates/character-sheet.md に服装の節
- 追記：OPERATIONS.md に保留中の検討事項（知見のSkill化、30秒ティザーのフォーマット確定）
- **覆した**：音楽を「1本の完成したトラックを探すもの」として扱っていたため、生成と選定の
  ループから抜けられなかった。探すのは起伏のない「床」だけで、ライザー・ヒット・ブラーム等は
  ライブラリで揃えて使い回す
- **覆した**：9:16の指定さえすれば縦構図になると考えていた。アスペクト比の指定と構図の指定は
  別物で、上中下の三分割で場所を書く必要がある
- **覆した**：冒頭を静かな画で始めていた。最初のフレームに動きがないと低エネルギーとして
  読まれる。ただし極小カットの連打などは文芸作品のトーンを壊すので、取る原則と捨てる原則を
  分ける
- **訂正**：「Higgsfield公式がSeedanceプロンプト用のClaude Skillを配布」は誤り。公式Skill
  9本の中身は生成API操作で、Seedanceのプロンプト作法は公式ブログ記事。Skill形式のものは
  有志製（platforms/higgsfield.md と OPERATIONS.md の両方を訂正）
- 差分その3の README 追記（sound.md / vertical.md の個別行）は見送り。2026-08-27 に
  構成節をディレクトリ単位へ整理したばかりで、個別ファイルを再び混ぜると整理が巻き戻るため

## 2026-08-27

- CLAUDE.md と OPERATIONS.md の重複を役割で解消。CLAUDE.md＝このリポジトリでの
  作業規則、OPERATIONS.md＝制作事業の運用。月次棚卸しの実施内容は OPERATIONS.md へ、
  public/private の判断は CLAUDE.md へ寄せた
- README の構成節をディレクトリとルート直下ファイルの2ブロックに分けた
- platforms/magnific.md の「2026-08-25〜」を月単位に丸めた。日付の粒度から
  案件のスケジュールが推測できたため。**platforms/ の確認日は日付のまま残す**
  （仕様がいつ時点のものか分からなくなるほうが実害が大きい）
- 追記：platforms/magnific.md に「2系統目（未定）」。OPERATIONS.md が
  「本番の生成手段は常に2系統持つ」としているのに実体が無かった。
  **決まっていないことを決まっているように書かない**

## 2026-08-26（差分その2）

- 追加：templates/asset-card.md（物アセットカードの型）
- 追記：principles/workflow.md にThink工程の制約セットと、文字作品から画を見つける手
- 追記：principles/shot-structure.md にアセット方式とファーストフレーム方式の比較、
  カメラモーションとVFXの一覧
- **覆した**：全カットで「静止画を作ってから i2v」を既定にしていたが、
  公開プロンプト6本を確認したところ composed な開始画像を渡している例は
  1本も無かった。開始構図は**文章で**固定し、画像では固定しないほうが
  カメラを動かせる。ファーストフレーム固定は、構図そのものが要点のカットに限る
- **覆した**：物の指定を単語1つで済ませていた。意味を担う物は人物と同格の
  アセットとして作る

## 2026-08-26（差分その1）

- 追加：principles/workflow.md（発想と制作の工程）、OPERATIONS.md（運用メモ）。
  初版で principles/ が「プロンプトの書き方」だけになっており、**その前段の工程が
  どこにも無かった**。この状態だと制作の頭でThink工程が飛ばされる

## 2026-08-26（構成変更）

- **corpus/ をこのリポジトリから分離した。** 収録していたのは第三者（Higgsfieldコミュニティの6名）が書いたプロンプトの原文で、publicリポジトリに他人の著作物を置く形になっていた。private の video-corpus に移し、`.gitignore` に `corpus/` を追加。履歴からも除去した
- これに伴い README の構成節・書き方のルール4・入れないものを更新
- principles/ の各項目にある `(corpus/xxx)` 参照はファイル名のみなので維持。原文の実体は video-corpus 側
- `principles/directing.md` の英語引用のうち、1文以上の長さだった2件を日本語の説明に置換

## 2026-08-26

- 初版。実写ティザー案件の制作を通じて得た知見と、Higgsfield公開プロンプト5本の分解を収録
- **覆した**：「1生成4〜5秒」は普遍則ではなくHailuo H3の制約だった。Magnific確認では多くのモデルが4〜15秒、マルチショットは1生成で最大6ショット
- **覆した**：「否定形は使わない」→ 否定は「対になる肯定」とセットなら有効。単独の否定が問題
- **覆した**：人物を静止させる指示 → AIっぽい死んだ顔の原因だった。瞬きと眉の微細表情を能動的に指定する
