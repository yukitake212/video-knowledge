# CHANGELOG

覆った知見はここに理由とともに残す。git logは変更を記録するが、**なぜ覆ったか**は残らない。

## 2026-08-30（構成論の棚が無い・地図として反映）

差分1本（`repo-gap-structure`）。**「何が分かったか」ではなく「何が無いか」を書く差分。**
差分自身が「そのまま本体へ写すのではなく、次に何を読むべきかの地図として使う」としており、
**その指示どおり、二次資料の要約は一切書いていない。**

**受け取り側で欠落を再確認した**（2026-08-30）。指摘は正確だった。

- `principles/` は13ファイル。**予告編そのものの構成論・編集論を主題とするファイルは0**
- `workflow.md` の出所は Higgsfield Academy が2件、AI映像制作の実務記事が1件、
  あとは自社の整理。**映画・映像の専門文献は0件**
- **差分が挙げていない、もっと鋭い形が見つかった。** Skill の工程2「構成・カット表」が
  読むものは `templates/shot-schema.md`（型）と `sound.md` `typography.md`（要素別）。
  **5工程のうち、自分の原則ファイルを持たないのは工程2だけ**

**新規ファイル**

- `principles/trailer-structure.md`（88行）— **中身はほとんど空。空であることを見せるための
  ファイル。** 欠落の記録、断片がどこに間借りしているか、用語と原典、読むべき文献、
  埋め方の規律。**二次資料しか見ていない用語（枕ショット、カーナンの3訴求）は、
  名前と原典だけを書き、要約は書いていない**

**今すぐ使える1件**

- `genres/README.md` の2軸に**「既知性への訴求」が無い**。書籍なら著者・原作・シリーズ・前作。
  実際に案件のエンドカードは前作への訴求を入れていたが、**設計としてではなかった**。
  **これは文献を読まなくても効く**ので、引きの型の3つ目として並べた

**追記**

- `principles/workflow.md` の「編集の5つの破綻」に、**この節が本来このファイルのものではない**
  ことを書いた。**移すのは中身が増えてから。今移しても場所が変わるだけ**
- `skill/SKILL.md` 工程2 に、構成論の原則ファイルがまだ無いことを明示

**ついでに直したもの**

- `skill/SKILL.md`「静止から始める場合」に **「差分9番が未反映ならこの節は無い」**という
  古い但し書きが残っていた。`workflow.md`「静止から始めてよい場合」は既に存在するので削除

**書誌の確認**

- Walter Murch, *In the Blink of an Eye: A Perspective on Film Editing* を一次で確認
  （Silman-James Press、初版1992、第2版2001、Coppola の序文つき）。
  差分が「Claude は書誌を確認していない」としていた1冊

## 2026-08-29（切り出された台詞の演技・11項目中9件を反映、2件を保留）

差分1本（`dialogue-intonation`）。**このリポジトリで初めての、外部研究が主体の差分。**
出発点は ChatGPT の回答で、そこに挙がった文献を差分の作成側が一次情報に当たって検証している。

**受け取り側で引用の実在を再確認した**（2026-08-29）。差分に書かれた**3件はいずれも実在し、
主張の内容も一致した。**

| 引用 | 確認 |
|---|---|
| Regev et al. (2025) ACL / arXiv:2503.11630 | 題名・著者8名・**過去3〜8語／未来1〜2語**まで一致 |
| Michel, Epure, Cerisara (2026) Findings of ACL / arXiv:2509.04072 | 題名・著者・**LibriQuote 5.3K時間・speech verb と adverb の疑似ラベル**まで一致 |
| ElevenLabs `previous_text` / `next_text` | 公式APIリファレンスに存在。**読み上げられず連続性にのみ影響**することを確認 |

**新規節**

- `principles/directing.md`「切り出された台詞に演技を与える」— **実写の予告編では音声ごと
  切り出されるが、AI映像では工程が逆**という問題の立て方。以下を含む
  - **業界はADRのキューシートで解いている。** 映像／元の音声／キューシート（文脈と感情の
    ノート）を渡す。**取り除かれた文脈を、文章にして俳優へ戻している。**
    既存の「音声指定は内容・声色・禁止事項の3点セット」には**文脈が入っていない**
  - **本編どおりの演技を再現すればよいわけではない。** 予告編には文脈が2つある
    （Source performance × Edited context）。**差分の作成側がこれを一番価値のある部分として
    挙げており、同意する**
  - **抑揚のスロットが空欄のまま運用されていた。** `wan.md` の式に抑揚があるのに、1案件で
    速さと調子しか埋めなかった。**式が使われない理由は、埋め方が書かれていないことかもしれない**
  - 次に確かめること — Performance Context という中間表現の提案。**差分の指示どおり、
    検証するまで手順としては書かず、実験計画としてだけ置いた**
- `principles/sound.md`「読ませない文脈を、読ませるテキストの隣に置く」— ElevenLabs の
  `previous_text` / `next_text`。**ADRのキューシート・LibriQuote の疑似ラベル・このAPIが
  同じ形に収束している**
- `principles/sound.md`「声の演技についての参照文献（外部・自社未検証）」

**追記**

- `models/video/wan.md` の声の式に、抑揚が埋められていない旨の注記

**反映しなかったもの（差分の指示どおり）**

- **「音声側の情報量がテキストより一桁以上大きい」（ACL 2026）** — ChatGPT の回答に
  あった主張だが、**差分の作成側が該当論文を特定できなかった。** 論文が特定できるまで書かない
- **Higgsfield が演技指示を書かせる方向へ進んでいる（4件）** — ChatGPT の回答由来で
  **一次情報が未確認。** `DIFF-FORMAT.md` の「外部調査由来のツール候補が入っていないか」に
  直接あたるため、**flag 付きでも public には置かない。** 自分のアカウントで公式資料を
  確認してから入れる

**受け取り側で強めたこと**

- **Regev et al. の誤引用に対する注意を、箇条の中で一段強く書いた。** この論文が測っているのは
  **語単位の局所窓**であって「数分間の出来事が声に残る」規模の主張ではない。
  **このリポジトリの問題設定（俳優の経験まで生成する）は、まさにこの論文を誤って
  引きたくなる形をしている。** 差分がその危険を自分から書いていたので、そのまま採った

## 2026-08-29（成功プロンプトの実物と、制御しすぎの回避・5項目）

差分1本（`channels-and-control`）。**1件を伏せて反映した。**

**public に出す前に伏せたもの**

- 実例プロンプトに**原作の台詞が原文のまま入っていた。** 差分のヘッダは
  「クライアント名・書名・著者名・キャラクター名は除いてある」としていたが、**台詞は
  除かれていなかった。** 同じ差分のもう1本（タイトルカード）は `"（告知の一文）"` の形で
  伏せてあり、**片方だけ抜けていた。** 台詞を `「（台詞）」` に置換して反映し、
  原文は `video-corpus/diffs/` にのみ残した

**新規節**

- `principles/sound.md`「画面に出る文字と、音声で流す台詞は別チャンネル」—
  **吹き出しのないコマは音声チャンネルが空いている。** 画面は原稿のまま保ち、意味とテンポの
  補いを音だけで行うと、「文言を出すと訴求方針と衝突する／出さないと理解できない」が両立する
- `OPERATIONS.md`「クライアントの指摘は、指摘された範囲だけを直す」—
  縦横比の破壊についての指摘を「生成側でカメラを動かさない」まで広げ、
  **その案件で最も価値のあった手法を最初に捨てていた**
- `platforms/model-quirks.md`「成立した実例 — ページ → コマへの寄り＋飛び出し」
- `models/image/nano-banana.md`「成立した実例 — 日本語の縦型タイトルカード」

**既存への統合**

- `principles/prompt-compilation.md` の「長い禁止の列が効いていない可能性」に、**書き方の
  指針**を足した。**否定を1つ書くたびに「そこは壊れる」という前提を成果物に書き込んでいる。**
  まず短く書いて、壊れたところだけを1箇所ずつ足す。差分が統合を指示していた
- `principles/still-to-motion.md`「発生を描くなら、発生前の絵を探す」に**第3の手**を足した

**受け取り側で足した読み**

- **「発生は描けない」（既存）と「飛び出しが成立した」（今回）は矛盾していない。**
  成立した側は**飛び出しの動作を作り込んでいない。** ページから下段のコマへ寄る
  カメラ移動を書いただけ。**モデルの挙動は変わっておらず、発生そのものは今も描けない。**
  変わったのは、被写体を大きくするしかなかったスケール変化に**カメラの接近という理由が
  与えられた**こと。**失敗時の「巨大化」と成立時に起きていることは同じで、動機づけの
  有無だけが違う。** 選択肢を3つ（発生前の絵／カメラで代替／諦める）として書いた
- **「1000語で3回破綻」は、これで3本目の差分に出てきた同じ3回。**
  `h3-keyframes`（FL2VAのジャンプ）、`h3-camera`（長いプロンプト）、今回（制御しすぎ）。
  `prompt-compilation.md` に既に「競合仮説であって独立した証拠ではない」と書いてあるので
  追記のみに留めた
- 運用の「指摘を広げない」と、プロンプトの「禁止を広げない」（`guards.md`）を相互参照で
  繋いだ。**根は同じ**

## 2026-08-28（同案件の後半・21項目）

差分3本（`endcard-sound` / `h3-camera` / `text-budget`）。エンドカード、H3のカメラと音声、
画面文字の読字予算。**1項目は重複のため反映せず、2項目は差分の判断を変えた。**

**覆したもの**

- `principles/typography.md`「日本語の責任分配」— **「日本語だから生成させない」を取り下げた。**
  現行の画像生成モデルは日本語の字形を正確に出せる。**この矛盾はリポジトリ内に既にあった。**
  `platforms/model-quirks.md` の自社検証（「日本語の文字を正確に出せる」）に対し、
  typography 側の**未検証の外部方針**が強く書かれたまま残っていた。
  ただし**どちらの記録もモデル名・時期・回数がない**ので、その旨を明記した。
  同節の「文字デザインと動きを同時に一発生成しない」は別の理由で有効なので残した
- `platforms/model-quirks.md` / `principles/still-to-motion.md` — **「素材を最終解像度に落とすと
  ズーム余地はゼロ」から「だから寄りは別クロップ」へ繋がる処方を、唯一の手ではなくした。**
  消えるのは編集側の自由度だけで、**生成側でカメラは動かせる。**
  差分は model-quirks だけを覆す対象に挙げていたが、**同じ記述は still-to-motion にもある。
  両方直した**

**新規節**

- `principles/still-to-motion.md`「素材に焼き込まれた文字は、読字予算になる」— 20カット構成で
  割当秒数に対し**2倍から10倍の超過**。最長カットは61字に1.5秒。12カットへ削減した。
  図解ページ優先、通過するコマは予算に入らない、も同節
- `principles/typography.md`「エンドカードは分割しない」「免責表示は点滅させず、対象ブロックを
  通して出す」
- `principles/sound.md`「音も表現ルールの対象」
- `OPERATIONS.md`「構成を確定する前に数える」— カット数・秒数・文字量。
  **画面に出る文字だけを通しで読む**検証（記憶で行わない）、構成パターンに属さないシーンを
  残さない、も同節
- `OPERATIONS.md`「素材を依頼する段階で、帯なしの版があるかを訊く」— **高解像度の書影を
  入手した時点で、低解像度では読めなかった帯の文言が読めるようになる**
- `models/video/minimax-h3.md`「話し方の指定をどこに書くか」
- `principles/prompt-compilation.md`「1件の観測 — 長い禁止の列が効いていない可能性」

**既存への追記**

- `platforms/model-quirks.md` H3節に4項目。**カメラを動かしても縦横比は壊れない**
  （3つの明示が条件）／**画面の日本語セリフを読み上げる。品質は実用に足りた**／
  塞ぐか使うかは方針で決まる／音の指定を足したときの副作用は未確認
- `principles/sound.md`「生成→選定ループ」に**同主調で床を2本作って差し替える**
- `models/image/nano-banana.md` に**「空けろ」だけでは空かない**

**受け取り側で変えたこと**

- **「床はカット単位で作るものではない」は反映しなかった。** 同日の `h3-keyframes` に同じ項目が
  あり、`236378a` で既に `sound.md` へ入っている。差分自身が重複確認を求めていた
- **読み上げの扱いが、同日の差分どうしで反転していた。** `h3-keyframes` は「読み上げうるので
  音のフィールドを埋めて塞ぐ」、`h3-camera` は「省くと読み上げる。品質は実用に足りたので
  資源になる」。**矛盾ではない——指定すれば止まり、省けば読む。** 方針で決まると書き分け、
  **事故になるのはどちらでもない中間（埋め忘れ）**だと明示した
- **「長いプロンプトは効いていない」の3回は、`h3-keyframes` の「FL2VAでジャンプする」と
  同じ3回。** 「版面差が原因」と「プロンプトの長さが原因」は**独立した2件の証拠ではなく、
  同じ観測に対する競合仮説。** 片方を根拠に片方を補強しない、と書いた。
  切り分け実験（同一カット・同一素材で長短各3回）は**1つで両方に効く**
- **「毎秒4文字」は保留せず、出所不明と明記して書いた。** 差分は `DIFF-FORMAT.md` の
  「数字には誰が測ったかを書く」に照らして保留を推奨していたが、**この数字を抜くと
  読字予算の節が運用できなくなる。** 文字数の実測（61字／20カット計数／12カットへ削減）は
  自社検証なのでそのまま書き、**秒あたりの変換値だけが出所不明**であることを節内に明示して、
  一次資料の調査を「検証したいこと」に立てた。「毎秒9〜10モーラ」も同じ扱い

## 2026-08-28（漫画原稿を素材にした実案件からの反映・18項目）

差分3本（`handoff` / `h3-keyframes` / `manga-source-prep`）。素材処理・H3の実挙動・受け渡し事故。

**新規節**

- `principles/still-to-motion.md`「素材の物理仕様を先に測る」— 解像度・ビット深度・網点線数を
  実測すると、出力解像度・ズーム上限・モアレ域が**算術で決まる**。1件の実測値を表で置いた
- `principles/still-to-motion.md`「網点素材の縮小」— **一律ディスクリーンをやめる。**
  面積平均で縮小すれば無処理でも網点は平坦に落ちる。ぼかしは
  **リサンプラーを制御できない工程（NLEのズーム・パン）にだけ弱くかける**
- `principles/still-to-motion.md`「先に、素材に文字なし版が隠れていないか調べる」—
  校了PDFは絵柄ラスタ／写植テキストに分かれていることがあり、**clean plate 工程が丸ごと消える**
- `principles/still-to-motion.md`「発生を描くなら、発生前の絵を探す」— I2Vは結果が描かれた
  素材から発生を遡れない
- `principles/vertical.md`「縦パンは、素材の高さがフレームを超えていないと成立しない」
- `principles/sound.md`「静止画素材では、声の手掛かりが画に残らない」
- `platforms/magnific.md`「出力の実効値」— **H3の出力尺が5.166667秒（24fpsで124フレーム）に
  固定**。モデル仕様の「4〜15秒」とは別レイヤーの話
- `OPERATIONS.md`「生成プロンプトと素材の受け渡し」— `.md` 経由のアンダースコアのエスケープ、
  zipの日本語名でフォルダ構造が壊れる件、画質判断の手段

**既存への追記**

- `platforms/model-quirks.md` H3節に4項目。FL2VAで**最終フレームへはジャンプで着地する**
  （3回とも共通、モーフィングには逃げない）／I2VAの巨大化／音の指定は画への干渉を止めるため
- `principles/still-to-motion.md`「2枚の間を補間する」に**効く範囲の限定**。
  中間動作は取れるが着地は取れないので、**決めポーズへの着地は編集で持つ**
- `principles/guards.md` に**禁止を広げすぎる副作用**。変倍（比率の破壊）と拡大（比率を保つ
  スケール）を混同して拡大を全面禁止すると、遠近として正しい動きまで潰れる
- `principles/sound.md`「環境音は連続性の担保」に**これは編集工程の指示**という但し書き
- `OPERATIONS.md` 承認設計の節に「**承認された資料がその段階の要件を満たしているとは限らない**」。
  節のヘッダが「自社未検証」なので、**サブ節を分けて自社検証と明示した**
- `DIFF-FORMAT.md` のエスケープ検査の適用範囲を、**入ってくる原文だけでなく出ていく
  プロンプトにも**広げた

**受け取り側で直したもの**

- 差分は「素材段階での一律ディスクリーンは**既存を覆す**」としていたが、
  リポジトリ本体を検索した結果、`ディスクリーン` `descreen` は**0件**で、その方針は明文で
  入っていなかった。**新規として入れた**（差分自身が「入っていなければ新規」と自己申告済み）
- 「検証の指標を作るより目視比較のほうが早い」は**そのままだと危ない**ので条件を付けた。
  同じ案件に**逆の事例**がある。σ1.2 のプリブラーが適用済みかの事実確認は目視では決まらず、
  数値でしか確定しなかった。**しかも最初に選んだ指標が現象に対して不適切で誤判定を出した。**
  「良く見えるか」は目視、「適用されたか」は数値、と分けて書いた
- FL2VAのジャンプについて、**既存の「1生成内でのハードカットが成立する」と同じ性質ではないか**
  という読みを推測として併記した。切り分け実験はしていない
- 網点1セル 10.06px は実測値。**公称は `dpi ÷ 線数` = 10.0** なので両方書いた。
  σ の目安を**セル幅 × 0.35 前後**として一般化した
- 中間倍率の未検証（0.347倍と1.00倍の両端しか見ていない）を**「分かっていないこと」として
  明示**した。ズームはその間を連続的に通過する

## 2026-08-28（工程1が圧縮される問題）

- **実運用で工程1の手順が走らないことが分かった。** 制約を1つしか使わず、20案で止まり、
  ★で評価して絞り込む、という出力になった。**「Skillを最初から最後まで通して」と明示すれば
  走るが、普通の依頼では走らない**
- 原因は2つ。**ユーザーが「20個出して」と件数を指定すると、それを総数として読んで
  「制約ごとに20案」が潰れる。** そしてスキル側に、件数の衝突をどう読むかの記述が無かった
- 対策：工程1の手順の冒頭に**実行表**を置き、発散前に「使う制約3つ以上／1制約20案／
  総件数＝制約数×20／制約ごとに分離／評価禁止」を固定させる。
  **ユーザー指定の件数は1制約あたりの数として読む**と明記
- **「1つ目の制約で20案出すと、それらしい分量になるので終わった気になる」**を
  最頻の失敗として本文に書き、総件数の検算をチェックリストに追加した

## 2026-08-28（工程4Cのルーティング修正）

- **バグ修正**：SKILL.md 工程4C が `templates/house-style-live-action.md` を「常に読む」に
  していた。**あのファイルの本文は `photorealism` と `NOT a cartoon` を含む。**
  漫画原稿を素材にするカットに貼ると、素材の画風と正面衝突する。
  「実写調で作る」ときだけに条件付けし、**素材が原稿・イラストのときは貼らない**行を追加した
- あわせて `templates/house-style-live-action.md` の冒頭に適用条件を明記。
  素材があるカットでは look の正本は素材のほうで、ハウススタイルブロックは貼らない
- 4Dのチェックリストにも「素材が原稿・イラストなら貼っていないか」を足した
- **発見の経緯**：ChatGPT に SKILL.md を読ませて漫画原稿案件を一周させたときに出た指摘。
  リポジトリは `genres/README.md` で実写調とアニメ調を分けていたのに、
  **SKILL.md のルーティングがその軸を使っていなかった**

## 2026-08-28（自社検証が外部情報を上書きした2件）

- **覆した**：`principles/still-to-motion.md` の「読める文字を生成中ずっと保持させない」を
  条件付きに修正。**元の記述は外部の実務記事に基づく自社未検証のもので、H3で漫画コマを
  動かした自社検証がそれを上書きした。** 軽い微動の範囲では文字を残したままで破綻しない。
  clean plate 化は必要なコマだけに限定する。あわせて **clean plate 化が原作から遠ざかる操作
  でもある**点（自由度と原作忠実度のトレードオフ）と、外すときの実体が「消す」ではなく
  「埋める」である点を追記した
- **条件が付いた**：`platforms/model-quirks.md` の「寄りカット用のクロップ素材の準備工程は
  不要」は、**生成側にハードカットを割らせる前提での結論だった。** 固定フレーム＋微動に振り
  カットを編集側で割る方針では逆になる。**素材を最終解像度の台紙に落とした時点で、編集側の
  ズーム余地はゼロ**（等倍でもアップスケールになる）。寄りは素材段階でクロップを別に作る。
  ズーム余地の消失は解像度からの帰結であって生成の検証ではない、と出所を分けて書いた
- 追記：`platforms/model-quirks.md` に「漫画コマは吹き出しと擬音を残したままでも破綻せず
  動く」（自社検証）
- 訂正：OPERATIONS.md の public/private 判断で「自社検証＝model-quirks.md」と読める書き方に
  なっていた。**`principles/` にも「（自社検証）」タグの項目が散在しており、
  `shot-structure.md` だけで12件、全体で22件ある。** 行数ではなくタグで数えるよう直した

## 2026-08-28（public/private の見直しを棚卸しへ）

- 追記：OPERATIONS.md「リポジトリを private にするか」。**判断せずに先送りするのではなく、
  月次の棚卸しで見直す形にした。** 目安は3つ（自社検証の分量が外部情報に近づく／
  クライアントから問われる／事業の資産として扱い始める）
- **現時点の判断は public のまま。** 自社検証34行に対して外部情報1034行で30倍の開きがあり、
  他人の調査で辿り着けるものが大半のうちは公開しても失うものが少ない。
  逆に public であることには「認証なしで clone できる」という実利がある
- private にする前に、**代替の受け渡し手段を先に決める**ことを明記した。
  現在の「差分を外で書いて中で反映する」分業が成立しなくなるため

## 2026-08-28（Phase B：画像側の知見）

**画像生成の知見は実質13行しか無かった。** `model-quirks.md` に3項目と `shot-schema.md` の
型1つだけ。一方 `shot-structure.md` は「画像生成の回数は案件全体で3〜7枚」と書いていて、
**上流固定の中心が画像生成**。投資対効果が一番高いところが一番薄かった。

- 追加：principles/image-production.md。**「アセット作成」1工程では粗すぎる。**
  画像タスクを12に分類、探索と本番を分ける、画像の3分類（証拠／方向／最終フレーム）、
  **動きやすい画という評価軸**（画として美しいことと動画の起点として良いことは別）
- 追加：principles/image-repair.md。**WHERE / WHAT / PRESERVE の3つを分けて書く。**
  PRESERVE が抜けるのが最大の失敗。OpenAI と Google の公式ガイドが独立に同じことを
  言っている（両方とも実物確認済み）。専用ツール → 局所編集 → 全体再生成の順に検討する
- 追加：principles/world-assets.md。場所を階層で持つ。**既存の「main / reverse の2枚」を
  否定せず、上流と下流を足した。** 30秒単発では2枚で足りる
- 追加：models/image/ に6本。gpt-image / nano-banana / seedream / krea-2 / soul / midjourney。
  **gpt-image と nano-banana と krea-2 は公式ドキュメントの実物を取得して確認**（Tier A）
- **ホストとモデルの分離を実際に適用した。** platforms/higgsfield.md から Soul ファミリーを
  models/image/soul.md へ、platforms/krea.md から Krea 2 を models/image/krea-2.md へ。
  Phase A で作った構造がここで効いた
- 追記：platforms/higgsfield.md に Higgsfield Edit と Cinema Studio 4.0。
  **4.0 は制御がプロンプトからUIへ大きく移った。** 感情・照明・色・カメラが選択項目になる。
  既存則が間違いになるのではなく、prompt-compilation.md の分類Bに該当する項目が
  このホストでは多い、ということ。**なお解像度と尺について公式3ソースが食い違っている**
- 追記：principles/prompt-compilation.md の分類Bを画像側へ拡張。
  **プロンプトに書くべきことが他の経路へ移っていく**というのが、
  「知見が増えてもプロンプトは長くならない」のもう1つの理由
- **SKILL.md の工程3を「アセット作成」から「画像」へ改称し、3A〜3Dに分解。**
  工程4と同じ構造にした。従来はモデルを聞いていなかった
- **未解決を記録**：肯定形と否定形の扱いが Google と OpenAI で食い違う。Google は
  「no cars ではなく empty street」、OpenAI は「no watermark, no extra text を明示的に書け」。
  既存の guards.md は動画側の corpus 由来。**画像では否定の扱いがモデルによって違う可能性**
- Midjourney は `models/image/midjourney.md` として作成。差分の推奨は candidates.md への
  縮小だったが、**candidates.md は「分業先のツール候補」であってベースモデルの棚ではない**
  ため、他の画像モデル5本と同じ扱いにした。契約の判断が付くまで
  「調べたが採用していない」記録として置く

## 2026-08-28（models/ の新設）

- 構造変更：`models/` を新設し、`platforms/` にあったベースモデルのファイル3本
  （minimax-h3 / seedance / wan）を `models/video/` へ移動。**ホストとモデルを混ぜない**
  という既存の教訓を、ディレクトリ構造に反映したもの。`platforms/` の862行のうち474行が
  実際にはモデルのファイルだった
- `build-skill.sh`：`DIRS` に `models/video` と `models/image` を追加。
  `find -maxdepth 1` のため、`models/` だけでは配下が拾われない
- 参照の更新：7ファイル。`CHANGELOG.md` は履歴なので更新していない
- **知識の追加はゼロ。** 画像側の知見は Phase B で入れる

## 2026-08-28（プロンプトの組み立て工程）

- 追加：principles/prompt-compilation.md。**知見が増えてもプロンプトは長くならない。**
  最終プロンプトは成果物であって原本ではない。情報の4分類（参照が持つ／プロンプト外で
  固定／言葉でしか伝わらない／失敗を塞ぐ）、MUST/SHOULD/MAY の削る順序、
  組み立ては最低3回ある（試し撮り・本番・修理、モデル変更で4回目）、出所の7段階格付け
- 追加：templates/shot-ir.md。**モデルを決める前に埋めるモデル非依存の仕様。**
  shot-schema.md の上流に立つ。既存の shot-schema.md に Seedance 向けと H3 向けの型が
  同居していたのは、**同じ仕様から変換した2つの結果**として説明できるようになった
- 追加：platforms/minimax-h3.md。**MiniMax 公式 Skill の実物から確認した項目を Tier A と
  明示。** モードを最初に決める（T2VA / I2VA / FL2VA / L2VA / Ref2VA で文法が変わる）、
  出力フィールドの順序、参照役割12種
- 追加：platforms/wan.md。6つの式と、**first/last frame と参照が排他**という制約。
  フレームを渡すときは「動き＋カメラの動き」だけ書く（still-to-motion.md と一致）
- 追記：platforms/seedance.md に公式の式・記号・参照の予算。**ファイルの性質が変わったので
  冒頭を書き換え**、公式の二次紹介と公開プロンプトからの逆算を分けた
- 追記：platforms/higgsfield.md に「プロンプトが内部で書き換えられる」。
  **自社のプロンプトの良し悪しを Higgsfield 経由の出力だけで判断できない**
- **条件が付いた**：「秒数は 0.0s から書く」を全モデル共通則として書いていたが、
  **そもそも秒を使うかどうかがモデル・バージョンで反転する**（Seedance 2.0 は番号方式を
  公式推奨、2.5 は秒指定が主機能）。既存則は「秒を使う場合の書き方」として残し、
  使うかどうかの判断を platforms 側へ分けた。二次資料であり自社未検証
- **未解決を記録**：ByteDance は「複数アングルを1枚のグリッドにまとめない」と述べているが、
  キャラクターシートの構成がこれに該当するかは不明。判断材料が足りないので変更せず、
  templates/character-sheet.md に比較の宿題として残した
- **SKILL.md の工程4を4段階に分解**（4A 仕様 → 4B 出力先 → 4C 知識を絞る → 4D 変換）。
  従来はチェックリスト10項目が全部「〜したか」の確認形で、**検証であって選択ではなかった。**
  全項目を満たそうとすると全部載せる方向にしか働かない。**モデルを聞いていなかった**のも
  同じ問題で、モデルが決まる前にプロンプトの形は決まらない

## 2026-08-28（原稿の入力ルールとプラットフォーム比較）

- 追加：OPERATIONS.md「クライアント原稿・第三者著作物の入力」。**預かった原稿は一般的な
  参照素材と分けて扱う。** 入れる前の4段階の確認、入力先を増やさない、加工しても素材の性質は
  変わらない、入力経路の記録、暫定の判断表。**公開資料の読み取りであって法的助言ではない**
- **既存の評価に条件が付いた**：platforms/higgsfield.md の判断メモは学習利用を
  「実害は限定的。エンタープライズの価値は説明できる根拠」と評価していた。
  第三者から預かった素材については**実害の大小と説明可能性を分ける**必要が出たため、
  既存の評価は残したまま「原稿は別。説明できるかで判断する」を足した。矛盾ではなく、
  既存メモ自身が説明可能性を価値としている
- 追加：OPERATIONS.md「制作プラットフォームの選択」。既存「リスク管理」は2系統確保を
  要求していたが選択の軸を定義していなかった。**モデル数で比べない。同一ショットで測る。**
  記録項目に「プラットフォーム間で素材を移した回数」を足した（移すたびに契約確認・
  アクセス管理・削除管理の対象が増えるため）
- 追記：platforms/magnific.md に契約・データ面、Spaces とワークフロー、料金。
  **学習に使わないことと、外部事業者が一切処理しないことは別条件。**
  画像・音声とプロンプト文章は同じ保護レベルではない
- 追記：platforms/higgsfield.md に Enterprise の料金体系と原稿入力の運用
- 追加：platforms/krea.md。2系統目の比較対象として個別ファイルに立てた。
  **通常プランのデータ利用条件は公開資料から確認できなかった。**
  「学習される」とも「されない」とも書かない
- **candidates.md の位置づけを変えた。** 「比較候補」として作ったが、制作基盤の候補と
  分業先のツール候補が同じ粒度で並ぶ状態だった。**分業先の候補に限定**し、基盤の候補は
  個別ファイルへ寄せた。あわせて DomoAI を落とした（Magnific が既に Seedance をホストし
  Spaces を持つため用途が重複）。候補の出所が外部調査であることを冒頭に明記
- DomoAI を落としたことで切れる参照2件を付け替えた。**higgsfield.md の比較表には出所列を
  足して値を行内で検証可能にした**（参照先から記述が消えても辿れるように）。
  seedance.md の参照先は higgsfield.md へ
- DIFF-FORMAT.md の受け取り側チェックに「外部調査由来のツール候補が入っていないか。
  **こちらの工程上の必要から探したものか**」を追加

## 2026-08-28（Skillの発火条件とprivate参照）

- **SKILL.md の description に静止画素材とSNS短尺を追加。** still-to-motion.md が独立した
  principles ファイルになったのに発火条件へ反映されておらず、漫画原稿を素材にする相談で
  Skill 自体が起動しない可能性があった。106→133文字（上限200以内）
- build-skill.sh に `--with-private` を追加。**video-projects/genres/*.md への絶対パスを
  skill-local にだけ埋める。** zip と ChatGPT 版には入れない。案件記述をアップロード先に
  乗せる判断は別途要るのと、OPERATIONS.md の「クライアント案件は必ずプライベート領域に置く」
  と衝突しかねないため
- SKILL.md の「案件の条件」3つ目を `<!-- GENRES -->` マーカー化し、版ごとに文言を差し替える
  形にした。**コピーではなく絶対パス参照**なので、private の内容は dist/ に一切出ない
- DIFF-FORMAT.md の受け取り側チェックに「原文を corpus へ入れるならエスケープ記号・
  強調記号の混入を確認したか」を追加。**今回は照合したから気づけたが、次に別の原文が来たとき
  同じチェックが走る保証が無かった**

## 2026-08-28（差分ファイルの保管先）

- **差分ファイルの受け取り前の置き場が定義されていなかった。** CLAUDE.md は「受け取ったら
  反映してコミットする」としか書いておらず、反映が済むまでチャットの出力にしか存在しない
  状態になっていた。実際に5本を渡し直してもらう事態が起きた
- `video-corpus/diffs/`（private）を新設し、既存18本を保管。**反映するより先に置く**運用に
  した。反映済みかどうかは CHANGELOG が唯一の判定先とし、diffs/ 側には書かない
  （2箇所で管理するとずれるため）

## 2026-08-28（差分 14／15／8／9／12）

- 追記：platforms/higgsfield.md に Shots、Popcorn の詳細、**Higgsfield 上の Seedance 統合**。
  **ホストの統合仕様とモデル本体の仕様を分けた。** 同じ Seedance 2.5 でもホストごとに
  案内される入力の内訳が違う（Higgsfield は最大50 multimodal、DomoAI は 30画像/10動画/10音声）。
  ホストの案内値をモデル仕様として書かない。「4秒以上」を H3 の制約と気づかず普遍則にした
  事故と同じ形になり得る
- 追加：platforms/candidates.md（未契約・未検証の比較候補）。DomoAI / Runway / Adobe Firefly。
  **個別ファイルにせず1本にまとめた。** 独立させると platforms/ に現行と候補が同じ粒度で
  並び、magnific が現行という区別が読めなくなる。切り出しの条件を明記
- 追記：principles/sound.md に「プロンプト内の音は二層」（全体の契約／その瞬間のキュー）と
  「音が何を証明しているか」（作品の性質ごとに音の役割を1つ決める）
- 追記：principles/workflow.md に「一発で作らず、段階に分ける」（動かない側を先に固定する）、
  導入をショットの外で済ませる、編集のペースと止まり
- **条件が付いた**：「冒頭は動きから始める」を無条件則として書いていたが、静止そのものが
  意味を持つ素材（漫画原稿など）では例外があり得る。**自社の失敗例と構造が同じ**
  （静止した被写体へのプッシュインで平均再生8秒・スキップ38.6%）で、違いは静止の後に
  何が起きるかだけ。成立条件を3つ付けたうえで例外扱いにした。自社未検証
- 追記：templates/vocabulary.md に漫画記号と大気・光の語彙。**変数の体系は
  atmospheric-fx.md、ここは対訳のみ**という分担を明記
- principles/still-to-motion.md の「差分9番で追加予定・未着手」注記を解除。
  参照先の節が生まれたため

## 2026-08-28（Skillビルド）

- 追加：build-skill.sh と skill/SKILL.md。リポジトリから Claude Skill / ChatGPT ナレッジを
  ビルドする。**配布版は reference/ にコピー、Claude Code 用は絶対パスで本体を直接参照**の
  2系統を吐く。案件フォルダで起動すると相対参照が切れるため
- **`pipefail` と `grep -q` の組み合わせで、閉じているフロントマターを「閉じていない」と
  誤判定する不具合を潰した。** grep -q が先頭マッチで即終了し、上流の tail が SIGPIPE(141)
  を受けて pipeline が失敗扱いになる。SKILL.md が 128KB 付近で発火する。実測で境界を確認。
  パイプを使わない形に書き換えた
- awk への値渡しを `-v` から `ENVIRON` へ。**`-v` はエスケープシーケンスを解釈する**ため、
  コミット件名の `C:	emp` が `C:<TAB>emp` になっていた。`&` の展開と同じ系統
- skill-local の sed 置換で `&` がエスケープされていなかったのを修正
- 参照切れ検査の `find | grep -q .` を `-print -quit` へ。同型が3箇所あって2箇所だけ
  直っている状態を避けた
- SKILL.md の鮮度の文言を `<!-- FRESHNESS -->` マーカー化。**ローカル版は常に最新なのに
  「ビルド情報より新しい変更は入っていない」と書いてあり、事実と逆だった**
- .gitignore に dist/ を追加
- OPERATIONS.md の棚卸しに「差分を反映してコミットしたら ./build-skill.sh を走らせて
  上げ直す」を追加

## 2026-08-28（差分 13／16）

- 追加：principles/still-to-motion.md（静止画素材を動かす）。**ほぼ全部が自社未検証。**
  書影・挿絵・提供写真・漫画原稿・先に確定させたシートなど、完成した静止画を素材として
  動かすときの工程・演出パターン・原作からの距離。検証項目7件つき
- **ジャンル固有ではなく素材の条件として立てた。** 元案は漫画ティザーとして genres/ へ
  入れる想定だったが、同じ問題が漫画以外の素材でも起きる。principles/ へ出したことで
  genres/ の2軸（物語作品／実用書 × 実写調／アニメ調）を崩さずに済んだ
- genres/README.md：現状表に sf-action.md を追加、practical-manga.md を private へ。
  末尾に still-to-motion.md への導線を追加
- video-projects（private）に genres/sf-action.md と genres/practical-manga.md を新設。
  **一般化すると価値が消える具体は private 側**という 2026-08-26 の決定に従った

## 2026-08-27（差分 1／2／4）

- 追加：platforms/seedance.md。**仕様書ではなく未検証の仮説置き場。** 他人の公開プロンプトの
  書きぶりから「作者が何を警戒しているか」を逆算した失敗モードの候補18件と、
  生成1〜2回で切り分けられる検証計画14項目。回した結果は表の右列に日付つきで書き込み、
  検証済みになったら model-quirks.md の自社検証側へ移す
- 追加：principles/atmospheric-fx.md。`heavy fog` としか書けない状態から抜けるための、
  大気の変数分解。Unreal / Blender / PBRT / Production Volume Rendering の物理モデルから
  語彙を取る形にした。**大半が外部情報で自社未検証**
- 追加：templates/action-sequence-schema.md。アクションのビート列の型（9ブロック）。
  shot-schema.md（会話・芝居）の対になるファイル
- platforms/model-quirks.md の Seedance 節から seedance.md への導線を1行追加。
  **既存2行は model-quirks.md 側に残した。** あちらは自社の実体験、seedance.md は未検証の
  推測で、性質が違うものを混ぜると model-quirks.md の信頼度が薄まるため
- principles/typography.md の「atmospheric-fx.md は未着手」注記を解除

## 2026-08-27（差分 5／6／7）

- 追記：principles/shot-structure.md に制御の階層化、全ビート共通の再固定、編集文法の宣言、
  終端状態、運動の途中から始める、4秒の3相分割、進行を証明するアングル、位相ロック、
  同種2体の4軸固定、force evidence、参照の役割つき管理と `only` による支配範囲の限定
- 追記：principles/directing.md に努力と負荷（行為／難易度／身体症状／感情トーンの4層）、
  二人芝居の5層と圧迫構造、肌のリアリズム、音声指定の3点セット
- 追記：principles/guards.md に失敗の12分類、多参照シーンの統合条項、世界の複製を止める、
  破局的な事象の増幅を止める、同一プロジェクト内の要素の混入を止める、性的なドリフトを
  止める（画側・音側・状況で定義する）
- **条件が付いた**：「抽象語は数えられる条件に翻訳する（数で固定する）」を無条件則として
  書いていたが、高速で交差しブラーのかかる多体では狭い許容幅（bounded count）のほうが
  実用的である可能性が出た。既存則は捨てず、**対象が読めるかどうかで分岐**させた。
  自社未検証。検証は「5体か6体」を「正確に6体」に変えて比較すれば生成2回で切り分く
- **条件が付いた**：末尾処理を NEGATIVE GUARDS の一択として書いていたが、否定群を持たず
  末尾に肯定形の再固定（POSITIVE LOCKS）だけを置いて成立している例が見つかった。
  「末尾に置く」原則は同じで、**そこに置くものが否定とは限らない**という追加。
  使い分けの基準は自社未検証。検証は末尾の再固定を消して比較すれば生成2回

## 2026-08-27（差分 3／10／11）

- 追加：principles/typography.md（文字の設計）。テロップ・タイトルカード・
  モーションタイポグラフィを独立した知見層として立てた。**大半が外部情報で自社未検証**
- 追記：templates/character-sheet.md にシートの5層（Identity → Body → Wardrobe →
  State → Packaging）、理想形のレイアウト、model-facing sheet と production card の分離
- 追記：templates/shot-schema.md に文字カードのショット記述、ASSETS台帳の状態違い欄と
  文字アセット欄、参照の支配範囲
- **覆した**：シート右半分に3つの study（正面ポートレート／横顔／目のクローズアップ）を
  置く構成をやめ、顔のクローズアップ1枚にした。あわせて全身側を headless にした。
  旧根拠は「目のクローズアップが高解像度アンカーになる」（corpus/character-sheet-zjasmin）、
  新根拠は「シート内に顔が複数あると動画モデルがドリフトする」（Higgsfield公式）。
  **どちらも同一ソース由来で、どちらも自社未検証。** 高解像度アンカーは別シートへ逃がせば
  両立するため顔1枚を採った。次の案件で両形式を比較する余地は残る（切り分けコストは
  画像生成2枚分）
- 差分に含まれていた第三者シートの記載文（英文）は、**リポジトリの規則に触れるため雛形の
  形に置き換えて反映**した。原文は video-corpus 側に置く

## 2026-08-27（差分ファイルの型）

- 追加：DIFF-FORMAT.md。知見を持ち込むための受け渡し形式を固定した。
  ヘッダ（使用LLM・Web検索の有無・リポジトリの現物を見せたか）、項目ごとの6要素
  （主張／出所／検証状態／適用範囲／配置先／既存項目との関係）、数字を扱うときの規則、
  他のLLMへ渡す指示文、受け取り側のチェックリスト
- **差分を作るたびに元の会話から書き起こすため、前の差分で一般化した語が再び入り込む**
  という問題が4回続けて起きた。形式を決めて出所と配置先を差分側に書かせることで、
  受け取り側の確認を毎回の勘に頼らない形にする
- CLAUDE.md の「知見の取り込み」から DIFF-FORMAT.md を参照。反映前チェックを規則化し、
  **変更履歴や理由の説明文も検査対象**であることを明記した
- DIFF-FORMAT.md のヘッダに「publicに出す想定か」を追加。出所・適用範囲・配置先は
  形式で埋まるようになったが、**publicに出せるかの判定だけは差分の書き手にしか
  できない**。書き手ほど案件の中身に近く、混ざっていても気づきにくいため、
  欄として固定した

## 2026-08-26（差分その4）

- 追加：genres/（ジャンル別の知見）。**principles/ の一部にジャンル固有の内容が
  混ざっていることに気づいたため、分ける枠を先に作った。** 普遍則とジャンル則が
  混ざったまま増えるのが一番まずい
- **genres/ の実体は video-projects（private）に置くと決めた。** 差分その4で用意した
  crime-mystery.md は案件固有の記述が密で、一般化するとファイルを作る意味が消える。
  ジャンル別の知見は具体性が価値なので、
  **一般化ではなく置き場所で解決する**。public 側は枠と目次のみ
- 追記：OPERATIONS.md に制作の相場と歩留まり、クライアント承認の設計。
  **いずれも他社の実績値と業界標準で、自社では未検証。** 現行の進め方との差分表を併記
- 追記：principles/workflow.md の制作フローに未検証である旨の注記
- **数字の根拠を得た**：「アセットを先に作る」判断に裏付け。上流で固定すれば1ショット
  3生成、固定しなければ8〜10生成に膨れる（他社の制作記録）

## 2026-08-26（差分その3）

- 追加：principles/sound.md（音の設計）、principles/vertical.md（縦型9:16の構図）
- 追記：principles/workflow.md に冒頭3秒の設計、謎型と人物型の選択、編集の5つの破綻と処方、
  AIスロップは工程の失敗
- 追記：principles/shot-structure.md に通過・変質系のカメラ技法
- 追記：templates/character-sheet.md に服装の節
- 追記：OPERATIONS.md に保留中の検討事項（知見のSkill化、30秒ティザーのフォーマット確定）
- **覆した**：音楽を「1本の完成したトラックを探すもの」として扱っていたため、生成と選定の
  ループから抜けられなかった。探すのは起伏のない「床」だけで、ライザー・ヒット・ブラーム等は
  ライブラリで揃えて使い回す
- **覆した**：9:16の指定さえすれば縦構図になると考えていた。アスペクト比の指定と構図の指定は
  別物で、上中下の三分割で場所を書く必要がある
- **覆した**：冒頭を静かな画で始めていた。最初のフレームに動きがないと低エネルギーとして
  読まれる。ただし極小カットの連打などは文芸作品のトーンを壊すので、取る原則と捨てる原則を
  分ける
- **訂正**：「Higgsfield公式がSeedanceプロンプト用のClaude Skillを配布」は誤り。公式Skill
  9本の中身は生成API操作で、Seedanceのプロンプト作法は公式ブログ記事。Skill形式のものは
  有志製（platforms/higgsfield.md と OPERATIONS.md の両方を訂正）
- 差分その3の README 追記（sound.md / vertical.md の個別行）は見送り。2026-08-27 に
  構成節をディレクトリ単位へ整理したばかりで、個別ファイルを再び混ぜると整理が巻き戻るため

## 2026-08-27

- CLAUDE.md と OPERATIONS.md の重複を役割で解消。CLAUDE.md＝このリポジトリでの
  作業規則、OPERATIONS.md＝制作事業の運用。月次棚卸しの実施内容は OPERATIONS.md へ、
  public/private の判断は CLAUDE.md へ寄せた
- README の構成節をディレクトリとルート直下ファイルの2ブロックに分けた
- platforms/magnific.md の「2026-08-25〜」を月単位に丸めた。日付の粒度から
  案件のスケジュールが推測できたため。**platforms/ の確認日は日付のまま残す**
  （仕様がいつ時点のものか分からなくなるほうが実害が大きい）
- 追記：platforms/magnific.md に「2系統目（未定）」。OPERATIONS.md が
  「本番の生成手段は常に2系統持つ」としているのに実体が無かった。
  **決まっていないことを決まっているように書かない**

## 2026-08-26（差分その2）

- 追加：templates/asset-card.md（物アセットカードの型）
- 追記：principles/workflow.md にThink工程の制約セットと、文字作品から画を見つける手
- 追記：principles/shot-structure.md にアセット方式とファーストフレーム方式の比較、
  カメラモーションとVFXの一覧
- **覆した**：全カットで「静止画を作ってから i2v」を既定にしていたが、
  公開プロンプト6本を確認したところ composed な開始画像を渡している例は
  1本も無かった。開始構図は**文章で**固定し、画像では固定しないほうが
  カメラを動かせる。ファーストフレーム固定は、構図そのものが要点のカットに限る
- **覆した**：物の指定を単語1つで済ませていた。意味を担う物は人物と同格の
  アセットとして作る

## 2026-08-26（差分その1）

- 追加：principles/workflow.md（発想と制作の工程）、OPERATIONS.md（運用メモ）。
  初版で principles/ が「プロンプトの書き方」だけになっており、**その前段の工程が
  どこにも無かった**。この状態だと制作の頭でThink工程が飛ばされる

## 2026-08-26（構成変更）

- **corpus/ をこのリポジトリから分離した。** 収録していたのは第三者（Higgsfieldコミュニティの6名）が書いたプロンプトの原文で、publicリポジトリに他人の著作物を置く形になっていた。private の video-corpus に移し、`.gitignore` に `corpus/` を追加。履歴からも除去した
- これに伴い README の構成節・書き方のルール4・入れないものを更新
- principles/ の各項目にある `(corpus/xxx)` 参照はファイル名のみなので維持。原文の実体は video-corpus 側
- `principles/directing.md` の英語引用のうち、1文以上の長さだった2件を日本語の説明に置換

## 2026-08-26

- 初版。実写ティザー案件の制作を通じて得た知見と、Higgsfield公開プロンプト5本の分解を収録
- **覆した**：「1生成4〜5秒」は普遍則ではなくHailuo H3の制約だった。Magnific確認では多くのモデルが4〜15秒、マルチショットは1生成で最大6ショット
- **覆した**：「否定形は使わない」→ 否定は「対になる肯定」とセットなら有効。単独の否定が問題
- **覆した**：人物を静止させる指示 → AIっぽい死んだ顔の原因だった。瞬きと眉の微細表情を能動的に指定する
